## 1. SKILL.md 总览层策略文档

- [x] 1.1 新增「检索质量优化策略」章节，总结 L1 优先法、多词扩展法、锚点优先法、低召回降级方案的核心思路
- [x] 1.2 补充各阶段检索策略速查表（阶段 / 目标 / 检索模式 / 核心层 / 典型命令）
- [x] 1.3 更新「8 个提效技巧」章节，融入新的检索策略（分层检索、多词扩展、锚点优先）
- [x] 1.4 更新「决策速查表」，补充不同场景下的检索策略推荐

## 2. Explore 阶段检索策略升级

- [x] 2.1 替换单一 `wpw graph context` 调用为「L1 定位 → 下钻」两阶段检索
- [x] 2.2 补充多词扩展检索词生成指南（业务概念 + 同义词 + 技术术语）
- [x] 2.3 补充低召回降级流程（L1 查不到时的三级降级方案）
- [x] 2.4 补充 --multi 并行查询的使用模板

## 3. Design 阶段检索策略升级

- [x] 3.1 替换单一 `wpw graph context` 调用为分层检索（先 L1 定位模块边界，再 L2/L3 看细节）
- [x] 3.2 补充多词扩展检索词生成指南
- [x] 3.3 补充上下游依赖查询的组合使用（确认模块边界后查影响范围）

## 4. Plan 阶段检索策略升级

- [x] 4.1 替换单一 `wpw graph context` 调用为 L1/L2 分层检索
- [x] 4.2 补充多词扩展检索词生成指南
- [x] 4.3 明确任务拆解应落到文件级，为 Apply 锚点模式做准备

## 5. Apply 阶段锚点优先模式

- [x] 5.1 将「获取代码上下文」步骤改为锚点优先：已知文件路径时直接用 --anchors
- [x] 5.2 补充锚点模式的判断逻辑（什么情况用锚点、什么情况退化为语义检索）
- [x] 5.3 补充语义检索兜底时的 L2/L3 定向检索策略
- [x] 5.4 更新代码上下文获取的命令模板（锚点模式 + 兜底模式各一套）

## 6. CR / Test 阶段检索策略补充

- [x] 6.1 CR 阶段：补充影响面分析的分层策略（先模块级、再文件级）
- [x] 6.2 Test 阶段：补充回归范围确定的图谱使用模式（上下游 + 业务映射 + 模块级）
