# -*- coding: utf-8 -*-
"""Build an AIGC-rewritten thesis copy without modifying the source DOCX."""

from __future__ import annotations

import copy
import hashlib
import re
import shutil
from pathlib import Path

from docx import Document
from docx.enum.text import WD_ALIGN_PARAGRAPH
from docx.oxml.ns import qn
from docx.text.paragraph import Paragraph


SOURCE = Path(
    r"F:\project\web-project-workflow\论文\基于大模型的美食推荐系统设计与实现--面向AI辅助开发的上下文工程方法研究.docx"
)
TARGET = Path(
    r"F:\project\web-project-workflow\论文\基于大模型的美食推荐系统设计与实现--面向AI辅助开发的上下文工程方法研究_AIGC降重送检版.docx"
)
CHAPTER1_MARKDOWN = Path(r"F:\project\web-project-workflow\论文\1.绪论.md")
SOURCE_SHA256 = "9DA51ABD570542411F8E67FC65A207776F605F2C401CDD3441A03E89862FDBCD"


REWRITES: dict[int, str] = {
    60: """大语言模型能力的提升，使 AI 辅助开发逐步越过代码补全的范围，开始参与需求分析、系统设计、开发计划、测试和实现等软件工程活动。不过，当开发对象变成结构复杂且持续演化的软件项目时，AI 编程智能体仍会受到上下文不足、项目知识分散和开发过程难以控制等因素的影响。扩大上下文会增加使用成本，也不能消除“中间丢失”现象；需求文档、代码和项目规则彼此分离，模型难以形成完整认识；模型输出带有非确定性，而工程流程需要明确的阶段、输入和结果。本文针对这些问题研究面向 AI 辅助开发的上下文工程方法。该方法将需求、设计、计划、测试和实现纳入同一开发框架，利用三段式契约规定智能体在各阶段的输入、处理和输出，并通过状态留痕保存过程变化。为使软件项目知识能够持续利用，本文建立了由能力知识层和代码结构层组成的双层知识组织模型，将业务能力与相应的代码实现关联起来，原本分散在文档、代码和规则中的信息由此进入统一的知识结构。智能体执行具体任务时，系统根据任务内容检索图谱，在候选结果上完成子图裁剪和代码骨架抽取，再压缩为可供模型使用的结构化上下文。生成过程受 Token 预算限制，目标是在控制输入规模的同时保留与任务高度相关的信息。上述方法由 wpw 工具实现。本文选取基于大模型的美食推荐系统作为实验载体，在受控条件下比较四种 AI 辅助开发方式，并设置三组机制消融实验。实验从开发成本、系统实现质量、上下文效率和流程可控性四个方面进行评价。结果表明，本文方法采用的流程约束、知识组织和上下文生成机制能够改善智能体获得的上下文质量，并提高 AI 辅助开发过程的可控性。""",
    196: """大语言模型（Large Language Model, LLM）已把 AI 编程智能体（coding agent）带入软件开发一线。智能体进入长期项目后，三个相互关联的上下文难题也随之显现：业务能力与代码结构彼此割裂，持续增长的代码库超出模型窗口的有效利用范围，项目演化过程中形成的知识难以长期积累。本文把这一组现象称为项目知识失忆。研究的核心问题，是如何把软件项目知识整理成智能体能够长期使用的结构化模型，并据此提供与当前任务相关的上下文。本文以基于大模型的美食推荐系统为实验载体，对面向 AI 编程智能体的软件项目知识建模与上下文生成方法进行研究和验证。""",
    200: """生成式人工智能进入软件工程的方式已发生明显变化。ChatGPT 主要体现通用对话能力，GitHub Copilot 将大模型用于代码补全，Claude Code 则以终端智能体工具的形态参与开发[28]。这一演进使大模型的作用从“单点补全”延伸到“全流程参与”，能够自主规划、调用工具并连续执行任务的 AI 编程智能体逐渐成为主要形态。Hou 等人对 395 篇 LLM 软件工程（LLM4SE）文献的系统综述也记录了相同变化：相关应用不再局限于代码生成、补全和解释，已经覆盖需求工程、软件设计、开发、质量保障与维护[1]。2025 至 2026 年的实证研究进一步在大量真实开源项目中发现了 coding agent 的使用痕迹[2]，并系统刻画了真实仓库中的 AI 辅助开发活动[3]。应用模式由“调用模型生成内容”转向“由智能体完成任务”后，任务规划、工具调用、状态维护和长程执行成为必要能力。Agent 运行框架（Agent Harness）由此出现，SWE-agent 等研究则对智能体与计算机交互的接口进行了系统设计[17]。""",
    201: """模型在单次交互中能够利用多少信息，并不只取决于名义窗口长度，还同时受到注意力分布和调用成本的限制。当前 AI 编程工具通常借助项目指令、会话状态、代码索引或持久化记忆提供跨任务上下文，但这些能力大多封装在工具内部，或者依靠文档式知识维护。它们尚未构成统一且能随项目演化更新的软件项目知识模型。因此，模型能力迁移到真实软件项目时仍会遇到重要的工程约束，长期项目知识问题也由此产生。""",
    203: """开发对象若只是一个函数或文件，上下文限制通常并不突出；当对象扩大为包含数十乃至上百个模块、持续演化的真实项目时，限制会集中表现为三类相关问题。本文将其统称为项目知识失忆。""",
    204: """第一类问题来自代码库规模。真实项目往往包含数十万乃至百万行代码，全量输入模型并不现实。输入规模增加会线性推高 Token 成本，而信息利用能力并不会同步增长。已有研究发现，即使模型支持长上下文，相关信息所处的位置仍会显著影响利用效果，这就是“Lost in the Middle”现象[4]。整库输入还会引入大量噪声，使关键信息更难定位。真实开发中的智能体通常依靠人工检索或整库注入来取得任务上下文[3][10]：人工检索效率较低，也容易遗漏；整库注入则同时面对 Token 成本和信息稀释。""",
    205: """第二类问题是业务知识与代码结构之间缺少联系。软件系统知识并不等于代码文本，需求、设计、架构、实现和演化知识都属于项目知识，代码只是其中一种载体。以“增加会员积分功能”为例，智能体既要判断需求所属的业务模块，也要找到相关文件和接口，还需理解历史设计原因。这些信息分别存在于需求文档、代码注释、提交历史和开发者经验中。传统代码检索回答的是“哪些代码与查询相关”，返回结果以文本或符号匹配为主；它无法直接说明某项业务能力由哪些代码共同实现，也不能维护这些关联随需求演化发生的变化[18]。业务与代码缺少关联后，智能体难以准确理解项目语义，跨模块修改遗漏和依赖破坏也更容易发生。""",
    206: """第三类问题出现在项目的长期演化过程中。对话历史会记录部分交互内容，却不足以成为可持续、结构化的项目知识资产。项目指令、代码索引和持久化记忆虽已被现有工具采用，但需求迭代中形成的业务知识、需求关系及代码关联仍缺少统一模型。每次开始新任务，智能体往往需要重新理解项目。需求迭代、功能增强和代码重构又会持续改变软件项目；如果没有项目级知识资产，上下文构造就无法利用以往的演化结果，相同的理解成本会在后续任务中反复发生[3][10]。""",
    208: """这些问题使上下文工程（Context Engineering）成为连接大模型能力与软件工程过程的一层关键机制。它围绕智能体当前的任务目标，对外部知识、代码结构和执行状态进行组织、选择、压缩与供给[5][6]。检索增强生成（Retrieval-Augmented Generation, RAG）通过按需引入外部非参数化知识，缓解了参数化知识访问和更新受限的问题[5]。2025 至 2026 年的智能体研究又进一步表明，仓库级上下文（repository-level context）与可执行反馈已成为软件工程智能体的重要资源[6]。""",
    209: """上下文能否发挥作用，很大程度上取决于其背后的知识如何组织。知识图谱以实体和关系显式表示领域知识，是较为成熟的结构化组织方式[37]。据此，可以把业务能力、代码结构和演化历史整理为持续维护的项目知识模型，再从中生成与任务相关的智能体上下文，这构成了本文的研究切入点。近年的上下文检索、压缩、记忆管理和工具调用研究正在汇聚为上下文工程方向。“如何用好大模型”的关注点也随之从单次提示词设计（Prompt Engineering）和临时检索，转向系统性的知识建模与上下文组织。""",
    210: """本文关注软件项目知识的组织方式和模型输入上下文的质量。基础模型与 Agent 运行框架（Harness）已经具备代码生成和任务执行能力，本文不再讨论如何提升这些基础能力，而是研究长期演化项目中的高质量上下文如何构建、管理与复用。""",
    216: """本文围绕 AI 编程智能体所需的软件项目知识和任务上下文展开研究。能力-代码双层知识组织模型负责建立业务语义与代码结构的联系，Spec 增量知识演化机制维护项目变化中的知识积累，图谱驱动的任务上下文生成方法则把相关知识送入智能体。三项设计共同回答“长期演化项目中的知识如何结构化地进入智能体上下文”，并为这一新兴问题提供方法与实验依据。""",
    218: """在工程应用层面，本文给出了软件工程智能体上下文构建、管理和复用的一条实现路径。wpw（Web Project Workflow）系统承担方法载体的作用：结构化知识供给用于减少智能体取得项目知识的成本，增量知识演化适应项目的持续变化，上下文压缩则控制 Token 开销。本文随后以基于大模型的美食推荐系统开发过程进行端到端验证。研究问题来自实际工程中的 AI 辅助开发困难，因此整套方法具有明确的问题驱动特征。""",
    220: """相关研究分别从国外和国内两个视角梳理。国外研究按大模型代码生成、AI 软件工程 Agent、Spec/Workflow 驱动开发、上下文工程、软件知识图谱与 Repository 智能五条线索展开；国内部分关注政策环境、大模型与编程工具生态，以及相应的学术研究和工程实践。在此基础上，本节再归纳现有工作的不足。用于对比的 Spec Kit、OpenSpec 和 Superpowers 都是快速迭代的开源项目，版本与生态资料的检索截止到 2026 年 8 月。文中能力描述均对应实验采用的具体版本，版本号、发布日期和代码提交记录在附录实验环境表中固定下来，以保证复现；这些描述不被视为永久不变的“产品属性”。""",
    222: """（1）大模型代码生成研究。代码生成研究从 Code Llama 等代码专用预训练模型[30]，逐步发展到 GPT、Claude 等兼具代码推理能力的通用模型系列；国产代码模型另在国内研究现状中讨论。LLM4SE 系统综述[1]对代码生成、补全、翻译和解释任务的能力边界及评估方法进行了系统梳理。上述模型能力是本文工作的基础。随着“模型能否编写代码”逐渐得到肯定回答，真实项目中的有效上下文从哪里获得，成为新的研究重点。本文不改进模型的代码生成能力，而是在现有模型能力之上研究输入上下文的组织方法。""",
    223: """（2）AI 软件工程 Agent 研究。该方向主要研究软件工程任务的规划和执行。SWE-bench 以真实 GitHub issue 构建软件工程任务基准[16]，SWE-agent 设计了语言模型智能体与计算机交互的接口[17]；Devin[32]、OpenHands[33] 等工程系统则已经具备任务规划、文件编辑、命令执行和自我修复能力。2025 至 2026 年，研究范围进一步进入真实工程环境。文献[2]分析十余万个 GitHub 项目并估计 coding agent 的采用情况，文献[10]考察真实项目中智能体重构的类型、动机和代码质量变化。国内关于智能体的实证研究及综述将在后文单独介绍。""",
    224: """该方向早期集中在任务规划、工具执行和环境交互，近年的讨论又加入智能体记忆、仓库级上下文与长期任务管理。不过，面向长期软件演化，把需求语义、业务能力和代码结构放入同一知识模型并持续维护的研究仍较少。""",
    226: """项目级规范治理、变更级规格管理和技能化执行纪律代表了三种工程路线。GitHub Spec Kit 以 Constitution、功能 Specification、技术 Plan 和任务分解为核心制品，借助 Clarify、Checklist、Analyze、Converge 等质量门禁组织开发，后继版本还增加了通用工作流引擎与分层扩展结构[8]。OpenSpec 以动作（Action）为中心建立工作流和制品图（artifact graph），面向存量项目管理变更级规格制品的生命周期，并把“依赖”定义为使能条件而非强制关卡[8]。Superpowers 则通过可组合技能和强制工作流规范执行，覆盖需求澄清、计划编写、子代理驱动开发、测试驱动开发及多层代码审查，强调“工作流是强制要求而非建议”[9]。""",
    227: """这些工作通过管理 spec、workflow 与 execution，对 AI 开发中的过程约束作出了有效回应，智能体执行的可靠性和过程可审计性因而得到改善。本文方法与它们属于互补关系，机制差异将在第 5 章进一步比较。表1-1列出了三类代表性工具与本文方法在知识对象上的区别。截至本文检索时点，尚未发现以三者为对象的同行评审系统比较研究，因此相关事实均以各自官方仓库和文档为依据。""",
    229: """由以上比较可见，现有方法主要管理 spec、workflow 和 execution 相关制品及过程，知识对象集中在规格制品、变更状态和执行活动。本文在这一基础上继续研究规格知识与代码结构之间如何形成长期、显式的联系，以及这种联系怎样用于生成任务级上下文。两类工作并不相互替代。""",
    230: """（4）上下文工程研究。该方向研究如何组织智能体输入。RAG 在基础机制上引入外部非参数化知识，以弥补参数化知识的限制[5]；“Lost in the Middle”现象促使研究者重新关注文档压缩和上下文选择[4]；GraphRAG 等方法则用实体-关系图连接检索与生成[19]。代码场景中的研究更多围绕仓库级生成展开。CodePlan 以规划方式组织仓库级修改的链式执行[35]，RepoHyper 构建代码结构超图并据此检索上下文[36]，CodeSearchNet 则明确了自然语言-代码检索任务并发布大规模数据集[12]。国内关于仓库级代码补全的研究将在后文介绍。""",
    231: """这些研究分别解决检索、Prompt 上下文构造或仓库级生成问题，但多数上下文仍针对单次任务临时形成。任务结束后，检索结果与子图不会继续积累为项目级知识资产；业务语义与代码结构之间的关系，也没有在知识层面获得显式表示。""",
    232: """（5）软件知识图谱与 Repository 智能研究。现有工作把图结构用于软件知识组织，大体形成了代码结构图谱、需求-代码追踪和 Repository 知识图谱三个方向。""",
    233: """代码结构图谱以代码属性图（Code Property Graph）为典型代表。该模型把抽象语法树、控制流图和程序依赖图组织为统一、可查询的图，是代码结构知识图式表达的经典范式[18]。它能够细致描述代码实体之间的结构关系，但节点和边仍定义在程序结构层，业务语义不在其表示范围内。""",
    234: """需求-代码追踪（Traceability）长期研究如何自动恢复需求制品与代码之间的联系[13][14][15]，因而与本文的业务-代码关联问题直接相关。已有方法通常把一次性链接恢复作为目标，恢复结果还没有成为可随项目变化持续维护的知识资产。国内该方向的近期研究将在后文介绍。""",
    235: """Repository 知识图谱在 2025 至 2026 年出现了一批面向仓库级智能体的新工作。有研究用代码仓库图谱增强演化代码库中的仓库级代码生成[39]。Repository Intelligence Graph 则为 Claude Code、Cursor、Codex 等编码智能体提供确定性的仓库架构图上下文；在 8 个仓库和 3 种商用智能体上的实验中，平均准确率提高 12.2%，完成时间降低 53.9%[40]。国内 KGCompass 等工作将在下一部分讨论。这些结果说明，“代码图谱服务 Agent 上下文”已经得到实验支持，并形成了明确的研究趋势，也为本文提供了直接参照和比较对象。""",
    237: """（1）大模型技术与编程工具生态。国内大模型技术快速发展，政策层面也给出了明确支持。国务院在 2025 年发布《关于深入实施“人工智能+”行动的意见》，提出推动人工智能与经济社会各领域融合，并加快智能体等场景落地[20]。产业生态呈现多主体和多技术路线并行的格局，通义千问[21]、文心[22]、混元[23]、豆包[24]、GLM[25]、Kimi[26]、DeepSeek[27]等模型与平台具有代表性。代码模型已形成 DeepSeek Coder[31]、通义千问代码系列和智谱 CodeGeeX 底层模型等系列；在工具端，CodeGeeX、通义灵码等国产编程助手也在推动大模型进入软件工程实践[29]。""",
    238: """（2）AI 软件工程智能体实证与综述。国内研究与国际进展基本同步。文献[3]利用 AIDev 数据集分析真实仓库中的 AI 辅助开发活动，不仅描述了活动特征，也揭示了智能体产出可信度与人类之间的差距。关于智能体自演化的综述则梳理了仓库级上下文、可执行反馈等智能体资源，并总结了智能体记忆机制的研究进展[6]。""",
    239: """（3）代码表示与仓库级理解。微软亚洲研究院提出 GraphCodeBERT，在代码表示预训练中显式使用数据流结构，是结构信息改善下游任务效果的代表性工作[11]。浙江大学团队开发的 CatCoder 把静态分析与检索结果结合起来，用于上下文感知的代码补全[34]。两项工作都属于仓库级代码理解研究，但其组织对象仍是代码层结构关系，没有把业务语义纳入模型。""",
    240: """（4）需求-代码追踪。南京大学团队提出的 TRIAD 使用共识词对和制品之间的传递链接，自动恢复需求到代码的追踪关系[41]。它直接涉及本文关注的业务-代码关联问题，不过目标仍是一次性恢复链接，尚未形成能够随项目演化持续更新的知识资产。""",
    241: """（5）仓库级知识图谱与工业实践。KGCompass 由燕山大学牵头、多家机构合作完成。该方法把 issue、PR、文件、类和函数组织为仓库感知知识图谱，通过图路径帮助智能体定位修复位置，在 SWE-Bench-Lite 上完成效果验证，并支持图谱随仓库演化增量更新[38]，代表了国内“代码图谱服务 Agent 上下文”的研究路线。工业实践中，中国工商银行软件开发中心把程序血缘分析用于软件工程，通过抽象语法树和字节码分析构建全链路程序血缘，以感知交易变动并分析影响范围[42]。这一案例也说明，大型金融系统确有连接业务与代码的现实需求。""",
    242: """国内研究与实践可以归纳为三条主要路线。代码大模型与编程助手以 DeepSeek Coder、CodeGeeX、通义灵码等为代表，重点在模型能力和开发工具形态；智能体与企业软件开发路线开始把工具调用、代码检索及代码库索引引入真实流程；软件知识图谱与代码知识工程则通过 KGCompass、程序血缘分析和需求追踪，探索结构化代码知识如何支持开发活动。国内已经出现“代码知识服务 Agent”的研究和工业案例，但从“Spec 与业务能力”延伸到代码及长期上下文的完整链路仍不多见。与本文直接相关的方法研究，目前更多来自国外学术界和开源工程社区。""",
    244: """国内外已有研究在代码语义检索、仓库级代码生成、代码知识图谱、软件制品追踪和 AI Agent 工作流等方面形成了较为充分的技术积累。KGCompass、Repository Intelligence Graph 等近期工作还通过实验说明，结构化仓库知识能够改善智能体的代码定位和任务执行效果[38][40]。""",
    248: """AI 编程智能体进入长期项目后，会出现本文所说的项目知识失忆：业务能力与代码结构缺少联系，代码库规模超过窗口的有效利用范围，项目演化也没有带来可持续的知识积累。国内外研究已经分别改进了代码检索、需求追踪和仓库级理解，但能够同时维护业务能力、代码结构与演化信息的软件项目知识模型仍然缺乏。本文的研究边界由此确定。基础模型和 Agent 运行框架已经负责代码生成与任务执行，现阶段的短板在输入上下文质量。因此，本文不改进模型本身，而把目标限定为项目知识的组织与上下文供给，并针对三类失忆问题分别设定可以实验检验的目标。""",
    250: """（1）建立业务能力与代码结构的统一知识表示。智能体理解项目语义时，业务知识和代码结构相互割裂是首要障碍。普通代码检索只能返回文本或符号匹配结果，无法回答某项业务能力由哪些代码实现，以及这些代码为什么这样组织。本文据此把业务能力、模块、文件、函数及其联系放入同一项目知识模型，使其能够被显式表示、查询和维护，业务语义也能与代码结构相互对应。""",
    251: """（2）建立任务相关上下文的生成方法。大型代码库无法整体输入模型，Token 成本和信息利用能力都会成为限制；人工查找相关代码不仅耗时，也可能遗漏关键信息。本文要解决的问题，是在给定 Token 预算内从项目知识中自动选取内容，形成与任务高度相关的最小充分上下文。""",
    252: """（3）建立项目知识的持续积累机制。现有 AI 编程活动主要依赖会话，对话历史在会话结束后难以继续发挥作用，后续任务仍要重新理解项目。本文希望项目知识能够随着需求迭代和代码演化增量更新，让智能体在长期开发中复用已经形成的项目认识。""",
    254: """三个研究目标对应一条逐层推进的技术路线。知识建模先形成可维护的项目知识，上下文生成再从中选取任务所需信息，增量演化负责在项目变化后更新这些知识；流程约束架构把三项机制纳入同一开发过程。""",
    255: """知识建模采用能力-代码双层知识组织模型，具体设计见第 3 章。Spec 是业务能力层的知识来源，多语言源码解析则生成模块、文件和函数三级代码结构。业务能力与代码的关系不要求人工逐条标注，系统把文档提取、命名匹配、语义匹配和 Git 历史四类启发式证据交给 noisy-OR 概率模型融合，得到关联边。图谱以 JSONL 文件和本地向量索引存储，无需部署数据库，普通开发环境即可运行。""",
    256: """任务上下文由图谱驱动的生成方法提供，方法细节同样位于第 3 章。系统根据任务描述，用置信度衰减权重选择锚点，再通过加权双向 BFS 取得任务级子图。距离感知的骨架抽取会去除与任务无关的细节，随后按不同压缩档位完成序列化。Token 预算贯穿整个过程；输出超出预算时，系统按级别降低信息粒度，直到得到任务所需的最小充分上下文。""",
    257: """项目变化由 Spec 增量知识演化机制处理，具体过程见第 3 章。新项目通过“源码扫描 + 需求 Spec”建立初始知识；存量项目出现新需求后，先把需求规格化为 Spec，再关联已有能力节点并更新相应代码关系。该过程只处理受影响部分，不需要重新构建整个图谱。""",
    259: """第 5 章采用受控工程实验检验方法有效性。实验载体为基于大模型的美食推荐系统，任务包括绿地开发（T1）、存量迭代（T2/T3）和多轮需求演化（T4）。四种上下文管理范式用于评价整体效果，分别为无上下文工程基线、规格驱动、Agent 流程和完整上下文工程方法；知识组织、上下文生成、流程约束三组消融与组件级基线用于区分各机制贡献。T4 单独考察长期积累，记录各轮上下文构造 Token、相关代码召回率和人工定位次数的变化，分析积累效应是否出现、出现时间及收益是否饱和，不预设结果方向。评价覆盖开发成本、系统实现质量、上下文效率、长期演化和流程可控性，并通过信息约束与版本锁定保持比较公平。""",
    263: """现有代码知识组织存在两项与本文直接相关的限制。代码属性图等代码图谱描述程序实体之间的结构关系，节点和边都停留在程序结构层，缺少业务语义；需求追踪方法通常以恢复需求与代码的静态链接为目标，恢复结果没有成为长期维护的知识资产。本文因此设计 Spec 驱动的 Capability-Code 双层知识组织模型，具体内容见第 3 章。模型以 Spec 为知识来源，把 C 层业务能力与 L1 模块、L2 文件、L3 函数组成的代码结构相连，形成 Spec -> Capability -> Code -> Evolution -> Context 知识链。Spec 记录需求来源与内容，Capability 层表示项目具备的业务能力，代码结构层说明能力由哪些代码实现；演化机制负责更新关联，上下文生成方法负责从中选取知识。""",
    264: """双层模型需要自动确定业务能力与代码之间的联系。本文使用文档提取、命名匹配、语义匹配和 Git 历史四类启发式证据，通过 noisy-OR 概率模型融合为 business_map 关联边。各证据源具有明确的权重区间和剪枝规则，因而不必依赖人工标注；每条关联边同时保存证据来源，便于追溯。""",
    267: """按任务临时拼接检索结果，既没有联合控制任务相关性和 Token 开销，也无法保证上下文能够继续复用。本文在第 3 章提出任务上下文生成方法，其目的并非完成代码检索，而是为 Agent 提供最小充分上下文：给定 Token 预算后，尽可能提高所选上下文与任务的相关性。""",
    268: """该方法把面向 Agent 的任务级子图生成分为四个阶段。锚点选择利用置信度衰减权重确定入口，让高置信度的业务能力和代码节点优先进入子图；子图裁剪采用加权双向 BFS，并受边权重和节点数量预算约束；骨架抽取依据节点到锚点的距离，分级保留结构信息并去除无关细节；序列化提供多档压缩结果，以适应不同 Token 预算，超出预算时再按既定级别降级。""",
    269: """该方法依托持续维护的项目知识模型，而不是每次任务临时检索、使用后即丢弃。上下文生成的各阶段均可以控制和配置。第 5 章通过 Token 缩减率、上下文召回率与下游任务表现，并结合消融实验和组件级基线评价其效果。""",
    272: """Spec 增量知识演化同时处理新项目和存量项目。新项目以“源码扫描 + 需求 Spec”形成初始知识模型，完成知识冷启动。存量项目新增需求时，先把需求整理为 Spec，再关联已有 Capability 节点并更新受影响的代码关系，不必全量重建图谱。随着多轮需求演化，项目知识沿业务能力主线逐步沉淀，可供智能体在后续任务中直接使用。""",
    273: """这里的知识积累属于工程过程，不是机器学习训练，因而不依赖训练数据或模型更新。与以代码实体为中心的仓库级图谱不同，该机制把 Spec 作为演化单元，用于维持长期项目中知识模型的时效性。第 5 章的 T4 多轮需求演化实验将检验这种积累是否产生效果。""",
    275: """wpw（Web Project Workflow）系统是三项方法创新的统一实现载体，系统设计见第 4 章。整体架构分为 AI 层、CLI 层和文件系统层：AI 层提供 Skill 命令，CLI 层管理状态与依赖，文件系统层保存图谱和阶段制品。在三层架构上，系统实现工作流、图谱构建、上下文生成及增量维护四个核心模块。多语言源码解析、JSONL 与本地向量索引构成支撑技术，使系统无需数据库也能部署在普通开发环境中。""",
    276: """第 5 章以基于大模型的美食推荐系统验证本文方法。任务集由绿地开发（T1）、存量迭代（T2/T3）和多轮需求演化（T4）组成，实验安排四种上下文管理范式的受控比较、三组消融及组件级基线。信息约束和版本锁定用于保证比较公平。开发成本、系统实现质量、上下文效率、长期演化和流程可控性五类指标共同检验 1.4 节所列目标，并用于识别方法的适用边界。""",
    277: """四个创新点承担不同层次的作用。前三项属于方法创新，创新点 4 中的 wpw 系统负责承载和实现这些方法；第 3 章的方法总体框架与流程约束架构，则把三项机制组织成完整设计。noisy-OR 聚合、双向 BFS、JSONL 与向量存储、多语言源码解析等内容属于支撑实现，在相应章节作为技术细节说明。""",
    282: """现代大语言模型通常采用 Transformer 架构[43]。架构的核心是自注意力机制，序列中的每个位置都能直接关注其他位置。查询向量与键向量的相似度经过缩放和归一化后形成注意力权重，模型再用这些权重对值向量求和；多头注意力把同一过程放在多个子空间中并行计算。其计算形式如下：""",
    285: """大语言模型同时具有清晰的能力边界。窗口长度限制了单次交互可利用的信息，模型参数中的知识也无法自动反映软件项目的最新状态，实时项目知识仍要依靠外部上下文、工具调用或持久化知识机制提供；生成内容还可能出现与事实不符的“幻觉”。因此，需要把知识区分为三种形态：模型参数中相对稳定的参数化知识，项目长期演化形成的外部项目知识，以及当前任务临时使用的上下文。本文所说的项目知识资产属于第二类。模型要可靠参与真实项目开发，必须获得外部知识，并对输入内容的质量加以控制，这正是本文上下文组织方法的立足点。""",
    287: """AI 编程智能体把大语言模型组织成可以自主执行开发任务的软件主体。与本文相关的能力主要包括规划、工具使用和状态/记忆管理。规划负责把任务目标拆解为可执行步骤，并在执行过程中调整顺序；工具使用通过函数调用、命令执行等方式连接外部环境。ReAct 将推理和行动交替组织，使模型能够在多步任务中一边执行一边观察[45]，Toolformer 则说明模型可以在训练中学习何时以及怎样调用工具[46]。状态和记忆管理用于跨步骤、跨会话保存中间结果与历史经验。Agent 运行框架（Agent Harness）把这些能力封装为文件编辑、命令执行和状态维护接口，SWE-agent 对智能体-计算机接口进行了系统研究[17]。在此基础上，Devin[32]、OpenHands[33]、Claude Code[28]等软件工程智能体已经具备端到端任务执行能力。""",
    288: """现有 Agent 运行框架为规划、工具调用和多步执行提供了较成熟的工程基础。进入真实软件项目后，任务结果仍同时取决于执行能力和输入上下文质量，实证研究也发现智能体产出与人类开发者之间存在差距[3]。本文不讨论智能体执行算法，而把研究重点放在项目级上下文的供给上。""",
    292: """上下文窗口规定了模型一次能够处理的信息总量，但窗口中的内容不会被同等利用。当相关信息处在长上下文中部时，模型的利用效果明显低于信息位于首尾的情况，形成 U 形位置效应，即“Lost in the Middle”现象[4]。输入增长还会增加处理开销和调用成本，大量无关内容也可能稀释关键信息。名义窗口长度与有效利用长度并不相同，这说明软件工程中的上下文困难不仅涉及容量，也涉及相关内容如何选择、组织和压缩。本文的任务上下文生成方法正是基于这一认识设计。""",
    297: """检索增强生成（Retrieval-Augmented Generation, RAG）是组织模型外部输入的一项基础机制。它先从非参数化知识库中找到与问题相关的内容，再把检索结果加入提示并生成答案[5]。模型因此能够按需访问参数之外的知识，知识更新也不必依赖重新训练。RAG 常用的检索单元是平面切分的文本片段，代码元素之间的结构关系难以直接表达；在软件项目中，仅检索文档片段往往不足以还原程序结构。GraphRAG 将实体和关系组织成图，随后利用社区划分与层级摘要处理跨文档的全局问题[19]，为本文把项目知识从扁平文本改为结构化关系提供了方法参照。不过，GraphRAG 面向通用文档语料，软件项目的实体类型、关系语义和更新机制仍需结合代码与业务重新设计。""",
    299: """代码任务所需的上下文通常来自整个仓库。CodeSearchNet 定义了自然语言-代码检索问题并发布大规模数据集，为语义代码检索及后续嵌入方法提供了数据基础[12]。仓库级代码生成随后把检索与生成进一步结合：CodePlan 将仓库级修改拆解为存在依赖的编辑链，并按顺序执行[35]；RepoHyper 建立代码结构超图，通过“搜索-扩展-精炼”为补全位置选取相关子图[36]。知识图谱路线中，KGCompass 把 issue、PR、文件、类和函数组织为仓库感知图谱，用图路径引导任务定位，同时支持图谱增量更新[38]；Repository Intelligence Graph 为商用编码智能体提供确定性的架构图上下文，并在多个仓库和多种智能体上完成验证[40]。ContextBench 等基准则用于系统评价编码智能体的上下文检索能力[58]。这类研究主要服务于具体任务的上下文获取与生成，部分方法已经开始持续维护仓库知识。然而，需求规格所表达的业务能力还没有普遍成为长期语义节点，也较少通过与代码结构的联系参与后续任务。本文并非认为现有仓库图谱不能表示业务语义，而是把业务能力明确设为一等知识对象，并由 Spec/Change 驱动其生命周期，以此系统化相应的组织和演化机制。""",
    301: """上下文压缩回答“已经选中的内容怎样进入窗口”。抽取式方法保留关键片段并删除冗余，生成式方法把长文本改写为摘要，LLMLingua 等提示压缩方法则在尽量保持任务效果的条件下减少提示 Token[56]。已有研究多处理自然语言或一般上下文中的 Token 级压缩。本文面向代码结构知识，先在图结构上裁剪子图，再压缩节点属性和序列化结果。结构化序列化使用符号化、分层格式表示代码结构，目标是在保留核心关系的同时减少冗余文本；第 5 章将用实测数据检验 Token 开销是否降低，并据此评价输出格式。时间维度上的上下文由智能体记忆扩展，智能体可以跨步骤和会话保存中间结果与历史经验[57]。Agent Workflow Memory 等工作已经尝试从编码智能体的历史执行中提炼可复用知识[59]，记忆也被视为自演化编码智能体的重要资源[6]。但通用记忆多面向会话或单次任务，并不等同于项目知识模型。本文以 Capability-Code 结构化知识作为项目记忆，使其随项目演化持续维护。""",
    302: """窗口与预算、外部知识来源、结构选取和时间延续分别由上述技术处理。本文不重新发明这些机制，而是在项目知识组织层面把它们连接起来：持续维护的项目知识模型作为基础，检索与压缩在图谱上完成，记忆则以项目知识资产的形式长期保存。""",
    306: """抽象语法树（Abstract Syntax Tree, AST）是代码结构分析的基础。解析器把源码转换成树，树节点对应语法构件，函数、类、导入及其嵌套关系可以由此提取。tree-sitter 支持多语言和容错解析，并能在代码编辑时增量重建语法树[52]。在 AST 之上，代码属性图（Code Property Graph, CPG）把抽象语法树、控制流图与程序依赖图合并为一个可查询图模型，支持跨结构分析，已经用于漏洞挖掘等任务[18]。这些技术直接支撑本文模块-文件-函数三级代码结构层。CPG 的标准模型主要描述程序结构和依赖语义，并不把业务能力作为核心对象；本文因此在代码结构之上增加业务能力层，用于表示需求语义与代码实现的关系。""",
    308: """知识图谱用实体、关系和属性显式表示领域知识，还可以结合规则或图算法完成关联查询与推理[37]。构建过程一般包括本体设计、知识抽取和知识融合。本体规定实体类型、属性及关系模式，抽取环节从数据源识别实体并建立联系，融合环节负责实体对齐和冲突消解。领域知识图谱通过定制本体适配特定语义，软件领域的代码知识图谱属于这种类型。""",
    309: """本文主要使用知识图谱中的关系溯源和增量更新机制。关系溯源（provenance）为图谱关系保存证据来源及置信度，使知识可以追查和审核；这些信息既能解释检索结果，也能支持置信度更新与错误定位。本文据此用多源启发式证据建立业务-代码关联，并保留每项证据。增量更新则允许图谱随数据源变化进行局部维护，不必每次全量重建，对应 Spec 增量知识演化机制的维护方式。存储方案可以选择 Neo4j 等图数据库，也可以采用文件式轻量存储。前者具有成熟查询能力，后者在部署成本和数据可控性上更有优势，本文的选择及依据见第 3、4 章。""",
    311: """嵌入向量把文本或代码对象映射到低维稠密空间，语义相近的对象因而具有接近的向量表示。BERT 显著改善了句段级语义表示[44]，检索型文本嵌入又把训练目标调整为“相关文本向量相近”；C-Pack 提出的 BGE 系列是中文场景的代表[47]。代码表示方面，GraphCodeBERT 把代码结构信息加入预训练过程[11]。工程实现通常用余弦相似度计算语义匹配，并以近似最近邻索引提高检索速度。本文在两个环节使用嵌入向量：知识建模把语义相似度作为业务-代码关联的一类证据，上下文生成则通过向量检索对齐任务描述与项目知识。""",
    316: """Spec 驱动开发（Spec-Driven Development）是一类约束 AI 开发过程的工作流方法。Piskala 将其概括为开发活动从“代码即事实源”（code-as-source）转向“规格作为首要制品”[7]。不同实践对规格的约束程度不同，形成规格先行（spec-first）、规格锚定（spec-anchored）和规格即事实源（spec-as-source）等形态；共同点是由规格组织开发活动。此类方法以规格和流程制品约束智能体行为，主要解决过程可审计性问题。工程社区已有多种实现，本文选择其中三个作为比较对象，能力描述均对应实验采用的版本，版本信息记录在附录实验环境表。""",
    318: """GitHub Spec Kit 面向项目级治理。开发活动由 Constitution（项目规范）、Specification（功能规格）、Plan（技术计划）和 Tasks（任务分解）四类制品组织，Clarify、Checklist、Analyze、Converge 等质量门禁用于检查各阶段输出[8]。""",
    323: """本文使用基于大模型的美食推荐系统作为实验载体，因此需要交代其业务实现所涉及的基础技术。推荐系统常见方法包括协同过滤、内容推荐和混合方法。协同过滤根据用户-物品交互数据识别偏好[48][49]，内容推荐利用物品属性与用户画像缓解冷启动[51]，混合方法组合多类技术[50]。载体采用前后端分离的 Web 架构，前端以 Vue.js 完成交互界面[54]，后端由 Spring Boot 提供服务接口[53]，二者通过 RESTful API 通信。""",
    324: """实验载体中的推荐模块采用常规的用户偏好规则，不使用大模型增强推荐。推荐算法效果不是本文的研究变量，相关内容仅作为业务技术背景。第 5 章实验设计部分将说明载体系统的分析与设计。""",
    326: """本章介绍了支撑后续方法和系统实现的四类技术。大语言模型与 AI 编程智能体说明能力基础及输入侧限制；上下文工程涉及窗口与预算、检索增强、仓库级获取、压缩和记忆，并明确本文从项目知识组织层统一这些机制的定位；软件知识图谱和代码结构分析提供结构抽取、知识组织、语义匹配与子图搜索方法；Spec 驱动开发则为流程设计和实验比较提供参照。推荐系统与 Web 架构只用于说明实验载体的业务背景。第 3 章将在这些技术基础上给出统一的项目知识组织与上下文生成方法。""",
    328: """第 1 章把业务与代码割裂、代码库超出有效窗口以及项目知识难以积累归纳为项目知识失忆。本章据此给出面向 AI 辅助开发的上下文工程方法。能力-代码双层知识组织模型负责统一表示业务能力与代码结构，图谱驱动的任务上下文生成方法在 Token 预算内形成最小充分上下文，Spec 增量知识演化机制使知识随需求变化继续积累，三项机制分别对应第 1 章前三个创新点。六阶段流程约束架构把它们放入开发过程，并规定上下文何时产生、怎样消费及如何更新。本章先说明总体框架和流程约束，再展开三项机制。这些设计由第 4 章的 wpw 系统实现，并在第 5 章接受实验检验。""",
    331: """本文方法改变了项目上下文的供给方式。以往上下文常在任务开始后临时拼接，任务结束便失去作用；本文则把持续维护的项目知识模型作为基础。图3-1展示了完整闭环。需求先进入六阶段流程，逐步转化为阶段制品并沉淀成能力规范。能力规范形成知识图谱中的业务能力层，多语言源码解析生成代码结构层，两层关联后构成项目知识模型。开发任务到来时，流水线从图谱选择锚点、裁剪子图并压缩序列化，把任务级上下文交给智能体。实施完成后，代码变化和需求归档再次反馈到知识模型，触发关联更新。项目知识因而沿开发过程循环积累，而不会在任务结束时一并消失。""",
    334: """从知识流动看，整个框架包含数据来源、知识组织和智能体消费三个层次。能力规范、需求过程文档、源码与 Git 历史提供原始知识；解析、结构化和多源证据融合把这些材料转化为稳态知识库与知识图谱；上下文生成流水线再将图谱内容整理成智能体可以使用的任务上下文。项目知识按生命周期分为稳态知识与过程知识。稳态知识描述项目长期业务能力，并在需求归档时持续沉淀；过程知识由六阶段流程产生，只服务当前任务。Spec 增量知识演化机制负责两类知识的转化，需求归档后，过程知识被并入稳态知识，业务内容由此得以保留。""",
    337: """第 5 章为三项机制和流程约束架构分别安排验证。双层知识模型及上下文生成方法由消融实验和组件级基线区分贡献；多轮需求演化实验（T4）观察 Spec 增量演化带来的知识积累；流程消融则检验流程约束架构。下文先介绍负责整合的流程约束，再分别说明三项核心机制。""",
    340: """软件工程依赖确定的阶段推进和可审计状态，而大语言模型的生成具有非确定性。无约束对话把需求理解、方案选择和实施进度都隐藏在聊天历史中，外部难以检查智能体当前处于什么状态。对话持续增长或会话中断后，这些状态也不容易恢复，返工原因和需求遗漏位置随之变得模糊。流程约束架构把开发活动改为显式阶段流转，使智能体的行为状态能够检查、恢复并作为项目资产保存。""",
    341: """wpw 的核心开发流程包含 BRD（业务需求）、PRD（产品需求）、Design（技术设计）、Plan（开发计划）、Test（测试方案）和 Apply（编码实施），Explore（技术探索）作为可选阶段。各阶段分别生成业务需求文档、产品需求文档、技术设计文档、开发计划、测试方案和代码变更，Explore 则输出探索报告。阶段推进由三类规则控制。强依赖要求前置阶段完成后才能继续；弱依赖允许 Design 在缺少探索报告时继续；若探索报告已经形成，拍板门禁要求用户明确确认后才能进入 Design。依赖检查和门禁判断由系统执行，不能依靠智能体自律跳过。开发路径因此由自由规划改为受系统约束的阶段流转，进入时间、预期产物和确认责任都有明确记录。""",
    343: """每个阶段都遵循准备、生成和收尾三段式编排契约。准备段创建工作区、检查依赖并下发模板；生成段由智能体按模板形成制品，先经用户确认大纲再写入文件；收尾段检查产物、更新阶段状态，并按需触发代码审查等后续动作。契约将确定性操作与生成性操作分开。依赖判定、状态变更和文件组织由系统负责，智能体只处理理解和生成，也不能直接修改状态文件。这样可以避免隐式状态修改引起的过程漂移，并让不同底层模型受到相同的流程约束。""",
    344: """阶段流转中的每项状态都会留下记录。完成、跳过和拍板都写入项目级状态文件，因此任意时点的项目状态都可以查询和恢复。流程约束还遵循四项原则：状态改变必须经过系统并显式留痕；智能体无权直接修改状态，制品写入前需要用户确认；关键拍板由人完成，智能体负责生成和建议；流程支持断点续作，知识设施缺失时可以降级而不阻塞开发。该架构控制的是智能体行为状态和过程可审计性，不以提高单次生成速度为目标，也不取代 Agent 运行框架的执行能力。""",
    346: """作为三项机制的整合框架，流程约束还规定了上下文如何在开发中流动。阶段制品本身就是受控上下文，后续阶段按规则取得前序结果，例如 PRD 读取 BRD，Design 读取 PRD；项目背景不必在对话中反复携带，过程知识按阶段定向传递。进入实施准备后，系统返回任务清单和上下文文件清单，图谱流水线在此生成编码任务所需的结构化上下文。需求归档又会启动知识更新，过程知识被合并到能力规范，知识图谱随之增量维护，最终完成从过程知识到稳态知识的转化。""",
    347: """流程约束在制品化和门禁化方面借鉴了第 2 章介绍的 Spec 驱动开发，但两者处理的知识生命周期不同。已有方法中的规格制品主要服务当前开发过程，交付后通常进入归档状态。本文进一步把 Spec 及其变更设为项目知识演化的触发单元。流程不仅约束智能体行为，还持续产生并更新项目知识；双层知识模型和增量演化机制负责实现这一扩展。""",
    351: """本文模型的对象不限于代码结构，这一点区别于代码属性图（Code Property Graph）[18]等方法。业务能力层位于代码结构层之上，能力节点作为语义锚点，使知识模型既能描述“代码结构是什么”，也能说明“代码服务于哪项业务能力”。第 1 章已经指出，智能体生成任务上下文时同时需要业务语义和代码结构，单独的代码关系图无法覆盖这一需求。""",
    353: """知识模型采用“能力-结构”双层四层本体。C 层保存业务能力，表示项目中相对稳定的业务语义。代码结构层再分三级：L1 是带 frontend/backend 侧别属性的业务模块，L2 对应文件，L3 对应函数、类、接口、常量及各类状态管理元素。""",
    354: """能力规范是 C 层节点的唯一数据来源。每项能力按 OpenSpec 格式存放在独立目录的 spec.md 中，文件以 Purpose（能力目的）、Requirements（需求条目）和 Scenarios（验收场景）三部分描述“用户认证”“订单管理”等长期业务能力。能力规范同时为业务-代码关联提供语义基准。结构层由源码解析产生：项目目录用于推断 L1 模块，文件形成 L2 节点，文件中的代码元素再生成 L3 节点。""",
    355: """结构层保留三个粒度，是为了对应开发任务中的三种定位需求。模块层确定业务影响范围，回答需求会波及哪些模块；文件层给出物理位置，说明修改落在哪些文件；元素层进一步定位需要编辑的函数或类。缺少其中任一层，任务上下文都会失去相应粒度的定位能力。C 层位于三层结构之上，作为自然语言任务的业务语义入口，使任务能够沿“能力 -> 模块 -> 文件 -> 元素”逐步定位到代码。""",
    362: """结构层节点由 web-tree-sitter 解析语法树后抽取。统一使用 tree-sitter，而不是 Babel Parser、TypeScript Compiler API 等单语言 AST 工具，主要有四方面原因。不同语言的 tree-sitter 解析器具有一致的节点遍历和字段接口，TS/JS/Vue/Java 可以按同一模式扩展；框架原生支持基于旧语法树的增量重解析，与图谱更新相适应；源码存在语法错误时仍能得到局部可用语法树，适合不完整或含方言的真实项目；前后端全栈项目也能共用一条解析管线，无需为每门语言建立完全独立的适配层。""",
    363: """不同语言和扩展类型的解析成本并不相同，系统因此采用“快速预检 + 完整解析”。文件先接受路径与内容特征检测，例如用 defineStore 调用判断 Pinia store；命中特定扩展类型后，才调用相应解析器完成语法树分析，避免所有文件都执行相同的重解析。TypeScript/TSX/JavaScript/JSX 解析函数、类、接口、常量及导入关系；Vue 单文件组件提取 script 块后复用 TS/JS 解析；Java（Spring Boot）覆盖 class/interface/enum/record、方法、常量、注解和 REST 端点元信息。状态管理侧解析 Pinia/Vuex/Redux 的 store、action、getter、state，小程序与 uni-app 则提取页面、组件、生命周期和路由关系。""",
    364: """代码库中经常存在同名元素，例如多个组件都定义 onSubmit，这会给检索和语义匹配带来噪声。系统生成 L3 节点时，强制写入 filePath 和 parentName 两类上下文属性。前者记录所属文件的相对路径，后者记录类、store 或模块文件等上层容器名称。向量富化与检索展示由此可以使用“容器/元素”区分同名实体。第 5 章消融 A 将检验这项设计对图谱贡献的影响，表3-2列出 L3 节点的完整属性。""",
    366: """能力规范解析器遍历目录并生成 C 层实体，节点名称取能力目录名，同时保存 description 和 features。结构层实体则由多语言解析器从语法树抽取。节点 ID 按“类型前缀 + 内容哈希”确定，哈希使用 SHA-256 的前 12 位十六进制；相同内容会得到相同 ID，使图谱重建具有幂等性，也便于增量更新。实体抽取后再生成语义向量，输入文本按节点类型富化。L3 节点使用“文件路径 + 父容器名 + 节点名 + 注释文本”，其中 JSDoc/Javadoc 中文注释有助于匹配中文业务语句和代码元素。""",
    367: """知识模型全部保存在文件系统中，不依赖数据库。能力规范由 Markdown 和结构化字段组成，节点与边按行写入 JSONL，语义向量使用二进制文件及映射索引，schema 版本、文件哈希快照、扫描根和项目类型等构建信息记录在元数据文件中。文件式存储可以随代码一同提交到 Git，内容可直接阅读，也没有额外部署成本；配合图谱增量更新后，普通开发环境即可运行。""",
    369: """模型构建的关键是连接 C 层业务能力与 L1/L2/L3 代码结构，即生成 business_map 边。需求-代码追踪研究已经表明，人工建立并维护此类联系成本较高[13-14]，自动方法则需要权衡准确率和召回率[15]。本文不提出新的追踪算法，而是服务智能体上下文生成，用概率模型融合多类启发式证据，在较低维护成本下获得可用、可溯源的关联，并通过权重剪枝控制噪声。图3-4给出处理顺序：能力节点先召回候选，系统计算四类证据，经过 noisy-OR 融合与阈值过滤后形成 business_map 边。""",
    374: """四类证据分别从文档明示、命名约定、语义相似和演化关联判断业务-代码关系，覆盖范围与可信度互有差异。文档提取较为权威，但依赖规范是否完整；语义匹配覆盖更广，结果会受向量表示质量影响；Git 历史能够反映长期实践，却无法用于没有提交积累的新项目。表中的基础权重和上限是启发式初值，第 5 章将依据组件级基线中的人工抽样标注准确率进行校准和敏感性检验。本文关注的是多源融合能否改善智能体上下文，而不是优化一个追踪模型的参数。""",
    378: """（3）证据权威性排名与溯源。各证据源可信度不同，系统会为 business_map 边保存最权威的支撑证据。排名依次为 structure（结构证据）> doc-extract > ai-refine（预留）> git-history > semantic > name-match。多源同时命中时，该顺序决定溯源标签，使每条业务映射边都能回到可信度最高的证据来源，也为第 5 章的可追溯性评价提供记录。""",
    379: """（4）低权重剪枝。候选关联的聚合权重低于 0.3 时不生成边，以免低可信关系持续扩大图谱噪声。阈值与各证据源权重共同调节映射质量，并参照第 5 章组件级基线的人工抽样标注准确率完成校准。""",
    387: """存量项目的知识演化从新需求进入六阶段流程开始。需求先形成 Spec 提案，归档时再判断能力归属：已有能力的增量内容以 delta 合并到原能力规范，新能力领域则创建对应规范。规范变化会同步更新知识图谱，C 层节点随规范增删改，业务-代码边根据规范内容和代码变化重新计算，代码结构层通过文件级增量解析维护。具体需求和代码实现经常变化，业务能力相对稳定，因此 Capability 层承担中间锚点。持续变化中的关联由此保持为“需求 -> 能力 -> 代码”，不会随着每次需求更替而碎片化。""",
    389: """没有既有知识模型的新项目，以及缺少能力规范的存量项目，需要先完成知识冷启动。初始模型由“源码扫描 + 需求 Spec”共同生成：源码扫描对项目执行多语言全量解析，建立 L1/L2/L3 结构；需求侧从已有文档或首轮 BRD/PRD 提炼能力规范，生成 C 层并计算业务-代码关联。冷启动时的证据并不完整。Git 历史很少，git-history 难以生效；能力规范刚建立，doc-extract 覆盖也有限，关联主要依赖语义和命名匹配。随着需求持续交付，规范内容和提交历史逐步增加，四类证据才形成互补。第 5 章 T4 将考察冷启动后关联质量的变化过程。""",
    392: """六阶段流程会产生需求文档、能力规格提案、技术方案、开发计划和测试方案，这些过程知识首先服务当前任务。需求归档时，系统进行能力归属分析，并用 delta 合并把已交付需求的业务内容写入能力规范。一次性过程文档中的信息由此转化为机器可解析的稳态知识。能力归属和合并由 AI 层在流程约束下执行，结果需要用户确认；目录约定、格式校验和图谱增量更新则保证内容一致落盘。""",
    393: """图谱更新以文件哈希快照识别变化，避免中型项目每次全量重建。系统比较当前文件哈希与元数据历史记录，只重新解析新增或变更文件，并重建这些文件对应的节点和出边；能力规范的增删改也会被检测，C 层及其关联边同步更新。确定性节点 ID 使未变化部分在更新前后保持一致。当前限制是：某个被引用的类如果重命名或删除，引用它的未变更文件不会自动重建入边，可能留下悬挂引用。完整性校验会对此告警，结构性重构完成后应执行全量重建。""",
    394: """该机制通过工程过程积累知识，不涉及机器学习训练，也不依赖训练数据或模型更新。知识增长来自规格化、源码解析和证据计算，这些操作要么确定，要么能够审计。第 5 章 T4 将比较初始阶段与多轮演化后的上下文构建时间、Token 消耗和召回率，以验证积累效果。""",
    411: """子图从多个锚点同时开始扩展，搜索既沿出边取得下游依赖，也沿入边找到上游依赖，并允许通过 business_map 跨越业务层与结构层。这里选择加权双向 BFS，原因与代码任务的影响范围有关。深度优先搜索容易沿一条依赖链进入局部深层，例如连续追踪工具函数调用，结果可能偏离任务主体；Dijkstra 类最短路径能计算精确距离，但在大图中的成本更高，而上下文生成只需感知按跳数计算的近似距离。BFS 按层扩展，正好对应“越靠近锚点越重要”的骨架分级假设，搜索深度也可以直接作为预算参数。""",
    417: """裁剪得到的子图仍可能包含较多远离锚点的细节，如果直接序列化，关键信息会被稀释。骨架抽取按照节点到锚点的距离改变保留粒度：距离 0 的锚点保留完整属性与签名，距离 1 的节点保留名称、类型和关键属性，距离不小于 2 时只留下名称与层级。序列化使用层级符号表达关系，⊃ 表示包含，-> 表示依赖，⇄ 表示双向关联，◉ 标记锚点。与自然语言描述或完整 JSON 相比，这种表示以较少 Token 保存同等结构信息。""",
    418: """骨架分级之上设置 loose、standard、extreme 三档压缩。档位逐步提高时，距离阈值增大，保留属性减少，Token 上限也进一步收紧；上层可根据任务预算选择，具体参数见表3-5。第 5 章消融 B 将用 Token 缩减率、上下文召回率和下游任务完成效果，量化三档压缩在召回、信息保真度与成本之间的关系。""",
    420: """表中的节点数量和 Token 上限只是默认参考值，运行时仍由预算迭代动态确定。最终采用的具体数值以第 5 章实测结果为准。""",
    421: """序列化结果超过用户或上层给定的 Token 预算时，系统沿五级链逐步降级：先降低压缩档位，再减少节点数和扩展深度，随后提高边权重阈值，仍然超限时只保留锚点。任何级别都不会删除锚点，以免上下文失去任务焦点。流水线还具有系统级容错。向量索引缺失时改用结构检索，图谱缺失时返回文件系统提示，序列化失败时至少输出锚点清单，从而避免上下文生成阻塞整个开发过程。""",
    426: """第 3 章给出了面向 AI 编程智能体的上下文工程方法，本章转向工程实现。wpw 把流程约束架构、能力-代码双层知识模型、任务上下文生成和 Spec 增量知识演化分别落实为可执行组件。它不替代大模型，也不承担 Agent 运行框架的职责；在智能体已经能够生成代码和调用工具的前提下，wpw 为长期演化项目维护过程制品、项目知识和任务级上下文。""",
    427: """系统使用 Node.js 与 TypeScript 开发，架构分为 AI 层、CLI 层和文件系统层。AI 层理解需求、生成阶段制品并负责开发者交互；状态校验、依赖检查、图谱操作和路径解析等确定性任务由 CLI 层完成；需求制品、能力规范、状态记录及本地图谱则写入文件系统。三层分离后，模型生成与项目状态变更不再相互混杂。即使替换模型，确定性程序仍能继续维护状态和知识。""",
    430: """wpw 有四项设计目标。开发阶段、阶段产物和推进条件需要显式记录，以保证 AI 辅助过程可控；能力规范与源码结构应共同形成项目知识，供不同需求复用；系统根据任务和 Token 预算组织结构连贯的上下文，而不是把大量文件直接拼入提示；需求或代码发生变化后，知识需要保持一致，部分知识设施不可用时流程仍应继续。""",
    431: """图4-1给出了三层系统结构。AI 层通过主 Skill 和 /wpw:xxx 阶段命令组织生成与交互，CLI 层提供初始化、阶段状态维护、任务追踪、归档和图谱服务等命令。文件系统层使用 wpw/active/、wpw/archived/、wpw/specs/、wpw/knowledge/ 保存过程制品、稳定能力规范和图谱产物，项目类型、模板覆盖及图谱配置记录在 workflow.config.yaml。知识图谱与向量索引均保存在本地文件中，不需要独立数据库服务。""",
    435: """各层职责见表4-1。AI 层不能直接修改过程状态，状态只能由 CLI 命令写入并留下记录；check 和 Apply 在编排前执行依赖门禁。这样可以避免会话中断或模型误操作后，状态文件与实际制品出现不一致。""",
    438: """在 wpw 中，一次需求变更对应一个独立过程实例，而不是一段连续对话。流程从需求创建开始，随后形成阶段制品并实施任务，最后归档需求和更新知识。开发者提出需求后，智能体生成符合 kebab-case 的需求标识，再调用 wpw new 建立 wpw/active/<change>/ 工作区及初始 .wpw.yaml。CLI 同时检查名称格式和全局重名，读取项目类型，并把各阶段初始化为 pending。需求名称、工作目录、工作流 Schema 与初始状态因而在流程起点就被固定。""",
    439: """需求实例建立后，BRD、PRD、Explore、Design、Plan、Test 与 Apply 按 Schema 中的依赖关系推进。系统在每个阶段先提供所需的前序制品和模板，AI 层据此生成内容并与开发者确认；制品得到确认并保存之后，CLI 才更新状态。Explore 和 Test 可以跳过。Explore 提供方案候选，Test 形成测试用例，二者虽不改变主链结构，但缺失时会分别减少设计决策依据和实施阶段的测试保障。""",
    440: """Apply 以 Plan 中的任务项为实施单元。系统向智能体返回已完成阶段的文档、待执行任务、当前进度和测试警告；每完成一项任务，CLI 就地修改 Plan 标记，并将汇总进度同步到状态文件。开发者验证功能后，归档命令把过程工作区移入按月组织的 wpw/archived/。移动之前，AI 层把已确认的需求增量沉淀到能力 Spec；归档完成后，系统触发图谱更新。当前需求产生的过程知识由此转为后续任务可以复用的稳定知识。""",
    443: """工作流模块使用内置 Schema 描述需求变更的静态结构。BRD、PRD、Explore、Design、Plan 和 Test 被定义为阶段制品，Apply 是消费 Plan 任务的实施入口。每个阶段都记录标识、制品文件名、强依赖 dependsOn、弱依赖 optionalDeps，以及 skippable 和人工确认属性。规则集中保存在 Schema 注册表中，依赖检查、模板解析与状态更新均读取同一来源，避免相同约束散落在不同提示词和命令里。""",
    444: """工作流实例从创建需求开始。AI 层生成名称并调用 wpw new <change>，CLI 随后检查名称是否满足 2--30 个字符的 kebab-case 规则，并在活跃和已归档需求中查重。校验通过后，系统读取项目类型、建立需求目录并写入状态文件。初始文件把所有阶段设为 pending，同时保存 Schema 名称、创建时间、项目类型和空任务进度。后续命令由 <change> 找到唯一实例，不必再从聊天历史推断当前任务所处阶段。""",
    448: """文档阶段统一执行“准备--生成--收尾”三段流程。准备阶段依次使用 wpw new、wpw check <phase> 和 wpw template <phase>，分别确认工作区存在、判断当前阶段能否继续并取得模板路径。检查若返回缺失依赖，AI 层停止编排并提示补齐前序制品；若只返回警告，相关制品和警告会一起交给开发者处理。此时系统只确定路径、规则与状态，不生成需求或设计内容。""",
    449: """进入生成阶段后，AI 编程智能体读取已完成制品、当前模板和必要的图谱上下文。以 Design 为例，其必需输入是 PRD；Explore 已完成时，还要读取探索报告和拍板结果。智能体先提交技术设计大纲，得到开发者确认后再写入文件。BRD、PRD、Design、Plan、Test 均沿用这一模式，只改变输入和模板。Apply 不再生成文档，而是解析 Plan 任务，并按任务取得代码上下文后实施修改。""",
    450: """收尾由 CLI 记录过程事实。文档保存后，wpw done <phase> 把阶段标为 done；可选阶段不执行时，wpw skip <phase> 写入 skipped；Explore 产生候选方案后，开发者用 wpw decision explore --option <choice> 保存最终选择。模型负责生成内容和调用命令，开发者承担确认与决策，CLI 则维护可查询状态。""",
    457: """wpw check 返回 canProceed、缺失依赖和警告。强依赖未满足时，命令以非零状态退出，AI 层不能继续生成目标阶段；Apply 还会单独确认 Plan 已完成，保证实施任务来源明确。弱依赖不改变硬门禁，但会调整 AI 层输入。例如 Explore 已完成却没有拍板，Design 会收到警告并要求开发者选择方案或明确跳过探索结果。Test 被跳过后 Apply 仍可执行，不过系统会提示当前实施没有测试用例保障。强依赖、可选信息和人工决策因而使用不同状态条件表达。""",
    463: """每项需求都在 wpw/active/<change>/ 中保存独立 .wpw.yaml。文件记录需求和 Schema 名称、各阶段状态、Explore 的选定方案，以及任务总数和完成数；配合阶段文档，它构成可恢复的过程快照。系统只允许 CLI 更新状态。done 记录完成，skip 只能修改 Schema 中声明可跳过的阶段，decision 只写入 Explore 方案；下一阶段能否进入，仍由编排前的依赖检查决定。""",
    464: """Plan 把开发计划写成 Markdown 任务清单，Apply 直接以该清单为任务源。wpw apply 先检查 Plan 状态，再返回已完成阶段的文档路径、任务列表、完成与剩余数量，以及 Test 未完成时的警告。任务开始时，智能体调用 wpw task --mark <id> --state in-progress；完成后用同一命令标记 done。CLI 仅替换目标任务复选框，重新统计数量，并把进度同步到 .wpw.yaml。会话中断后，apply --from <task> 可以重新取得剩余任务和阶段制品，恢复依据来自显式状态而不是聊天记录。""",
    465: """工作流模块同时限定图谱上下文的使用位置。Explore、Design 和 Plan 在准备时可以查询既有能力及模块边界；Apply 在任务实施前，根据已知文件或语义描述请求图谱上下文；Test 与代码审查沿上下游关系确定回归范围；Sync 通过业务映射寻找代码变化后可能需要同步的制品。图谱不存在、向量索引缺失或没有命中锚点时，系统提示开发者并退回人工读取文件，知识服务不会成为流程硬门禁。""",
    466: """功能通过开发者验证后，需求工作区被归档到 wpw/archived/YYYY-MM/<change>/。移动前，AI 层根据已确认的 PRD、Design 和实施结果，新建或更新相关能力 Spec。归档命令随后检查图谱是否存在；图谱可用时执行增量更新，让能力 Spec 和代码结构的变化进入项目知识库。该步骤把工作流终点与第 4.5 节的长期知识维护连接起来。""",
    469: """图谱构建模块落实第 3 章的 C-L1-L2-L3 双层知识模型。wpw/specs/<capability>/spec.md 中的能力规范生成 C 层节点，节点保存能力名称、Purpose、Requirements 和场景等业务语义；L1、L2、L3 分别对应代码模块、源文件和代码元素。源码解析完成后，系统建立模块--文件--元素的包含边，并补充导入、调用、继承和 business_map 等关系。稳定的节点、边标识及属性用于后续查询、重建和溯源。""",
    470: """源码解析统一以 tree-sitter 为基础，当前覆盖 TypeScript、JavaScript、Vue 单文件组件和 Java 等语言；状态管理及跨端项目中常见的模块、状态单元也由扩展解析器识别。构建图谱前，系统先按文件类型和内容特征预检，仅对候选源码执行 AST 解析。构建目录、依赖目录及普通文本资料因此不会混入代码结构层。""",
    475: """节点的 level 表示其位于能力--代码结构的哪一层，type 则说明该层中的实体种类，attrs 保存随实体类型变化的属性。把层级与类型分开后，C--L1--L2--L3 可以直接用于图遍历、裁剪和序列化；函数、类、接口、组件及状态管理单元等异构实体仍可按 type 区分和过滤。""",
    476: """表4-3列出所有节点共用的字段。节点标识由实体的稳定识别信息计算 SHA-256 后截断，通常写成“类型前缀:12 位十六进制摘要”。相同输入会得到同一标识，重建或增量更新时仍能定位原节点。创建和更新时间只记录本次图谱构建过程，不表示源码的提交历史。""",
    480: """attrs 使用可扩展对象，而不是为全部层级预设一张宽表。能力 Spec 的业务描述、AST 得到的签名与参数、源码位置等不同信息都可以按需保存，Vue、Pinia、Vuex、Redux、小程序和 Spring Boot 等项目形态也能继续扩展。表4-4按层级汇总当前属性，其中“可选”表示属性由语言、框架或源码内容决定，节点构建器不会补造缺失值。""",
    485: """能力--代码映射综合文档提取、命名匹配、语义相似度和 Git 历史等现有证据。同一能力与目标节点之间的候选证据先被收集，再按第 3 章 noisy-OR 规则聚合边权重；边上还保存权威性最高的证据，供后续核查。如果向量索引或 Git 历史不可用，相应证据源退出计算，结构与文本证据仍可独立形成图谱。这里的多源信息用于工程关联，并不训练或更新基础模型。""",
    486: """节点与边逐行写入 JSONL，元数据文件记录 schema 版本、构建时间、节点和边数量、配置摘要及文件哈希快照。向量索引保存为二进制浮点数组，再由 JSON 映射文件连接到图谱节点。保存 JSONL 和元数据时，系统先写临时文件，完成后再重命名，以降低中途失败造成图谱不完整的风险。本地 Embedding 可用时建立向量索引以支持语义锚点选择；缺少该能力时，结构化查询和显式锚点查询仍然可用。""",
    489: """上下文生成模块对应第 3 章的任务级子图流程。自然语言任务先在本地向量索引中检索候选锚点；调用方已经知道模块、文件或元素标识时，也可以直接指定锚点，不再执行语义检索。候选结果会结合层级、相关性和能力映射置信度处理。系统随后沿入边和出边扩展图结构，并用深度、边权重和节点数量限制范围，形成结构连贯的候选子图。图4-5展示多锚点加权双向 BFS 的扩展路径与距离合并。""",
    492: """裁剪子图时始终保留锚点，其他节点按照语义相关性、结构重要度和锚点距离排序。深度、最小边权重、节点上限、层级过滤和类型过滤都作为命令参数开放，阶段命令可以据任务粒度改变上下文范围。模块提供的是与任务相关的结构化知识，并不代替开发者阅读关键源码。""",
    494: """候选子图还要经过骨架抽取和分层序列化。锚点保存较完整的属性与签名，近邻节点保留标准属性，远距离节点只留下名称、类型和必要关系。输出按能力、模块、文件、元素组织，并标明包含、依赖与业务映射。紧凑符号代替重复的自然语言描述，使同一结构占用更少 Token。""",
    495: """结果超过调用方给定的 Token 预算后，系统按第 3 章设定的顺序调整：提高压缩程度、减少节点、缩短扩展深度并提高边权重阈值，最后退化为只含锚点的最小上下文。每次调整都重新抽取骨架、完成序列化并估算 Token。锚点不会在降级过程中被移除；最小结果仍超出预算时，系统明确返回预算不足，而不是静默截断。Token 预算由此成为可见的输入约束，不再是隐藏在提示词中的经验参数。""",
    498: """长期项目若在每次代码变化后全量重建图谱，知识维护成本会持续增加。wpw 在元数据中保存文件哈希快照，wpw graph update 比较当前扫描结果和上次状态，从而识别新增、修改及删除文件。变更文件对应的旧文件节点、元素节点和关系边先被移除，重新解析后再写入局部结构，未改变部分则保留。能力 Spec 同样会重新解析并与既有 C 层比较，以处理能力新增、修改和删除，受影响的 business_map 关系随后重新计算。""",
    499: """增量更新要求 schema 主版本兼容。没有历史图谱，或者发现主版本不兼容时，系统会自动执行全量构建；结构性重构可能影响大量依赖，开发者也可以显式调用 rebuild。向量索引目前采用折中方案，即图结构局部更新后全量重建向量。该选择优先保证节点与语义索引一致，向量的增量性能优化留作后续工作。""",
    501: """阶段性需求制品会随任务归档，能力规范则应跨需求长期复用。归档时，AI 层根据 PRD、Design 和实施结果识别业务能力。已经存在对应 Spec 时，确认后的增量需求被合并进去；不存在时，系统按统一结构创建新的能力 Spec。图谱随后增量更新，让新增或修订的能力节点及其代码映射进入知识库。""",
    505: """本节只验证 wpw 功能是否可用、实现是否与设计一致，不以性能优势或推荐效果作为结论。测试记录 Node.js、操作系统、依赖版本、项目规模和图谱配置。第 5 章受控实验还要固定基础模型、对比工具及关键参数。由于相关工具更新较快，论文以实验实际采用的版本和提交记录为准，不把后续版本能力加入比较。""",
    506: """功能测试覆盖工作流状态、阶段依赖、任务追踪、图谱构建、能力与代码解析、多源映射、结构化查询、语义搜索、上下文生成、多图谱存储和增量更新。仓库中的综合验证脚本 src/graph/__verify__.ts 会创建独立临时项目，执行构建、查询、更新及重建；单元测试与集成测试另外覆盖解析器、映射器、存储模块和命令接口。判断通过的依据是命令输出、状态转换与持久化结果符合预期，而不是某一次大模型回答。""",
    507: """本次系统测试环境见表4-6。运行时、package.json、已安装依赖、Git 提交记录和图谱元数据分别提供对应的版本信息。""",
    510: """工作流、图谱与上下文服务的功能结果记录在表4-7，证据来自命令 JSON 输出、.wpw.yaml、meta.json 和 JSONL 图谱文件。图谱及上下文生成耗时、Token 消耗和上下文召回等量化指标将在第 5 章比较，本节不依据这些指标声明性能优势。""",
    512: """表4-7说明，系统能够依据持久化状态实施强依赖门禁，并为可选 Explore 决策给出明确警告；图谱服务可以构建、查询和增量更新节点、边、元数据及索引。缺少向量索引时，显式锚点仍能取得结构化上下文。这些结果只验证功能与降级行为，方法在不同上下文管理范式中的效果差异仍由第 5 章受控实验判断。""",
    515: """本章实现证明该方法具有可运行的工程载体，但功能可用不等于方法已经有效。第 5 章将在统一任务和配置中设置上下文管理范式对比、机制消融及多轮需求演化实验，分别检验知识组织、上下文生成、流程约束和 Spec 增量演化的影响。""",
    518: """本章选择基于大模型的美食推荐系统作为实验载体，用对比实验和消融实验检验本文上下文工程方法的有效性。这里需要区分方法验证与工具比较：wpw 只是方法的实现载体，实验并不用于证明它相对其他工具具有绝对优势，所考察的是流程约束、知识组织和上下文生成机制会怎样影响 AI 辅助开发。实验对象也限定为美食推荐系统的软件实现过程。研究重点在开发上下文的组织方式，因此不评价推荐算法准确率，而是在业务系统实现任务相同的条件下，分析不同大模型辅助开发方式带来的开发成本、质量和可维护性差异。""",
    519: """第 3 章提出的上下文工程方法是本章实际验证的对象，流程约束、知识组织和上下文生成三项核心机制都服务于“面向 AI 编程智能体的上下文工程”这一主线。wpw 对应第 4 章给出的方法实现，美食推荐系统则是 1.1.2 节界定的实验载体。受控对比因而关注不同方法在既定维度上的表现，不对工具作高下排序；实验将考察由受控流程、结构化知识供给和预算控制组成的上下文工程方法对开发成本、质量、上下文效率及流程可控性的影响，并通过消融确定三项机制各自的贡献。""",
    523: """实验围绕第 1 章提出的三个研究问题（RQ1-RQ3）展开，目标是在受控条件下验证上下文工程方法对 AI 辅助软件开发过程的作用，而不是比较 wpw 与其他工具的优劣。四种上下文管理范式将在开发成本、开发质量、上下文效率、长期演化和流程可控性等维度接受比较；知识组织、上下文生成和流程约束的贡献由消融实验分别识别，多轮需求演化任务（T4）用于考察长期项目支持能力。表5-1给出了研究问题和验证方式的对应关系：主实验呈现完整方法与代表性范式的综合差异，消融实验负责分离机制贡献，T4 则提供长期演化方面的观察依据。""",
    525: """本实验属于受控工程实验，其外部效度应据此理解。它验证的是方法机制能否在所设条件下发挥作用，并不据此声称该方法适用于所有软件项目。为减轻单一项目带来的外部效度限制，实验采用“一个业务载体 + 多任务集”的组织方式，具体设置见 5.1.2 节。""",
    529: """实验控制和测量的对象是 AI 辅助开发过程中的流程约束、知识供给与上下文管理，推荐算法效果以及业务系统本身的先进程度均不属于研究变量。美食推荐系统只需提供典型业务复杂度，因此设计中主动排除了无关的技术负担；推荐算法精度、LLM 推荐效果和微服务治理能力都不进入评估范围。""",
    531: """系统面向管理员和普通用户两类角色，分别对应 B 端管理平台与 C 端用户端。用户侧业务从登录、浏览美食和生成推荐，延续到加入购物车、提交订单及评价反馈（收藏 / 评论）；管理端则覆盖用户、美食分类、美食信息、订单和系统管理。响应性能、数据一致性与可扩展性等非功能需求采用典型 Web 系统的常规标准，不额外设置与研究变量无关的特殊约束。""",
    533: """图5-1展示了系统的前后端分离架构。商家管理平台采用 Vue3 + Vite + Element Plus + Pinia，用户端采用 Uni-app + uView UI 微信小程序；后端基于 Spring Boot 分层实现用户、菜品和订单等业务模块。为体现真实业务逻辑的复杂度，系统还设置了以用户偏好规则计算为核心的个性化业务逻辑模块，但其算法效果不属于研究变量。数据层使用 MySQL + Redis + Elasticsearch，服务间异步通信使用 RabbitMQ。推荐模块同样采用常规的用户偏好规则计算，不涉及大模型增强推荐，其算法效果也不纳入本章评价。实验只保留横向比较所需的系统规模，微服务治理和监控组件按需简化。""",
    551: """实验围绕同一美食推荐系统实现任务设置四种大模型辅助开发方案。它们代表从无约束交互到完整上下文工程的不同范式，机制差异明确，便于在控制其他条件后分析结果来源。表5-3呈现的是各方法在研究维度上的差异，不是工具排名；方案选择也以代表性和可归因为准，不追求穷尽所有 AI 工程框架。""",
    553: """（1）实验信息约束原则。四种方案获得相同的初始信息，包括同一份需求描述、技术栈说明和任务执行时点的已有代码仓库。方案 D 使用的能力规范与知识图谱不是人工预置的额外先验，而是在工作流执行期间从需求文档和源码中自动构建。首次扫描源码、生成能力规范、建立图谱与向量索引所产生的完整冷启动开销--均计入方案 D 的时间与 Token 成本，实验不使用任何预构建知识库。其他方案也可通过人工阅读仓库取得等价信息，相应检索成本同样计入。""",
    554: """（2）主实验与消融实验的分工。方案 D 相比 B/C 同时包含流程、知识组织和压缩等更多机制，这是完整方法的设计特征。由于研究目的在于分析各机制的组合效果，而非比较工具的整体能力，主实验只用于观察完整方法的综合表现，不能把组间差异直接归因于某一个机制。单项贡献将在 5.1.5 节的消融实验中进一步分离。""",
    555: """（3）Spec Kit 参照说明。Spec Kit 关注 Constitution + 质量门禁 + Workflow Engine 等项目级规范治理体系，本文研究的则是项目知识结构化和任务级上下文生成等代码侧上下文工程。二者研究目标不同；若把 Spec Kit 的完整治理体系纳入实验，还会把生态配置成本等混杂变量带入组间差异，使目标变量难以分离。基于研究定位和变量控制的考虑，主实验不设置 Spec Kit 方案，而在 1.2 节研究现状和 5.4.2 节讨论中进行机制层面的比较。这一安排不评价工具本身的能力。""",
    556: """（4）控制变量与版本锁定。各方案使用同一需求规格、同一开发者、同一大模型和同一技术栈，时间预算也保持一致。工具能力以实验实际采用的版本为准，版本号、发布日期和代码提交标识统一记录在 5.2.1 节实验环境表中，以免工具快速迭代造成比较基准漂移。""",
    560: """上下文召回率按如下协议标注：两名具有开发经验的标注者根据需求描述和最终实现，分别标出任务实际涉及的代码元素集合 R，标注同时覆盖文件与函数两个粒度；该集合再与实际注入上下文的集合 C 求交，用于计算召回率。标注一致性通过 Cohen's kappa 系数衡量，出现分歧时经仲裁确定最终标注。""",
    567: """实验环境表将在成稿时回填硬件、操作系统、Node.js 运行时、大模型及 API 版本，并记录各对比工具的版本号、发布日期和代码提交标识。考虑到工具迭代较快，比较基准以实验实际采用的版本为准。四种方案均在同一需求规格、技术栈、开发者和时间预算下，依照相同顺序完成 T1/T2/T3 三组任务集。Token 消耗、工时、迭代/返工事件及人工介入事件在全过程中使用统一模板记录，并作为 5.3 节指标计算的数据来源。""",
    570: """该方案由开发者直接通过 AI 对话生成前端组件和后端接口，并允许使用同一模型提供的代码解释、调试建议、重构建议等常规辅助能力。它不设置显式规格管理、项目知识索引和任务阶段约束，作为“无流程约束的自然语言交互开发”基线。实验全程记录 Token、工时、返工和缺陷，并观察对话状态是否一致、需求是否遗漏以及上下文是否随轮次持续膨胀。""",
    572: """该方案按照实验采用版本的工作流编写 proposal/spec/design/tasks 等变更规格，再通过 apply/sync/archive 推进实现。观察内容包括规格制品如何关联代码结构与业务知识、变更规格衔接实现所需的成本，以及以文档和人工检索为主构造任务级上下文时产生的实际开销。""",
    574: """该方案依照实验采用版本的技能化工作流执行，由 brainstorming 澄清需求，writing-plans 形成计划，subagent-driven-development 负责实施，再由 code-reviewer 完成审查。实验将记录不同技能流程之间的衔接和状态管理方式，并考察子代理上下文隔离的实际效果、配置成本及任务级上下文构造成本。""",
    579: """（3）实现阶段：展示 Apply 阶段形成的用户、菜品、推荐和订单等模块代码及其结构，同时结合图谱上下文注入记录，说明各模块实现与关键功能的对应关系。""",
    580: """（4）运行效果：以系统首页、推荐页面、管理后台和订单页面等运行截图作为系统实现完整性的直观证据，相关图5-3在成稿时回填。""",
    581: """本部分主要观察三个方面：各阶段是否完整且状态可审计，图谱上下文在实际任务中的密度和命中率，以及能力归档沉淀机制能否按设计运转。""",
    583: """注：本节给出的是实验执行前使用的分析框架，所有判断都须根据实测数据回填或修正。每个评价维度均考察组间是否存在显著差异、差异可能来自哪种上下文构造方式/流程约束/知识供给，以及结果能否支持表5-1对应的研究假设；在数据产生之前不预设结论方向。""",
    587: """• 四种方案在三组任务集上的 Token 消耗将分别统计，其中上下文构造消耗与生成消耗单独记录，同时比较 API 成本分布。分析重点是对话式逐轮累积、手动检索和图谱按需注入等上下文构造方式如何改变成本结构。""",
    588: """• 社区先验参照（非学术证据，仅作动机）：2026 年 6 月曾有用户公开比较 OpenSpec 与纯 Claude Code 的实现过程。该案例中，前者多发现 3 个需求缺口，但代码量约 +50%、工时约 2 倍、API 成本约 3 倍。这个单用户案例只提示规格驱动流程可能带来额外过程成本，具体价值仍需实验测量，不能作为本文结论的证据。""",
    591: """• 实验将比较四种方案的缺陷率和需求覆盖度，并结合消融 C 区分两条可能的影响路径：测试方案前置、依赖检查等流程约束如何作用于开发质量，以及上下文质量产生了什么影响。""",
    595: """• 以消融 B 为主要依据，分别报告三档压缩在 Token 缩减率、上下文召回率、信息保真度和下游任务成功率之间的权衡曲线，据此识别给定任务规模下的较优工作点。""",
    597: """• 结合消融 A 的结果，比较图谱供给与手动检索在上下文召回率和 Token 成本上的差异，以分析知识组织方式的影响。""",
    599: """• T4 多轮需求演化任务构成 RQ3 的主要证据。实验将比较方案 D 在初始阶段和多轮需求之后的上下文构建时间、Token 消耗及上下文召回率变化，演化曲线见图5-5。""",
    600: """• 方案 B（规格驱动）作为对照，用于考察多轮演化过程中结构化知识积累与文档制品积累的边际成本差异及召回稳定性，具体数据待回填。""",
    601: """• 多轮执行后若出现召回率下降或构建成本回升，实验将按实际结果报告，并结合图谱密度上升、能力规范膨胀等可能的边界条件分析原因。""",
    603: """• 流程完整度和状态可审计性将按组比较，并以实测评分为准。方案 D（wpw 载体）通过三段式契约 + 状态文件显式记录项目级阶段状态；OpenSpec 使用 artifact graph 跟踪变更制品状态；Superpowers 依靠技能工作流约束执行过程；vibe coding 不设置流程约束。""",
    606: """• 知识组织消融（A）：根据实测结果，量化图谱供给相对手动检索在 Token 消耗、需求覆盖度和人工介入次数上的差异。""",
    607: """• 上下文生成消融（B）：报告三档压缩的权衡曲线并识别较优工作点；若不同档位之间没有显著差异，也将按实测结果说明原因。""",
    608: """• 流程控制消融（C）：比较完整工作流和单命令模式在流程完整度、返工次数及缺陷率上的实测差异。""",
    609: """• 组件级基线：分别量化 noisy-OR 与三种聚合基线、双向 BFS 与三种选择策略基线的差异。若增益不显著，将如实报告，并从任务规模、图谱密度等边界条件分析原因。""",
    612: """实验数据回填后，将从三个方面判断上下文工程方法是否有效以及在什么条件下成立。技术栈适配性方面，前端 Vue3/Pinia/Uni-app 解析和后端 Java/Spring Boot 解析应使测试系统的前后端源码以一致方式进入图谱；后端解析包括类型/方法/常量节点、注解与 REST 端点元信息、包路径 import 边及业务包模块推断，business_map 多源融合需要在两端同等生效，不同技术栈则按子目录构建命名图谱并由多图谱机制隔离。机制贡献将依据消融 A/B/C 的实测结果分解，分别讨论知识组织、上下文生成和流程控制的作用路径与边界。适用范围则根据绿地/存量迭代分组任务集的结果，比较方法在不同开发场景中的表现。实测若与预期不一致，将据实分析原因。""",
    615: """其一，Spec Kit 以 Constitution + 质量门禁 + Workflow Engine/扩展生态构成项目级规范治理体系，强调通过规范和工作流约束保持 Agent 执行一致；本文方法把重点放在项目知识结构化和任务上下文生成，用于降低 AI 取得代码知识的成本，两者可以互补。本文在设计上选择较轻的起点，依靠项目类型自动嗅探、模块自动推断、纯 Node.js 单运行时、图谱本地构建，以及 CLI 层无需 API Key 达到零配置开箱即用。相应代价是治理强度较低，不提供全局规范宪法和一致性检查，这一限制见 5.4.3。""",
    616: """其二，OpenSpec 使用 artifact graph + action 工作流管理变更制品，本文方法则把变更之外的项目级知识纳入同一基础设施。能力规范随归档持续积累，代码结构由双层图谱建模，业务与代码的关联通过多源证据融合建立；命令级解耦允许各项能力按需组合，图谱增量更新则用于控制功能增加后的边际维护成本。""",
    617: """其三，Superpowers 等现有方法主要通过子代理上下文隔离、工作区隔离等机制作用于 prompt/工作流/扩展层；本文方法的区别在代码侧上下文供给。项目知识沉淀为图谱后，系统按照锚点检索 -> 子图裁剪 -> 骨架抽取 -> 压缩序列化的顺序生成任务上下文。这里还需区分“规避”与“解决”：本文没有从技术上消除规则膨胀造成的失真，规则侧只通过分阶段轻量加载加以规避；被技术机制直接处理的是同构的代码上下文膨胀问题。""",
    620: """注：● 表示支持，◐ 表示部分支持或可通过扩展/生态支持，○ 表示非设计目标，◉ 表示本文重点探索方向。""",
    621: """现有工具分别侧重规则治理、变更管理和执行纪律，部分能力之间已有重叠。本文方法把项目知识沉淀与任务导向的上下文生成纳入同一工作流，与这些工具之间更多是互补关系，而非替代关系。""",
    623: """• 实验规模与性质边界：本研究采用受控工程实验，以单一业务载体上的三组任务集完成个案层面的机制验证。相关结论仍需在其他项目类型、项目规模和团队场景中进一步检验。人工评审具有主观性，实验通过双人交叉评审和标注仲裁降低这一影响。""",
    624: """• 后端语言覆盖：系统已经实现 Java/Spring Boot 解析器，但现阶段的落地验证规模有限，首版也未建立依赖注入边和 JPA 关联边；Go/Python 等后端语言尚不在覆盖范围内。""",
    626: """• 规则治理：系统目前缺少全局规则治理能力。项目与团队规模增长后，规则膨胀仍可能造成同类失真；分阶段轻量加载只能缓解这一问题，尚未从根本上解决。""",
    293: """上下文组织效率用上下文压缩率衡量，本文将其作为基本指标：""",
    391: """演化机制最终落实为两件事：把过程知识沉淀为稳态知识，并对知识图谱执行增量更新。""",
    589: """• 统计各方案的开发工时和返工次数，并对组间差异作出归因，相关数据待回填。""",
    592: """• 汇总需求符合度的双人评审结果及分歧仲裁记录，数据待回填。""",
    593: """• 比较代码 review 通过率并分析差异来源，数据待回填。""",
    596: """• 在等价任务条件下，对照各方案的上下文构造方式，记录上下文规模和召回率，数据待回填。""",
    604: """• 比较变更可追溯性和文档完整度，相关数据待回填。""",
    614: """本文与现有方法（工具）的差异体现为设计取舍，而不是对工具能力作高下判断，主要可从三方面说明：""",
    625: """• 冷启动：缺少能力规范时，系统退化为纯结构检索。""",
}


CITATION_RE = re.compile(r"(\[\d+(?:[-,]\d+)*\])")


def file_sha256(path: Path) -> str:
    digest = hashlib.sha256()
    with path.open("rb") as fh:
        for chunk in iter(lambda: fh.read(1024 * 1024), b""):
            digest.update(chunk)
    return digest.hexdigest().upper()


def replace_paragraph_text(paragraph, text: str) -> None:
    p = paragraph._p
    if p.xpath(".//m:oMath | .//m:oMathPara"):
        raise RuntimeError(f"Refusing to replace a paragraph containing OMML: {paragraph.text[:80]}")

    for child in list(p):
        if child.tag in (qn("w:r"), qn("w:hyperlink")):
            p.remove(child)

    for part in CITATION_RE.split(text):
        if not part:
            continue
        run = paragraph.add_run(part)
        if CITATION_RE.fullmatch(part):
            run.font.superscript = True


def chapter1_items() -> list[str]:
    """Read the chapter-1 Markdown source, excluding its reference mapping."""
    markdown = CHAPTER1_MARKDOWN.read_text(encoding="utf-8")
    body = markdown.split("\n---\n", 1)[0]
    items: list[str] = []
    for line in body.splitlines():
        line = line.strip()
        if not line or line.startswith("|") or line.startswith("```"):
            continue
        if line.startswith("#"):
            line = line.lstrip("#").strip()
        # Markdown emphasis is not represented in the thesis DOCX text runs.
        line = line.replace("**", "")
        items.append(line)
    # Word supplies the chapter number through the heading style; keep only its title text.
    if items:
        items = ["引言"] + items[1:]
    return items


def sync_chapter1_from_markdown(paragraphs) -> None:
    """Keep the送检版 chapter 1 exactly aligned with the Markdown source."""
    items = chapter1_items()
    start = next((i for i, p in enumerate(paragraphs) if p.text.strip() == "引言" and i > 150), None)
    end = next((i for i, p in enumerate(paragraphs[start + 1:], start + 1) if p.text.strip() == "相关技术"), None)
    if start is None or end is None or end - start != len(items) - 1:
        raise RuntimeError(f"Unexpected chapter-1 bounds: start={start}, end={end}, items={len(items)}")
    for offset, text in enumerate(items[:-1]):
        replace_paragraph_text(paragraphs[start + offset], text)
    final_xml = copy.deepcopy(paragraphs[end - 1]._p)
    paragraphs[end]._p.addprevious(final_xml)
    replace_paragraph_text(Paragraph(final_xml, paragraphs[end]._parent), items[-1])


def build() -> None:
    actual_hash = file_sha256(SOURCE)
    if actual_hash != SOURCE_SHA256:
        raise RuntimeError(f"Source DOCX changed: expected {SOURCE_SHA256}, got {actual_hash}")

    shutil.copy2(SOURCE, TARGET)
    document = Document(TARGET)
    paragraphs = list(document.paragraphs)

    for index, rewritten in sorted(REWRITES.items()):
        if index >= len(paragraphs):
            raise IndexError(f"Paragraph index out of range: {index}")
        paragraph = paragraphs[index]
        if not paragraph.text.strip():
            raise RuntimeError(f"Paragraph {index} is unexpectedly empty")
        replace_paragraph_text(paragraph, rewritten.strip())

    sync_chapter1_from_markdown(paragraphs)

    document.save(TARGET)
    print(f"saved: {TARGET}")
    print(f"rewritten paragraphs: {len(REWRITES)}")


if __name__ == "__main__":
    build()
