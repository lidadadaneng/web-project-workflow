# 面向 Web 项目开发的 AI 驱动六阶段工作流及其知识图谱上下文压缩方法

## 内容摘要

### 一、研究背景与问题

大语言模型的快速发展使 AI 辅助编程从代码补全扩展到更广泛的开发场景，但现有 AI 开发工具普遍存在三方面不足：一是**流程覆盖面窄**，主要聚焦编码环节，对需求分析、设计、计划、测试等上游环节支持薄弱；二是**可控性不足**，LLM 的非确定性与工程流程的确定性要求存在矛盾，直接由 AI 驱动流程易导致状态混乱、阶段跳跃、路径漂移等问题；三是**上下文效率低**，面对大型代码库时手动读文件既不全面又消耗大量 Token，长上下文还存在"Lost in the Middle"现象。

针对上述问题，本课题提出构建 **wpw（Web Project Workflow）**——一套 AI 驱动的六阶段 Web 项目开发工作流系统，及其配套的面向 LLM 上下文的能力-结构双层知识图谱与压缩方法。

### 二、拟研究的主要内容

本课题围绕"一个架构、两条主线、八项关键技术"展开研究：

**一个架构**：三层分离的 AI 协同开发架构。将系统严格分为 AI 层（理解/生成/交互）、CLI 层（状态/依赖/路径/模板）、文件系统层（工作区+稳态能力规范），确定性逻辑由代码承载，非确定性任务交由 AI，通过标准化契约约束 AI 行为。

**两条主线**：
- **工作流主线**：构建 BRD → PRD → Explore → Design → Plan → Test → Apply 的六阶段标准化开发工作流，配套强依赖检查、文档模板、Plan-as-tracker 任务追踪、归档驱动的能力沉淀等机制。
- **上下文主线**：研究面向 LLM 上下文的知识图谱压缩方法，实现"检索→裁剪→骨架抽取→序列化"的端到端上下文生成 Pipeline。

**八项关键技术**：
1. 三层分离架构与三段式编排契约；
2. 六阶段工作流引擎与 Plan-as-tracker 任务追踪；
3. 能力-结构双层知识图谱模型（C + L1/L2/L3）；
4. 多源证据融合的业务-代码映射方法（noisy-OR 聚合，4 个证据源）；
5. 置信度衰减加权锚点选择算法；
6. 加权双向 BFS 子图裁剪与语义-结构复合打分；
7. 距离感知骨架压缩与层级符号化序列化；
8. Token 预算迭代上下文生成与多级降级。

### 三、拟解决的关键问题与创新点

**关键问题一：如何在充分利用 AI 能力的同时保持开发流程的可控性？**
- **创新点一：三层分离的 AI 协同开发架构与三段式编排契约。** 将确定性逻辑（状态/依赖/路径）与非确定性逻辑（理解/生成/交互）严格分层，所有状态变更必经 CLI，AI 行为可审计；13 个阶段命令统一遵循"CLI 准备→AI 生成+交互→CLI 收尾"的三段式契约，通过大纲确认、落盘门禁等防线约束 AI 行为。

**关键问题二：如何建立业务语义与代码结构的跨维度关联，并实现知识的持续沉淀？**
- **创新点二：能力-结构双层知识图谱模型与归档驱动的稳态能力沉淀机制。** 提出 C（能力层）+ L1/L2/L3（结构层）的双层四维图谱模型，将业务能力规范作为图谱的稳态层与临时需求解耦；需求归档时自动分析归属的能力领域并增量合并规格，使业务知识随项目推进持续积累，形成组织级可复用知识库。
- **创新点三：基于 noisy-OR 的多源证据融合业务-代码映射方法。** 设计文档提取、命名匹配、语义匹配、Git 历史追溯四个独立证据源，采用 noisy-OR 概率模型聚合并设上限 0.95，每条边记录最高排名证据源实现可追溯性，比简单加权平均更具概率论依据。

**关键问题三：如何在有限 Token 预算内为 AI 提供最高密度的结构化代码上下文？**
- **创新点四：置信度衰减加权锚点选择算法。** 利用 C 层语义置信度动态调节 L1 模块层的权重（w_L1 = exp(−α·Conf_C)），高置信时抑制模块膨胀靠能力层精确定位，低置信时提高模块权重保证召回，单参数、可微分、物理意义明确。
- **创新点五：加权双向 BFS 子图裁剪与语义-结构复合打分。** 多锚点双向扩展（支持跨层 business_map），入度加权结构重要度，语义分（0.6）+结构分（0.4）+距离衰减的复合打分机制，按节点上限裁剪，实现相关性驱动的子图选择。
- **创新点六：距离感知骨架分级与 Token 预算迭代控制。** 节点到锚点距离决定输出粒度（full/standard/minimal 三级），三档整体压缩（loose/standard/extreme），层级符号化序列化（⊃/→/⇄）；5 级迭代降级策略（降压缩→减节点→减深度→提权重→仅锚点）保证输出在 Token 预算内，锚点始终保留。

**关键问题四：如何使开发计划文档既是人读的规划又是机器可追踪的任务系统？**
- **创新点七：Plan-as-tracker 单源真实任务追踪机制。** 将 Markdown checkbox 作为任务数据库，原地正则改写更新任务状态，进度自动聚合反规范化缓存，支持断点续作，人读友好、git 可 diff、无需独立任务存储。

### 四、研究方法与技术路线

本课题采用**架构设计→核心算法研究→系统实现→实验验证**的技术路线。实现上采用 TypeScript 开发 CLI 层，基于 Tree-sitter 实现多语言源码解析，使用本地 Embedding 模型（Xenova/bge-small-zh-v1.5）实现语义检索，采用纯 JSONL + 二进制向量索引的零数据库存储方案。AI 层以 Claude Code Skill 的形式实现，通过标准化 CLI 接口与确定性层交互。通过消融实验（映射质量、锚点选择、压缩效果）和实际开发任务对比实验验证系统的有效性。

---

## 1 文献综述

### 1.1 国外研究现状

本课题的核心赛道是**规格驱动（Spec-Driven）的 AI 工作流编排**，即通过规格文档约束 AI 行为、以可组合 Skill 为能力单元、用知识图谱提供结构化上下文。以下从四个紧密相关的方向展开综述。

#### 1.1.1 规格驱动开发与 OpenSpec 范式

规格驱动开发（Spec-Driven Development）的核心思想是"规格先行、代码后置"——先通过形式化或半形式化的规格描述系统行为，再由规格驱动设计、实现和测试。这一思想可追溯到形式化方法领域的 Z 记号、VDM、TLA+[1]、Alloy[2] 等，但形式化规格的高学习门槛使其难以在工业界普及。

近年来，轻量级规格驱动在 AI 开发浪潮中焕发新生。**OpenSpec** 是其中最具代表性的开源范式，它将规格从"形式化证明的数学对象"转变为"AI 可理解、人类可读的结构化文档"：
- 采用 Purpose / Requirements / Scenarios 的三段式结构，用自然语言+结构化标记描述系统能力；
- 规格文件（spec.md）与代码共存于仓库，既是文档也是 AI 的确定性约束；
- 支持增量变更（change → delta spec → sync → archive），使规格与代码同步演进。

与 OpenSpec 类似的工作还有：
- **Specification by Example**（SBE, 实例化需求）：通过具体示例描述系统行为，ATDD/BDD 是其工程化实践；
- **Architecture Decision Records**（ADR）：用轻量级文档记录架构决策，强调可追溯性和演进性；
- **RFC 流程**：在开源社区广泛使用的变更审议机制，强调讨论、评审和共识。

**与本课题的关系**：OpenSpec 的规格驱动思想直接启发了 wpw 的 C 层（能力层）设计——`wpw/specs/` 目录中的 OpenSpec 规格构成了知识图谱的稳态业务能力层。OpenSpec 的变更机制（changes/specs/archive）也与 wpw 的六阶段工作流形成对应：Explore→Design 对应 change proposal，Apply 对应 implementation，Archive 对应 spec sync。

**现有工作的局限**：
1. OpenSpec 及其同类工作主要聚焦规格本身的组织和管理，缺少与下游开发流程的系统化串联；
2. 规格的生成和维护仍以人工为主，缺少 AI 辅助的自动化沉淀机制；
3. 规格与代码的映射需要人工维护，缺乏自动建立和校验关联的技术手段。

#### 1.1.2 AI 工作流编排与 Harness 工程

随着 AI Agent 从"单轮对话"走向"多步骤任务执行"，**Agent Harness（智能体工程框架/脚手架）** 成为一个新兴工程领域。Harness 的核心问题是：如何在 LLM 的非确定性之上构建可靠的、可控的、可观测的工作流系统。

国外代表性工作包括：

**Claude Code Skill 系统（Anthropic）**：Claude Code 提出了 Skill 机制——将 AI 能力封装为可安装、可组合的 Markdown 技能包，每个 Skill 包含触发条件、工具集和系统提示。Skill 通过 `/<skill-name>` 斜杠命令调用，遵循"加载→执行→结束"的生命周期。这一设计使 AI 能力可以像软件库一样复用和分发，是 Harness 工程的重要实践[3]。

**LangGraph（LangChain）**：将 Agent 工作流建模为有向图，节点是工具/LLM 调用，边是条件跳转。支持循环、分支、持久化状态，适合构建多步骤 Agent 流程[4]。

**CrewAI**：多 Agent 协作框架，通过角色定义（Role）、任务分配（Task）和流程编排（Process）实现 Agent 团队协作，支持顺序流和层级流两种模式[5]。

**AutoGPT / OpenHands**：早期的自主 Agent 框架，强调目标驱动的自主规划和工具调用，但可控性和可靠性不足，逐渐向更结构化的编排模式演进。

**Harness 工程的核心设计原则**（在工业界逐渐形成共识）：
- **确定性骨架 + AI 血肉**：将流程控制、状态管理、输入输出校验等确定性逻辑用代码实现，AI 只负责理解和生成等非确定性环节；
- **可观测性**：每一步 Agent 的输入输出、工具调用、状态变更都可追踪、可回放、可调试；
- **失败可恢复**：支持断点续作、重试、降级等容错机制；
- **人在回路**：关键决策点引入人工确认，避免 Agent 跑偏。

**与本课题的关系**：wpw 的三层分离架构（AI 层 / CLI 层 / 文件系统层）正是 Harness 工程原则的具体实践——CLI 层是"确定性骨架"，AI 层是"血肉"，文件系统层是可观测、可回放的状态存储。wpw 的六阶段工作流可以看作是一个领域专用的 LangGraph：节点是六个阶段的 AI 命令，边是强/弱依赖关系，状态由 CLI 统一管理。

**现有工作的局限**：
1. LangGraph、CrewAI 等通用编排框架缺乏软件工程领域的领域知识，需要用户自行设计流程；
2. Claude Code Skill 侧重单个能力的封装，缺少跨 Skill 的长流程编排和状态管理；
3. 多数 Harness 框架把"AI 驱动流程"理解为 AI 自己规划路径，缺乏对"流程是确定性的、AI 只是执行者"这一模式的系统性支持。

#### 1.1.3 AI 技能生态与 Superpowers

AI 技能（Skill / Plugin / Tool）是 Harness 生态的核心组成部分——将可复用的 AI 能力封装为标准化单元，按需加载、组合使用。

**obra/superpowers** 是 Claude 生态中最具影响力的开源 Skill 集合之一，包含几十个经过打磨的生产级 Skill，代表性的有：
- **brainstorming**：结构化头脑风暴，用于需求澄清、方案探索；
- **code-reviewer**：代码审查专家，从正确性、性能、安全等维度评审代码变更；
- **requesting-code-review**：发起代码审查的协调型 Skill；
- **product-manager**：产品经理视角的需求分析和优先级排序。

Superpowers 的核心价值在于：
1. **经过验证的提示工程**：每个 Skill 都是反复迭代打磨的高质量系统提示，开箱即用；
2. **标准化的调用约定**：统一的触发方式、输入输出格式，便于组合；
3. **社区驱动的生态**：开源社区持续贡献新 Skill，形成可复用的 AI 能力市场。

其他相关工作：
- **Plugin 生态**：ChatGPT Plugins、Cursor Add-ons、Windsurf Extensions 等平台级插件系统；
- **MCP（Model Context Protocol）**：Anthropic 提出的工具调用协议，标准化 Agent 与外部工具的交互方式；
- **Agent Tools**：LangChain、LlamaIndex 等框架中的工具抽象层。

**与本课题的关系**：wpw 的 AI 层直接构建在 Skill 体系之上——`wpw init` 将 wpw-workflow 主 Skill 及联动 Skill（brainstorming、code-reviewer、humanizer-zh）释放到项目的 `.claude/skills/` 目录，13 个 `/wpw:xxx` 命令是工作流 Skill 的子命令。wpw 本质上是 superpowers 模式在"Web 项目开发全流程"这一垂直领域的深化——不仅提供单个 Skill，还提供了 Skill 之间的编排关系、状态流转和数据通路。

**现有工作的局限**：
1. Superpowers 等通用 Skill 集覆盖了单点能力（代码审查、头脑风暴等），但缺少垂直领域的端到端工作流 Skill；
2. Skill 之间的协作主要靠人工调用顺序，缺少结构化的流程编排和状态共享机制；
3. 技能生态缺乏与项目知识（代码库、业务逻辑）的深度整合，Skill 的上下文获取仍依赖手动操作。

#### 1.1.4 代码知识图谱与 AI 上下文工程

上下文工程（Context Engineering）是 AI 开发效率的关键变量——给 AI 什么信息、以什么格式给、给多少，直接决定了 AI 的输出质量和 Token 成本。

**代码知识图谱（Code Knowledge Graph）** 是结构化代码上下文的重要技术路径。国外代表性研究：
- **GraphCodeBERT**（微软，2020）：将代码数据流图引入预训练模型，通过结构信息增强代码表示[6]；
- **CodeSearchNet**（GitHub，2019）：大规模代码检索数据集和基准，推动了代码语义检索的研究[7]；
- **GitHub Code Search**：基于语义索引的代码搜索工具，支持自然语言查询代码。

**业务-代码追踪（Traceability Link Recovery）** 是需求工程领域的长期研究问题——如何建立需求文档到代码实现的可追踪关联。传统方法基于信息检索（IR）和关键词匹配，近年来开始引入 LLM 和语义嵌入技术。Capobianco 等人（2023）的综述总结了该领域的发展脉络，指出语义嵌入和多源融合是当前主要技术趋势[8]。

**RAG 与上下文压缩**：检索增强生成（RAG）已成为长上下文场景的标准范式[9]。LangChain 提出了文档压缩器（Document Compressor）概念，通过摘要提取、重排序和 Token 截断减少输入。但通用 RAG 框架主要面向文档问答场景，面向代码结构的专用压缩方法研究较少。

**与本课题的关系**：wpw 的知识图谱子系统是上下文工程在代码领域的专用实现。与通用代码图谱不同，wpw 引入了 C 层（业务能力层）这一稳态维度，将规格驱动（OpenSpec）与代码结构结合，形成"业务-结构"双维度图谱。多源证据融合（noisy-OR）的 business_map 边本质上是对追踪链路恢复（Traceability Link Recovery）问题的工程化解答。

**现有工作的局限**：
1. 多数代码知识图谱研究侧重检索和查询，缺少面向 LLM 上下文消费的端到端优化；
2. 业务-代码追踪研究以学术基准评测为主，缺乏工程化的多源融合框架和可追溯性设计；
3. 上下文压缩研究主要面向文本文档，针对代码知识图谱的结构感知压缩方法较少；
4. 缺少将知识图谱系统与 AI 工作流深度整合的完整系统——图谱是孤立工具，而非工作流的基础设施。

### 1.2 国内研究现状

国内在 AI 编程工具、知识图谱、需求工程等方向已有较多积累，但在本课题聚焦的"规格驱动工作流 + Skill 生态 + 图谱上下文"交叉领域，系统性工作相对较少。

#### 1.2.1 AI 编程工具与大模型

国内大模型厂商纷纷布局 AI 编程领域：
- **CodeGeeX**（智谱 AI，2023）：开源多语言代码生成模型，支持 20+ 编程语言，在 HumanEval-X 基准上取得较好成绩[10]。CodeGeeX 也推出了 IDE 插件和编程助手产品；
- **通义灵码**（阿里云）：基于通义大模型的 AI 编码助手，支持代码补全、生成、解释、测试等功能；
- **豆包编程助手**（字节跳动）：面向开发者的 AI 编程工具，集成了代码生成、调试、解释等能力；
- **讯飞星火编程助手**、**文心一言编码功能**等。

这些工具主要聚焦代码补全和单轮问答层面，与 Copilot 类似，缺少对开发全流程的覆盖。

#### 1.2.2 代码知识图谱与程序分析

国内高校和研究机构在代码知识图谱和程序分析领域有扎实积累：
- **复旦大学 CodeKG**：构建了面向 Java 项目的多维度代码知识图谱，支持代码检索、缺陷定位和变更影响分析[11]；
- **中科院软件所 CP-Graph**：将代码属性图（CPG）与概念图谱结合，提升代码的语义理解能力；
- **清华大学代码表示学习**：在代码预训练模型、代码克隆检测、漏洞挖掘等方向发表了多篇高水平论文。

这些研究主要聚焦学术问题和特定任务（如缺陷检测），面向 AI 工作流上下文场景的工程化研究较少。

#### 1.2.3 需求工程与 AI 结合

需求工程是软件工程的传统方向，国内学者在该领域有持续研究：
- **北京大学 RE-Agent 系列工作**：探索基于多 Agent 协作的需求分析、需求分类和规格生成；
- **同济大学、北航等**：在需求追踪、需求变更影响分析方面有长期积累。

但总体而言，国内研究主要聚焦需求工程本身的自动化，缺少将需求规格与下游开发流程、代码实现通过 AI 系统化串联的工作。规格驱动开发（特别是 OpenSpec 范式）在国内的传播和实践还处于早期阶段。

#### 1.2.4 国内 Harness 与 Skill 生态现状

Agent Harness 和 Skill 生态在国内仍处于发展初期：
- 部分创业团队开始探索 AI Agent 开发框架，但多为通用框架，缺少垂直领域深度；
- Skill 生态尚未形成气候，缺乏类似 superpowers 这样经过打磨的高质量开源 Skill 集合；
- 规格驱动 + AI 工作流的理念在国内工程社区传播有限，实践案例不多。

### 1.3 总体评价与研究定位

综合国内外研究现状，可以得出以下判断：

**第一，规格驱动开发正在经历 AI 时代的复兴。** OpenSpec 等轻量级规格范式降低了规格的使用门槛，AI 又提供了自动生成和维护规格的能力，二者结合形成"AI 驱动的规格驱动开发"这一新方向。但现有工作（包括 OpenSpec 本身）主要停留在规格管理层面，缺少与开发全流程的系统化串联和闭环机制。

**第二，Agent Harness 工程正在形成共识，但领域专用方案不足。** "确定性骨架 + AI 血肉"的架构原则已在工业界形成基本共识，LangGraph、CrewAI、Claude Skill 等框架提供了通用编排能力。但在 Web 开发这一具体领域，缺少经过工程验证的端到端工作流方案——团队仍然各自搭建流程，重复造轮子。

**第三，AI 技能生态蓬勃发展，但缺少垂直工作流级别的 Skill 整合。** superpowers 等通用 Skill 集证明了可组合 AI 能力的价值，但 Skill 之间是松散的、靠人工串联调用，缺少流程编排、状态共享和数据通路层面的系统化整合。

**第四，代码知识图谱研究丰富，但面向 LLM 上下文的端到端优化不足。** 代码图谱在检索、查询、缺陷检测等场景已有大量研究，但将图谱作为 AI 工作流的上下文基础设施、专门针对 LLM 消费优化的端到端压缩管线（检索→裁剪→骨架→序列化→Token 预算控制）研究较少，业务语义与代码结构的双维度建模更是空白。

**本课题的研究定位**：

wpw 处于上述四个方向的交叉点——**以规格驱动为方法论、以 Harness 工程为架构原则、以 Skill 为能力单元、以知识图谱为上下文基础设施**，构建一套面向 Web 项目开发的 AI 驱动六阶段工作流系统。与现有工作相比，本课题的独特之处在于：

1. **垂直整合**：不是通用 Harness 框架，而是针对 Web 开发场景的全流程垂直方案，包含六个阶段的模板、依赖关系和最佳实践；
2. **双轮驱动**：规格驱动（稳态能力层）+ 工作流驱动（临时需求层）双轮运转，需求结束后能力沉淀回规格，形成知识积累闭环；
3. **图谱即基础设施**：知识图谱不是附加工具，而是工作流各阶段的统一上下文提供者，嵌入 apply/cr/explore/design/test 等每一个需要读代码的环节；
4. **三层分离的严格执行**：将"AI 不碰状态"从口号落到 13 个命令的统一契约，状态变更必须经过 CLI，行为可审计。

### 1.3 相关基础

#### 1.3.1 大语言模型与代码理解

大语言模型通过在海量代码数据上进行预训练，获得了强大的代码理解与生成能力。其核心技术基础包括：

- **Transformer 架构**：基于自注意力机制的神经网络架构，能够建模长距离依赖关系，是 GPT、Claude、Llama 等主流大模型的基础[10]。
- **代码预训练**：在 GitHub 等开源代码库上进行大规模预训练，使模型掌握编程语言的语法规则、设计模式和常见实现方式。
- **指令微调与对齐**：通过指令微调（Instruction Tuning）和人类反馈强化学习（RLHF）使模型能够理解自然语言指令并生成符合预期的代码。

代码理解的关键挑战在于**上下文窗口限制**。即使是 200K Token 的大模型，面对中型项目（数万文件、数百万行代码）也无法一次性加载全部源码。因此，如何高效检索和压缩相关代码上下文，是 LLM 应用于大型项目开发的核心问题。

#### 1.3.2 知识图谱技术

知识图谱是一种结构化的语义网络，由节点（实体）和边（关系）组成。在代码领域，知识图谱可用于表示代码元素及其关系：

- **节点类型**：模块、文件、类、函数、接口、变量等；
- **关系类型**：包含（contain）、调用（call）、导入（import）、继承（inherit）、实现（implement）等；
- **属性信息**：函数签名、参数类型、返回值、JSDoc 注释、修饰符等。

代码知识图谱的构建通常基于**静态分析**技术：
- **抽象语法树（AST）解析**：通过 Tree-sitter、ANTLR 等解析器生成源码的语法树，提取结构化信息；
- **依赖分析**：通过分析 import 语句、函数调用等建立模块间和文件间的依赖关系；
- **语义提取**：从注释、文档字符串、命名规范中提取语义信息。

本项目使用 Tree-sitter 作为解析引擎，它是一个高效的增量解析器库，支持 JavaScript、TypeScript 等多种编程语言，具有解析速度快、错误容忍度高的特点。

#### 1.3.3 语义检索与向量嵌入

语义检索（Semantic Search）通过将文本映射为高维向量（Embedding），利用向量空间中的距离衡量语义相似度。核心技术包括：

- **文本向量化模型**：BERT、GPT、BGE（BAAI General Embedding）等预训练模型可将文本转换为固定维度的向量表示。本项目使用 `Xenova/bge-small-zh-v1.5` 模型，输出 512 维向量，支持中英文语义匹配。
- **相似度计算**：余弦相似度（Cosine Similarity）是最常用的向量相似度度量方法，取值范围为 [-1, 1]，值越大表示语义越相似。
- **向量索引**：对于大规模向量数据，可使用 FAISS、Annoy、HNSW 等近似最近邻（ANN）索引技术加速检索。

在代码场景中，语义检索能够突破关键词匹配的局限，实现"按概念找代码"——用户用自然语言描述想要的功能，系统返回语义相关的代码片段。

#### 1.3.4 软件开发流程与工作流引擎

软件开发流程通常遵循一定的方法论和阶段划分：

- **瀑布模型**：需求分析→设计→实现→测试→部署，线性推进；
- **敏捷开发**：Scrum、Kanban 等迭代式开发方法，强调快速反馈和持续交付；
- **DevOps**：开发与运维一体化，通过 CI/CD 流水线实现自动化构建、测试和部署。

工作流引擎（Workflow Engine）是实现流程自动化的核心组件，其关键能力包括：
- **状态管理**：跟踪每个任务/需求的当前阶段状态；
- **依赖检查**：确保前置条件满足后方可进入下一阶段；
- **模板化**：通过标准化模板统一各阶段输出格式；
- **可审计性**：记录状态变更历史，支持追溯和复盘。

#### 1.3.5 三层分离架构思想

本课题的核心架构思想——"三层分离"（AI 层 / CLI 层 / 文件系统层），源于以下技术趋势和设计原则：

- **关注点分离（Separation of Concerns）**：将不同职责的模块解耦，每层专注单一目标；
- **确定性与非确定性分离**：将状态管理、依赖检查等确定性逻辑交由程序代码承载，将理解、生成、交互等非确定性任务交由 AI 处理；
- **AI 作为协作者而非控制者**：AI 拥有强大的生成能力但缺乏可靠性保证，因此将其定位为"执行者"而非"决策者"，关键控制权保留在确定性层。

这一思想与软件架构中的"命令-查询分离"（CQRS）、"不可变基础设施"等设计原则一脉相承，也与自动驾驶中的"人在回路"（Human-in-the-Loop）理念相通——在 AI 能力尚未完全可靠的阶段，通过架构设计约束 AI 的行为边界，确保系统整体可控。

#### 1.3.6 Noisy-OR 证据融合模型

Noisy-OR 是一种概率图模型中的条件概率分布，用于建模多个独立证据对结论的累积影响。其核心公式为：

$$
P(Y=1 | x_1, x_2, ..., x_n) = 1 - \prod_{i=1}^{n} (1 - p_i)^{x_i}
$$

其中 $p_i$ 是第 $i$ 个证据单独导致结论成立的概率。当多个证据同时成立时，最终概率通过"1 减去各证据不导致结论的概率之积"计算，体现了"多证据独立增强"的物理意义。

在本课题中，noisy-OR 模型用于融合多个独立证据源（文档提取、命名匹配、语义匹配、Git 历史追溯）对"业务能力-代码元素"映射关系的置信度，比简单加权平均更具概率论依据。

---

## 2 课题来源和选题意义

### 2.1 课题来源

本课题来源于实际工程项目中 AI 辅助开发的痛点与需求。随着大语言模型技术的快速发展，AI 辅助编程工具（如 GitHub Copilot、Cursor 等）已广泛应用于日常开发中，显著提升了编码效率。然而，在实际 Web 项目开发过程中，仍存在以下突出问题：

**第一，AI 辅助停留在编码层面，缺乏全流程覆盖。** 现有工具主要在代码编辑时提供补全和建议，而对于需求分析、技术方案设计、开发计划制定、测试方案设计等上游环节的支持薄弱，开发流程的大部分工作仍需人工完成。

**第二，AI 的非确定性与工程流程的确定性要求存在矛盾。** 直接让 AI 驱动开发流程会导致状态混乱、阶段跳跃、文件路径不一致等问题，难以保证工程质量和可追溯性。如何在充分利用 AI 能力的同时保持流程的可控性，是一个亟待解决的架构问题。

**第三，大型项目的代码上下文获取效率低下。** 当 AI 需要理解已有代码进行开发或修改时，开发者通常通过手动 grep、翻目录、读文件的方式提供上下文，既不全面又耗费大量 Token。即使大模型的上下文窗口不断扩大，"Lost in the Middle"现象和 Token 成本仍制约着长上下文的有效利用。

基于上述背景，本课题提出构建一套 AI 驱动的 Web 项目开发工作流系统（wpw, Web Project Workflow），并重点研究面向 LLM 上下文的知识图谱压缩方法，旨在通过系统化的工程方法和知识图谱技术，提升 AI 辅助 Web 开发的全流程效率。

### 2.2 课题目的

本课题旨在设计并实现一套面向 Web 项目开发的 AI 驱动六阶段工作流系统，及其配套的知识图谱上下文压缩方法。具体目标如下：

**目标一：构建三层分离的 AI 协同开发架构。** 设计 AI 层 / CLI 层 / 文件系统层的三层分离架构，将确定性逻辑（状态管理、依赖检查、路径解析、模板加载）交由 CLI 承载，非确定性任务（理解、生成、交互）交由 AI 处理，通过标准化契约约束 AI 行为，实现"AI 能力强"与"流程可控"的平衡。

**目标二：建立六阶段标准化开发工作流。** 定义 BRD（业务需求）→ PRD（产品需求）→ Explore（技术探索）→ Design（技术设计）→ Plan（开发计划）→ Test（测试方案）→ Apply（编码实施）的六阶段流程，配套强依赖检查、文档模板、状态追踪等机制，使 AI 辅助开发有章可循。

**目标三：提出能力-结构双层知识图谱模型。** 构建 C（业务能力层）+ L1/L2/L3（结构层）的双层四维知识图谱，通过多源证据融合自动建立业务能力到代码元素的跨层映射，解决传统代码图谱缺少业务语义维度的问题。

**目标四：研究面向 LLM 上下文的知识图谱压缩方法。** 设计"检索→裁剪→骨架抽取→序列化"的端到端上下文生成 Pipeline，包括置信度衰减加权锚点选择、加权双向 BFS 子图裁剪、距离感知骨架分级、Token 预算迭代控制等关键技术，在有限 Token 预算内输出高密度结构化代码上下文。

**目标五：验证系统的有效性与实用性。** 通过实际项目测试和对比实验，验证工作流系统在开发效率、代码质量、流程规范性等方面的提升效果，以及知识图谱上下文压缩方法在 Token 节省、信息保留等方面的性能表现。

### 2.3 选题意义

**理论意义：**

1. **提出了 AI 协同开发的三层分离架构模式**，为解决"LLM 非确定性与工程流程确定性"的矛盾提供了一种系统化的架构方案，丰富了 AI 软件工程（AI4SE）领域的架构设计理论。

2. **构建了能力-结构双层知识图谱模型**，将业务能力规范作为图谱的稳态层与临时需求解耦，拓展了代码知识图谱的维度和应用场景，为代码语义理解提供了新的建模思路。

3. **提出了面向 LLM 上下文的知识图谱压缩方法体系**，包括置信度衰减锚点选择、语义-结构复合打分裁剪、距离感知骨架分级、Token 预算迭代控制等技术，丰富了知识图谱摘要与上下文压缩的研究内容。

4. **将 noisy-OR 概率模型应用于多源证据融合的业务-代码映射**，为跨模态关联的置信度计算提供了有概率论依据的方法，具有方法论参考价值。

**实践意义：**

1. **提升 Web 项目开发效率**：通过 AI 辅助覆盖需求-设计-计划-测试-编码全流程，减少重复性文档工作，加速开发迭代。

2. **降低 AI 使用门槛**：标准化的工作流和模板化输出使开发者无需精通 Prompt Engineering 即可获得高质量的 AI 辅助，降低了 AI 工具的使用成本。

3. **提高代码上下文获取效率**：知识图谱上下文压缩方法能够以更少的 Token 提供更精准的代码结构信息，显著降低大模型 API 调用成本，提升代码理解效率。

4. **沉淀组织级知识资产**：能力-结构双层图谱和归档驱动的知识积累机制，使项目过程中的业务知识得以沉淀和复用，形成组织级知识库，支撑长期项目演进。

5. **推广 AI 原生开发模式**：三层分离架构和六阶段工作流为中小型团队引入 AI 辅助开发提供了可落地的方法论和工具参考，具有较好的推广价值。

---

## 3 研究内容和时间安排

### 3.1 研究内容

本课题的研究内容围绕"一个架构、两条主线、八项关键技术"展开。一个架构即三层分离的 AI 协同开发架构；两条主线分别是工作流主线和知识图谱主线；八项关键技术分布于两条主线中。

#### 3.1.1 三层分离的 AI 协同开发架构

**研究内容**：设计并实现 AI 层 / CLI 层 / 文件系统层的三层分离架构，明确各层的职责边界和协作方式。

- **AI 层**：以 Claude Code Skill 的形式实现，负责需求理解、文档生成、用户交互等非确定性任务。通过调用 CLI 命令完成状态变更和数据查询。
- **CLI 层**：以 Node.js TypeScript 命令行工具的形式实现，承载状态管理、依赖检查、路径解析、模板加载、图谱构建与查询等所有确定性逻辑。
- **文件系统层**：定义标准化的工作区目录结构（`wpw/active/{需求}/`）、状态文件（`.wpw.yaml`）、知识图谱存储（`wpw/knowledge/graph/`）和能力规范（`wpw/specs/`）。

**关键问题**：
- 如何设计层间契约，确保 AI 行为可审计、状态变更可控；
- 如何处理 AI 输出的非结构化结果与 CLI 结构化输入之间的适配；
- 如何实现图谱缺失、向量索引缺失等异常情况下的优雅降级。

#### 3.1.2 六阶段工作流引擎

**研究内容**：设计 BRD → PRD → Explore → Design → Plan → Test → Apply 的六阶段开发工作流，并实现支撑该工作流的引擎系统。

- **阶段定义**：明确每个阶段的输入、输出、依赖关系和产出物模板。BRD 输出业务需求文档，PRD 输出产品需求文档，Explore 输出技术探索方案，Design 输出技术设计文档，Plan 输出开发计划，Test 输出测试方案，Apply 输出可运行代码。
- **依赖管理**：实现强依赖（前置阶段必须完成）和弱依赖（有则参考、无则跳过）的依赖检查机制，确保流程顺序正确。
- **状态管理**：实现需求的生命周期状态管理，包括进行中、已完成、已跳过、已归档等状态，支持状态查询和变更追踪。
- **模板系统**：为每个阶段提供标准化的文档模板，支持按项目类型（前端 H5、后端 Node.js、全栈）自动选择模板，并允许用户自定义覆盖。
- **Plan-as-tracker**：将开发计划文档（Markdown checkbox）作为任务追踪的单一真实来源，实现任务进度的原地更新和进度聚合。

**关键问题**：
- 如何设计灵活且严格的依赖关系模型，既保证流程规范又不过度约束；
- 如何实现 Plan 文档即任务数据库的低仪式感设计，兼顾人读友好和程序可解析；
- 如何处理 Explore 等可选阶段的拍板（decision）和跳过（skip）逻辑。

#### 3.1.3 能力-结构双层知识图谱模型

**研究内容**：提出 C（能力层）+ L1/L2/L3（结构层）的双层四维代码知识图谱模型，解决传统代码图谱缺少业务语义维度的问题。

- **C 层（Capability Layer，能力层）**：来源于 OpenSpec 格式的能力规范文档，描述系统具备的业务能力及其功能点、场景需求。能力层是稳态的，不随具体需求的产生和完成而变化。
- **L1 层（Module Layer，模块层）**：通过源码目录结构识别的业务模块，是业务语义与代码结构的交汇层。
- **L2 层（File Layer，文件层）**：源码文件节点，包含文件路径、语言类型、导出内容等信息。
- **L3 层（Element Layer，元素层）**：函数、类、接口、组件、状态管理单元（Pinia/Vuex/Redux store）等代码元素节点。

**边类型**：
- contain（包含）：高层级节点包含低层级节点；
- import（导入）：文件间的模块导入关系；
- business_map（业务映射）：C 层能力节点到结构层节点的跨层关联。

**稳态能力沉淀机制**：需求归档时自动分析其归属的业务能力领域，将需求增量合并到对应的能力规范中，实现知识的持续积累。

**关键问题**：
- 如何从非结构化的能力规范文档中提取结构化的能力节点信息；
- 如何设计能力规范的变更检测与增量更新机制；
- 如何实现冷启动项目（无能力规范）下的图谱构建与后续补充。

#### 3.1.4 多源证据融合的业务-代码映射方法

**研究内容**：设计多源证据融合框架，自动建立业务能力（C 层）到代码元素（结构层）的跨层关联，无需手动标注。

**四个已实现的证据源**：
1. **文档提取（doc-extract）**：从能力规范文档中正则抽取模块名、关键词等命名实体，与代码中的同名实体建立映射。基础权重 0.85。
2. **命名匹配（name-match）**：基于 30+ 中英词典的命名匹配，支持前缀匹配、包含匹配等多种匹配模式。权重上限 0.7。
3. **语义匹配（semantic）**：通过向量嵌入计算能力描述与代码节点的语义相似度，建立语义映射。基础权重 = min(0.7, sim×0.6)。
4. **Git 历史追溯（git-history）**：分析 commit message 中提及的业务关键词与文件修改频次的关联，通过历史行为建立映射。基础权重 = min(0.7, normFreq×0.6)。

**noisy-OR 证据聚合**：采用 noisy-OR 模型融合多个独立证据源的权重，公式为：
$$ finalWeight = 1 - \prod(1 - w_i) $$
上限设为 0.95，保留一定不确定性。同时记录最高排名的证据源作为边的溯源标签（EdgeSource），实现边的可追溯性。

**关键问题**：
- 如何选择和设计各证据源的权重计算方法，确保映射的准确率和召回率；
- 如何处理各证据源之间的相关性（独立性假设的偏差）；
- 如何评估多源融合相比单源的性能提升。

#### 3.1.5 置信度衰减加权锚点选择算法

**研究内容**：提出基于 C 层置信度动态调节 L1 层权重的锚点选择算法，解决语义检索中粗粒度模块层节点子图膨胀的问题。

**算法原理**：
1. 对所有层级节点进行语义检索，获得原始相似度得分；
2. 计算 C 层最高置信度 Conf_C = max(所有 C 层节点得分)；
3. 计算 L1 层衰减权重 w_L1 = exp(−α · Conf_C)，其中 α 为衰减系数（默认 3.0）；
4. 调整 L1 层有效得分：L1_effective = L1_raw × w_L1；
5. 所有节点按有效得分排序，取 Top-K 作为锚点。

**物理意义**：
- 当查询能精准匹配到某个业务能力（高 C 置信）时，抑制模块层膨胀，依靠 C 层锚点沿 business_map 精确定位；
- 当查询找不到匹配的能力（低 C 置信）时，提高模块层权重，靠模块级检索保证召回；
- 当 C 层为空（冷启动项目）时，退化为纯结构检索，不影响系统可用性。

**关键问题**：
- 衰减系数 α 的最优取值与项目规模、图谱质量的关系；
- 为何仅衰减 L1 层而不衰减 L2/L3 层的理论依据；
- 与直接使用所有层检索结果相比，该算法在锚点质量和最终上下文效果上的差异。

#### 3.1.6 加权双向 BFS 子图裁剪与语义-结构复合打分

**研究内容**：设计从锚点扩展子图并进行相关性裁剪的方法，在节点上限与 Token 预算约束下保留最相关的节点。

**多锚点加权双向 BFS**：
- 每个锚点同时沿出边（下游依赖）和入边（上游依赖）双向扩展；
- 仅遍历权重大于等于阈值的边，过滤弱关联；
- 多锚点扩展结果合并，取最小距离作为节点的最终距离；
- 支持 business_map 边的跨层扩展（C 层锚点 → 结构层节点）。

**语义-结构复合打分**：
- 结构重要度：入边权重 × 1.0 + 出边权重 × 0.5，归一化到 [0,1]。入度权重更高，因为被越多节点依赖的节点越核心、越不能裁剪。
- 综合得分：score = (semanticScore × 0.6 + structuralScore × 0.4) × distFactor
- 距离衰减因子：distFactor = max(0.7, 1 − dist × 0.05)，防止远距离节点因结构重要度高而抢分。
- 锚点节点强制保留（得分 1.0）。

**节点上限裁剪**：锚点必留 + 其余节点按综合得分降序取前 N 个，裁剪后重新收集两端都在子图中的边。

**关键问题**：
- 语义分与结构分的权重分配（0.6:0.4）的合理性；
- 入度加权高于出度的设计依据和适用场景；
- 距离衰减因子的函数形式（线性衰减 vs 指数衰减）对结果的影响。

#### 3.1.7 距离感知骨架压缩与层级符号化序列化

**研究内容**：设计基于节点到锚点距离的骨架分级抽取方法，以及层级化、符号化的序列化输出格式，最大化 Token 信息密度。

**距离感知骨架分级**：
- 距离 0（锚点）：full 等级——输出名称 + 签名 + 入参出参类型 + JSDoc + 注解 + 父类；
- 距离 1：standard 等级——输出名称 + 签名 + 类型；
- 距离 ≥2：minimal 等级——仅输出名称 + 类型标签。

**三档压缩等级**：
- loose：在距离分级基础上整体提高一级详细度，附带 JSDoc 和完整依赖列表；
- standard：默认等级，平衡信息密度和 Token 消耗；
- extreme：整体降低一级详细度，锚点也只给 standard，依赖边仅保留 business_map 与高权重边。

**层级符号化序列化**：
- 按 C → L1 → L2 → L3 的层级顺序输出；
- 使用标准化符号表达关系：⊃（包含）、→（依赖）、⇄（业务映射）；
- C 层能力节点单独列出，附带 business_map 关联目标和功能条目；
- 锚点节点用 ◉ 标记，便于 LLM 识别核心关注点。

**关键问题**：
- 距离分级的粒度划分（3 级是否足够）与阈值选择；
- 符号化表示对 LLM 理解效率的影响；
- 不同压缩等级在各类开发任务中的适用性。

#### 3.1.8 Token 预算迭代上下文生成与多级降级

**研究内容**：设计带 Token 预算约束的上下文生成迭代算法，保证输出不超过 LLM 窗口限制，同时尽可能保留核心信息。

**Token 估算公式**：
$$ tokens = 中文字符数 / 1.5 + 英文词数 × 0.75 + 其他字符数 / 4 $$

**五级迭代降级策略**（由宽松到严格，最多 8 次迭代）：
1. 降低压缩档：loose → standard → extreme；
2. 节点上限减半（下限 10）；
3. 扩展深度减 1；
4. 提升最小权重阈值 +0.1（上限 0.9）；
5. 最终降级：仅锚点节点 + extreme 压缩（仍超则加预算不足警告）。

每级调整后重新生成上下文并估算 Token，满足预算即返回。锚点始终保留，确保核心信息不丢失。

**三级系统降级**：
- 图谱不存在 → 提示并回退手动读文件；
- 向量索引缺失 → context 自动降级为 --anchors 模式；
- 0 锚点 → 回退手动读文件，提示图谱未匹配。

**关键问题**：
- Token 估算公式的准确性（与实际 tokenizer 的偏差）；
- 迭代降级策略的效率（迭代次数与最终质量的权衡）；
- 降级顺序的合理性（先降压缩 vs 先减节点数）。

### 3.2 研究方案

#### 3.2.1 技术路线

本课题采用"架构设计→核心算法研究→系统实现→实验验证"的技术路线，具体分为以下步骤：

**第一步：架构设计与需求分析**
- 调研现有 AI 辅助开发工具的架构模式和优缺点；
- 明确三层分离架构的各层职责、接口契约和交互方式；
- 定义六阶段工作流的阶段模型、依赖关系和产出物规范；
- 设计能力-结构双层知识图谱的 Schema（节点类型、边类型、属性字段）。

**第二步：核心算法研究**
- 研究多源证据融合的业务-代码映射方法，设计各证据源的权重计算方案和 noisy-OR 聚合模型；
- 研究置信度衰减加权锚点选择算法，推导衰减函数形式和参数设置；
- 研究加权双向 BFS 子图裁剪算法，设计语义-结构复合打分机制；
- 研究距离感知骨架压缩与 Token 预算迭代控制方法，建立压缩质量评估指标。

**第三步：系统实现**
- 实现 CLI 层核心功能：命令解析、状态管理、依赖检查、模板加载；
- 实现知识图谱构建模块：源码解析器（JS/TS/Vue/Pinia/Redux/Vuex/小程序/uni-app 等）、图谱构建器、向量索引构建；
- 实现图谱查询与检索模块：结构化查询（上下游依赖、最短路径）、语义检索、置信度衰减锚点选择；
- 实现上下文生成 Pipeline：子图裁剪、骨架抽取、层级序列化、Token 预算迭代控制；
- 实现 AI 层 Skill：13 个 /wpw:xxx 命令，遵循三段式契约；
- 实现工作流配套功能：Plan-as-tracker 任务追踪、归档与能力沉淀、增量更新。

**第四步：实验验证与评估**
- 构建测试数据集：选择若干不同规模的 Web 项目作为测试对象；
- 工作流评估：对比使用 wpw 前后的开发效率、文档质量、流程规范性；
- 图谱质量评估：评估业务-代码映射的准确率、召回率、F1 值；
- 压缩效果评估：测量不同压缩等级下的 Token 压缩率、信息保留率、检索准确率；
- 用户体验评估：通过实际使用反馈评估系统的易用性和实用性。

#### 3.2.2 关键技术及实现方案

**（1）基于 Tree-sitter 的多语言源码解析**

使用 web-tree-sitter 作为解析引擎，支持 JavaScript、TypeScript 等语言。针对不同前端框架和技术栈，开发专用解析器：

- **JS/TS 通用解析**：提取函数、类、接口、常量等代码元素；
- **Vue 组件解析**：解析 .vue 单文件组件，提取 template、script、style 各部分；
- **Pinia/Vuex/Redux 解析**：识别状态管理 store 定义，提取 state、getter、action/mutation；
- **小程序/uni-app 解析**：识别小程序页面和组件，解析页面配置和生命周期。

解析器采用快速预检 + 完整解析的两级策略：先通过文件名和内容特征快速判断文件类型，只对匹配的文件进行完整 AST 解析，提升构建效率。

**（2）纯本地零数据库的图谱存储方案**

为实现零配置、纯本地运行，采用轻量级存储方案：

- **JSONL 图谱存储**：每行一个 JSON 对象表示节点或边，便于增量追加和人类可读；
- **二进制向量索引**：32 字节文件头（magic + version + dimensions + count）+ 连续 Float32Array 向量数据，零拷贝加载；
- **JSON 元数据与映射**：存储 schema 版本、构建时间、向量索引到节点 ID 的映射。

节点 ID 采用内容哈希（SHA-256 前 12 hex / 48 位），保证幂等重建和多用户一致性。Schema 版本管理支持主版本号变更时自动全量重建。

**（3）本地 Embedding 与国内镜像适配**

使用 `@xenova/transformers` 库在浏览器/Node.js 环境中本地运行 embedding 模型，无需调用外部 API，保护代码隐私。

- 模型选择：`Xenova/bge-small-zh-v1.5`（512 维，中英文支持，体积小速度快）；
- 国内镜像：配置 ModelScope 镜像源，解决 HuggingFace 国内访问受限问题；
- 懒加载模式：首次使用时自动下载模型（约 80MB），后续本地缓存。

**（4）AI 层 Skill 的三段式契约实现**

每个 `/wpw:xxx` 阶段命令统一遵循三段式编排：

**第一段：CLI 准备**
```
wpw new <需求名>          # 幂等创建，已存在则复用
wpw check <阶段> -c <需求> # 依赖检查，失败则退出码 1
wpw template <阶段> -c <需求>  # 获取模板路径
```

**第二段：AI 生成+交互**
- 基于模板生成文档大纲；
- 通过 AskUserQuestion 让用户确认大纲；
- 确认后撰写完整文档并落盘。

**第三段：CLI 收尾+AI 后处理**
```
wpw done <阶段> -c <需求>   # 标记阶段完成
# 或 wpw skip / wpw decision
```
- 执行阶段 hook（如调用 humanizer-zh 去机器腔）；
- 更新状态文件和进度信息。

关键防线：所有状态变更必须经过 CLI，AI 不直接修改 `.wpw.yaml`；落盘前必须输出大纲让用户确认。

**（5）Plan-as-tracker 任务追踪机制**

利用 Markdown checkbox 的人读友好 + 程序可解析特性，将 Plan 文档作为任务数据库：

- 任务解析：正则匹配 `- [ ] 任务编号. 任务描述` 格式的 checkbox；
- 状态枚举：` `（待开始）、`🔄`（进行中）、`x`（已完成）、`~`（已取消）；
- 原地更新：`wpw task --mark <id> --state <state>` 命令通过正则精准替换对应任务的标记，保留文档其余内容不变；
- 进度聚合：计算已完成任务数 / 总任务数，反规范化缓存到 `.wpw.yaml.progress`；
- 断点续作：`--from <taskId>` 参数支持从指定任务继续实施。

#### 3.2.3 实验方案

**实验一：业务-代码映射质量评估**

- **实验目的**：验证多源证据融合相比单源的映射质量提升。
- **实验设计**：在测试项目上人工标注部分业务能力对应的代码元素作为基准；分别测试 doc-extract、name-match、semantic、git-history 单源的准确率和召回率；测试 2 源、3 源、4 源融合后的 P/R/F1 值，绘制消融实验曲线。
- **评估指标**：准确率（Precision）、召回率（Recall）、F1 值。
- **预期结果**：多源融合的 F1 值显著优于任一单源，4 源融合达到最佳综合性能。

**实验二：锚点选择算法对比**

- **实验目的**：验证置信度衰减加权锚点选择算法相比普通 Top-K 检索的效果提升。
- **实验设计**：设计若干典型查询场景（精确业务查询、模糊技术查询、混合查询）；对比普通 Top-K 检索和置信度衰减加权检索的锚点准确率；对比两种锚点选择方式下最终上下文的相关性评分（人工评估或基于已知答案的自动评估）。
- **评估指标**：锚点准确率、最终子图相关性得分、Token 效率（相关信息 Token / 总 Token）。
- **预期结果**：置信度衰减算法在高 C 置信查询场景下显著减少子图膨胀，提升信息密度。

**实验三：上下文压缩效果评估**

- **实验目的**：评估不同压缩等级下的 Token 压缩率和信息保留率。
- **实验设计**：选择若干模块作为测试用例；分别生成 loose/standard/extreme 三档压缩的上下文；测量压缩后的 Token 数与原始完整文本 Token 数的比值（压缩率）；通过信息保留任务（如代码问答、关系推理）评估不同压缩等级下的信息保留程度。
- **评估指标**：Token 压缩率、信息保留率、任务准确率。
- **预期结果**：extreme 压缩可达到 5-10 倍压缩率，且在结构关系理解任务上保留大部分信息。

**实验四：Token 预算迭代控制有效性**

- **实验目的**：验证 Token 预算迭代控制算法能否在给定预算内输出高质量上下文。
- **实验设计**：设置不同的 Token 预算（500/1000/2000/4000/8000）；测试迭代算法生成的上下文是否满足预算约束；对比迭代生成结果与直接使用对应压缩等级的结果质量差异。
- **评估指标**：预算满足率、平均迭代次数、最终压缩率、锚点保留率。
- **预期结果**：算法能在 8 次迭代内满足预算，且在相同预算下比单一步长调整获得更高质量的上下文。

**实验五：工作流系统实用性评估**

- **实验目的**：评估 wpw 工作流系统对实际开发效率的提升效果。
- **实验设计**：选择 2-3 个典型 Web 开发任务（如用户登录模块、购物车模块、订单管理模块）；邀请若干开发者分别在使用 wpw 和不使用 wpw 的条件下完成任务；记录任务完成时间、文档产出质量、代码缺陷数等指标；通过问卷收集用户体验反馈。
- **评估指标**：任务完成时间、文档完整度、代码 review 通过率、用户满意度。
- **预期结果**：使用 wpw 后文档产出效率显著提升，开发流程更规范，整体开发效率提升 20-40%。

#### 3.2.4 可行性分析

**技术可行性**：
- 大语言模型和 AI 编程工具已在工业界广泛应用，技术成熟度高；
- Tree-sitter、transformers.js 等开源库提供了坚实的技术基础；
- 知识图谱、语义检索、子图裁剪等技术已有大量研究基础，可借鉴成熟方法；
- 开发团队具备 TypeScript 全栈开发和 AI 应用开发经验。

**经济可行性**：
- 本系统为纯软件产品，开发成本主要为人力投入；
- 基于开源技术栈构建，无授权费用；
- 本地 Embedding 模型运行，无需持续支付 API 费用；
- 系统轻量，对硬件要求低，普通开发机即可运行。

**操作可行性**：
- CLI 工具安装简单（`npm install -g wpw`），使用门槛低；
- 零配置运行，开箱即用，图谱自动构建；
- 与现有开发工作流（Git、IDE、CI/CD）无缝集成；
- 三级降级机制确保图谱不可用时仍可正常开发。

**综上所述，本课题在技术、经济和操作层面均具备可行性。**

### 3.3 时间安排

| 阶段 | 时间 | 主要工作内容 | 预期产出 |
|------|------|-------------|---------|
| 第一阶段 | 第 1-2 周 | 文献调研与需求分析 | 文献综述、需求分析报告 |
| 第二阶段 | 第 3-4 周 | 系统架构设计与核心算法设计 | 架构设计文档、算法设计文档 |
| 第三阶段 | 第 5-8 周 | CLI 层核心功能实现 | 工作流引擎、状态管理、模板系统 |
| 第四阶段 | 第 9-12 周 | 知识图谱构建模块实现 | 多语言解析器、图谱构建器、向量索引 |
| 第五阶段 | 第 13-16 周 | 上下文生成 Pipeline 实现 | 语义检索、子图裁剪、骨架压缩、序列化 |
| 第六阶段 | 第 17-18 周 | AI 层 Skill 实现与集成 | 13 个 /wpw:xxx 命令、三段式契约 |
| 第七阶段 | 第 19-20 周 | 系统测试与实验验证 | 测试报告、实验数据、性能评估 |
| 第八阶段 | 第 21-22 周 | 论文撰写与修改 | 毕业论文初稿、修改稿 |
| 第九阶段 | 第 23-24 周 | 论文定稿与答辩准备 | 毕业论文终稿、答辩 PPT |

**关键里程碑**：
- 第 4 周末：完成架构设计和算法设计，通过中期检查；
- 第 12 周末：知识图谱构建与查询模块基本完成；
- 第 18 周末：系统集成完成，进入测试和论文撰写阶段；
- 第 22 周末：完成论文初稿；
- 第 24 周末：论文定稿并准备答辩。

---

## 参考文献

[1] Chen M, Tworek J, Jun H, et al. Evaluating Large Language Models Trained on Code[J]. arXiv preprint arXiv:2107.03374, 2021.

[2] Cognition AI. Devin: The AI Software Engineer[EB/OL]. https://www.cognition-labs.com/, 2024.

[3] Yang J, Qian Y, Yang J, et al. SWE-agent: Agent-Computer Interfaces Enable Automated Software Engineering[J]. arXiv preprint arXiv:2405.15793, 2024.

[4] Atzeni M, Atzori M, Mazza M, et al. CodeGraph: A Tool for Graph-based Code Representation and Analysis[C]//Proceedings of the 15th International Conference on Software Technologies. 2020: 420-427.

[5] Guo D, Ren S, Lu S, et al. GraphCodeBERT: Pre-training Code Representations with Data Flow[C]//International Conference on Learning Representations. 2020.

[6] Liu N F, Lin K, Hewitt J, et al. Lost in the Middle: How Language Models Use Long Contexts[J]. Transactions of the Association for Computational Linguistics, 2024, 12: 1113-1131.

[7] GitHub. GitHub Copilot Workspace[EB/OL]. https://github.blog/news-insights/product-news/introducing-github-copilot-workspace/, 2024.

[8] Zheng X, Xia C, Zou Y, et al. CodeGeeX: A Pre-Trained Model for Code Generation with Multilingual Evaluations on HumanEval-X[J]. arXiv preprint arXiv:2303.17568, 2023.

[9] 复旦大学. CodeKG: Code Knowledge Graph 项目[EB/OL]. https://gitee.com/fudan-se/code-kg, 2022.

[10] Vaswani A, Shazeer N, Parmar N, et al. Attention Is All You Need[C]//Advances in Neural Information Processing Systems. 2017: 5998-6008.

[11] Devlin J, Chang M W, Lee K, et al. BERT: Pre-training of Deep Bidirectional Transformers for Language Understanding[C]//Proceedings of the 2019 Conference of the North American Chapter of the Association for Computational Linguistics. 2019: 4171-4186.

[12] Lewis P, Perez E, Piktus A, et al. Retrieval-Augmented Generation for Knowledge-Intensive NLP Tasks[C]//Advances in Neural Information Processing Systems. 2020: 9459-9474.

[13] 中国计算机学会. 人工智能软件工程技术白皮书[R]. 2023.

[14] Xia C, Zhang L, Chen Z, et al. Retrieval-Augmented Generation for Code: A Survey[J]. arXiv preprint arXiv:2401.17108, 2024.

[15] Touvron H, Lavril T, Izacard G, et al. LLaMA: Open and Efficient Foundation Language Models[J]. arXiv preprint arXiv:2302.13971, 2023.
