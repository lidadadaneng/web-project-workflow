# 毕业论文核心创新点

> 课题：面向 Web 项目开发的 AI 驱动六阶段工作流及其知识图谱上下文压缩方法
>
> 本文整理课题中最核心、最能体现工作量的创新点。每点标注**实现状态**（✅ 完整实现 / ⚠️ 部分实现），确保论文 claims 与代码实际一致，避免答辩翻车。

---

## 0. 课题概述

本课题提出 **wpw（Web Project Workflow）**——一套 AI 驱动的六阶段 Web 项目开发工作流，及其配套的**面向 LLM 上下文的四层知识图谱与压缩方法**。

核心矛盾：LLM 编码能力强但存在两点不足——① 非确定性，直接驱动开发流程会导致状态/依赖不可控；② 上下文窗口受限，理解大型代码库时手动 grep/读文件既不全又耗 Token。

本课题的两条技术主线：
- **流程主线**：三层分离架构 + 六阶段强依赖状态机，把确定性逻辑交 CLI、理解生成交 AI，约束 LLM 非确定性。
- **上下文主线**：四层知识图谱 + 端到端上下文 Pipeline（检索→裁剪→骨架抽取→序列化），在 Token 预算内输出高密度结构化上下文替代手动读文件。

```
┌─────────────────────────────────────────────────────────────┐
│  AI 层（Skill + /wpw:xxx 命令）  理解/生成/交互              │
└───────────────┬─────────────────────────────────────────────┘
                │ wpw <command> --json
┌───────────────▼─────────────────────────────────────────────┐
│  CLI 层（wpw 命令）  状态/依赖/路径/模板  +  图谱子系统       │
└───────────────┬─────────────────────────────────────────────┘
                │
┌───────────────▼─────────────────────────────────────────────┐
│  文件系统  wpw/active/{需求}/ + .wpw.yaml + knowledge/graph/ │
└─────────────────────────────────────────────────────────────┘
```

六阶段：`BRD → PRD → Explore(可选) → Design → Plan → Test(可选) → Apply`（业务→产品→探索→设计→计划→验证→实施）。

---

## 创新点一：三层分离的 AI 协同开发架构与三段式编排契约  ✅

**问题**：LLM 输出具有非确定性，若由 AI 直接掌管开发流程的状态、依赖、路径，会出现状态错乱、阶段乱序、文件路径漂移等不可控问题。

**方法**：将系统严格分为三层——
- **AI 层**：负责理解、生成、用户交互，调用 CLI 完成确定性操作；
- **CLI 层**：承载状态管理、依赖检查、路径解析、模板加载等所有确定性逻辑；
- **文件系统层**：工作区（`wpw/active/{需求}/` + `.wpw.yaml`）。

**三段式契约**（每个 `/wpw:xxx` 阶段命令统一遵循）：
1. **CLI 准备**：`wpw new`（幂等）→ `wpw check`（依赖门禁）→ `wpw template`（取模板）；
2. **AI 生成+交互**：生成大纲 → `AskUserQuestion` 确认 → 撰写文档落盘；
3. **CLI 收尾+AI 后处理**：`wpw done`/`decision`/`skip` → 执行 hook + `@humanizer-zh` 去机器腔。

**关键防线**：
- **状态变更必经 CLI**——所有状态写操作收敛到 `done`/`skip`/`decision`/`task` 四个入口，AI 不直接改 `.wpw.yaml`，使 AI 行为可审计（每次状态变更即一次 CLI 调用）；
- **落盘前必出大纲确认**——禁止静默写文件；
- **BRD 角色隔离**——BRD 面向业务方，不讨论技术文件/模块/框架，AI 独立估算逐步人力/时间成本填入成本评估表。

**技术体现**：13 个 `/wpw:xxx` 命令统一遵循契约；`wpw init` 释放 AI 层资产到 `.claude/`，桥接三层；`check` 在依赖不满足时以退出码 1 程序化阻断 AI。

**创新性**：架构模式（中等）。"确定性骨架 + AI 血肉"在工业界有先例（Claude Code、Cursor 等），本课题的贡献在于**执行的严格性与一致性**——把"AI 不碰状态"从口号落到 13 个命令的统一契约与可审计的 CLI 入口。

**实现状态**：✅ 架构与三段式契约完整实现。
> ⚠️ 边界：大纲确认与下文双门禁目前为 **AI 提示层约束**，状态机中的 `outlining`/`confirmed` 状态未启用；论文应表述为"提示层防线 + CLI 状态收敛"，勿声称状态机强约束。

---

## 创新点二：面向 LLM 上下文的四层知识图谱与端到端上下文 Pipeline  ✅

**问题**：LLM 编码时理解大型代码库依赖手动 grep/读文件，上下文不全且 Token 消耗大。

**方法**：构建四层知识图谱，并设计端到端 Pipeline 输出可直接喂 LLM 的压缩上下文。

**四层图谱模型**：
```
L1 业务需求  →  L2 业务模块  →  L3 文件  →  L4 代码元素
(需求文档)      (模块/目录)     (源文件)    (函数/类/接口/组件/常量)
```

**端到端上下文 Pipeline**（`wpw graph context`）：
```
自然语言查询
   │
   ▼
语义检索（找锚点）  ── 向量余弦相似度，多查询合并、阈值/层级过滤
   │
   ▼
子图裁剪          ── 加权双向 BFS（见创新点三）
   │
   ▼
骨架抽取          ── 距离感知分级（见创新点四）
   │
   ▼
层级符号化序列化    ── 三档压缩 + 符号化输出（见创新点四）
   │
   ▼
Token 预算迭代     ── 5 级降级保证不超窗口（见创新点五）
   │
   ▼
结构化上下文文本（直接喂 LLM）
```

**工程特性**：
- 纯 **JSONL 图谱 + 自定义二进制向量索引**（32 字节头 + magic/version + 零拷贝 `Float32Array` 视图），**零数据库依赖**，纯本地运行；
- 本地 embedding（`all-MiniLM-L6-v2`，384 维），配套 **ModelScope 镜像**解决国内网络拉取；
- 确定性内容哈希节点 ID（SHA-256 前 12 hex / 48 位），保证幂等重建与多用户一致；
- **统一三级降级**：图谱缺失→提示并回退手动读文件（不硬阻断）；向量缺失→`--anchors` 模式；0 锚点→回退手动。

**创新性**：中高。多粒度代码图谱 + 端到端压缩 Pipeline 的组合，且全本地零依赖，适配 LLM 编码场景。

**实现状态**：✅ 完整实现。端到端 12 阶段 `__verify__` 验证脚本全通过。

> ⚠️ 边界：语义检索为**暴力线性扫描** O(N·D)（无 ANN 索引），适用于万节点级图谱；论文应说明规模边界，勿声称大规模高效检索。

---

## 创新点三：加权双向 BFS 子图裁剪与语义×结构复合打分  ✅

**问题**：从锚点扩展子图时，如何在节点上限与 Token 预算约束下，保留最相关的节点、裁掉弱相关节点。

**方法**（`SubgraphTrimmer`）：

1. **多锚点加权双向 BFS**：每个锚点同时沿出边（下游）与入边（上游）扩展，仅遍历权重 ≥ 阈值的边；多锚点结果合并取最小距离；
2. **结构重要度**：`入边权重×1.0 + 出边权重×0.5`，归一化到 [0,1]——**入度加权更高**（被越多节点依赖 → 越核心 → 越不能裁）；
3. **综合得分**：
   ```
   score = (semanticScore × 0.6 + structuralScore × 0.4) × distFactor
   distFactor = max(0.7, 1 − dist × 0.05)   // 距离衰减，防远节点抢分
   ```
   锚点强制 1.0（必留）；无语义分节点按 0.5 兜底；
4. **节点上限裁剪**：锚点必留 + 其余按综合得分降序取前 N，裁剪后重新收集两端都在的边。

**创新性**：中高。双向 BFS + 入度加权结构重要度 + 语义结构复合打分 + 距离衰减的组合，是一个有方法依据的子图相关性排序方案（而非纯启发式）。

**实现状态**：✅ 完整实现。

---

## 创新点四：距离感知骨架压缩与三档符号化序列化  ✅

**问题**：子图序列化时，如何在 Token 约束下最大化信息密度——锚点附近该详细，远处该精简。

**方法**：

**距离感知骨架分级**（`distanceToSkeletonLevel`）——节点到锚点距离决定输出粒度：
| 距离 | 等级 | 输出内容 |
|------|------|---------|
| 0（锚点）| full | 名称 + 签名 + 入参出参类型 + JSDoc + 注解 + 父类 |
| 1 | standard | 名称 + 签名 + 类型 |
| ≥2 | minimal | 仅名称 + 类型标签 |

**三档压缩等级**（`loose`/`standard`/`extreme`）：在距离分级基础上整体提/降一档；`extreme` 模式锚点也只给 standard、依赖边仅保留 business_map 与高权重边。

**符号化层级序列化**：按 L1→L2→L3→L4 包含关系构建树状缩进输出，用标准化符号表达关系：
- `⊃` 包含关系（contain）
- `->` 调用/导入/继承等依赖关系
- `⇄` 业务映射关系（business_map）

输出末尾附带 Token 统计与压缩率。

**创新性**：中。"距离越近越详细"是直观且有效的信息密度启发式，符号化输出对 LLM 友好。

**实现状态**：✅ 完整实现。

---

## 创新点五：Token 预算迭代上下文生成与多级降级  ✅

**问题**：保证生成上下文不超 LLM 窗口，且尽可能保留核心信息。

**方法**（`ContextPipeline.generateWithBudget`）：

**Token 估算公式**（`estimateTokens`）：
```
tokens = 中文数/1.5 + 英文词数×0.75 + 其他字符数/4
```

**5 级迭代降级**（由宽松到严格，最多 8 次迭代）：
```
超预算?
  ├─ 1. 降压缩档：loose → standard → extreme
  ├─ 2. 节点上限减半（下限 10）
  ├─ 3. 扩展深度 −1
  ├─ 4. 提升最小权重阈值 +0.1（上限 0.9）
  └─ 5. 最终降级：仅锚点 + extreme 压缩（仍超则加预算不足警告）
```

每级调整后重新生成并估算 Token，满足预算即返回；锚点始终保留。

**创新性**：中。工程性强的自适应预算控制，多级降级策略实用。

**实现状态**：✅ 完整实现。
> ⚠️ 边界："Token 降 60-80%"为设计预期值，**需实验补强**——论文应以实测压缩率/Token 对比实验支撑，勿直接引用未经实验的数字。

---

## 创新点六：多源证据融合的业务-代码映射（noisy-OR 聚合）  ✅ 4源已验证 + AI校准未来工作

**问题**：如何自动建立"业务需求"到"代码元素"的跨层关联（`business_map` 边），无需手动标注。

**方法**：设计多源证据融合框架——每条业务-代码关联由多个独立证据源支持，证据加权聚合。

**证据源**（已实现 4 源 + AI校准为未来工作）：
| 源 | EdgeSource 排名 | 实现状态 |
|----|----------------|---------|
| 文档提取（从 PRD/Design 正则抽取模块名） | doc-extract (8) | ✅ 接入 buildGraph，baseWeight 0.85 |
| 命名匹配（完整 30+ 中英词典 + 前缀/包含匹配） | name-match (2) | ✅ 接入，权重上限 0.7 |
| 语义匹配（向量相似度，仅 L2/L3） | semantic (4) | ✅ 接入，baseWeight = min(0.7, sim×0.6) |
| Git 历史追溯（commit message 匹配文件频次） | git-history (5) | ✅ 接入，baseWeight = min(0.7, normFreq×0.6) |
| AI 校准（可选） | ai-refine (7) | ⚠️ 配置桩，未实现（未来工作） |

**noisy-OR 证据聚合**（`aggregateWeights`）：
```
finalWeight = 1 − ∏(1 − wᵢ)     // 多个独立弱信号复合增强，上限 0.95
```
并取最高排名源作为边的溯源标签（`EdgeSource`），实现**边的可追溯性**——每条边都知道"为什么存在"。

**创新性**：较高。noisy-OR 是有概率论依据的证据融合方法（独立证据复合），`EdgeSource` 溯源排名是可追溯性设计。这是本课题方法论上最有依据的一个设计决策。

**实现状态**：✅ **4 源已验证（doc / semantic / git / name），AI 校准为未来工作**。
- `buildGraph` 经重构后接通 4 源：向量构建后新增"语义回填"阶段，Git 历史经 `traceFromGit` 接入，命名匹配升级为完整 30+ 中英词典；
- 多源证据经 `aggregateWeights` 的 noisy-OR 一次性聚合，边 `source` 取最权威源（与证据收集顺序无关）；
- 已修复 `aggregateWeights` 此前"算了 bestSource 却丢弃"的隐藏 bug，溯源名副其实；
- `boostEdge` 定位为增量场景预留接口（updateGraph 追加证据），非死代码；
- AI 校准（ai-refine）为配置桩，留作未来工作；
- 配套消融实验脚手架 `__mapping_eval__.ts`：2源/3源/4源 P/R/F1 对比（合成样例 3源 F1=1.0、4源 R=1.0/P=0.8，印证多源召回提升与精度权衡）。

> ⚠️ 另一相关边界：图谱设计了 5 种边类型（contain/import/call/inherit/business_map），但 `call`、`inherit` 边**从未生成**（解析器已抽到类继承信息但未建边，函数调用未追踪）。实际图谱仅含 contain/import/business_map 三种边。论文应明确边类型范围，勿声称调用/继承依赖分析。

---

## 创新点七：Plan-as-tracker 任务驱动实施与双门禁交付  ✅（门禁为提示层）

**问题**：开发计划与实施进度脱节，计划文档写完即弃，实施阶段缺少结构化任务追踪与交付门禁。

**方法**：

**Plan-as-tracker（单源真实）**：Plan 文件的 Markdown checkbox 即任务数据库——
```
- [ ] 1. 搭建登录页面骨架
- [🔄] 2. 对接认证接口          // 进行中
- [x] 3. 表单校验               // 已完成
```
- `apply` 命令正则解析 checkbox 返回 `{contextFiles, tasks, progress, warnings}`；
- `task --mark <id> --state <state>` **原地正则改写** Plan 文件（仅改对应任务的 mark，保留前后缀），并同步 `.wpw.yaml.progress` 聚合进度（反规范化缓存）；
- `--from <task>` 断点续作。

**双门禁交付**（`apply` 阶段）：
1. **CR 门禁**：`/wpw:cr`（`@code-reviewer`）全量/快速复审，BLOCKER 必须全部修复方可合并；
2. **测试门禁**：测试方案通过。
"二者均合格"方可交付。

**每任务上下文注入**：apply 每个任务前调用 `wpw graph context` 取代码上下文，替代手动读文件。

**创新性**：中。Plan-as-tracker 是优雅的低仪式设计——单源真实、人读友好、git 可 diff、无需独立任务存储。

**实现状态**：✅ 任务追踪完整实现。
> ⚠️ 边界：双门禁为 **AI 提示层约束**，`archive` 命令不强制校验 CR/测试/任务完成；论文应表述为"提示层门禁约定"，勿声称 CLI 强制门禁。

---

## 工作量量化

| 维度 | 数量 |
|------|------|
| TypeScript 代码 | ~9,900 行 / 54 文件 |
| ├─ 图谱子系统 | ~7,300 行 / 25 文件 |
| └─ 工作流引擎 | ~2,300 行（schema+lib+commands+templates+docs） |
| 形式化能力规格（OpenSpec） | 6 个（graph-build / graph-context / graph-query / prompt-compression / semantic-search / subgraph-trimming） |
| AI 层命令文档 | 14 个（`/wpw:brd/prd/explore/design/plan/test/apply/cr/map/exp/sync/archive/init/skills`） |
| Skill | 1 主 Skill（wpw-workflow）+ 3 联动 Skill（brainstorming / code-reviewer / humanizer-zh） |
| 结构化文档模板 | 9 个（BRD/PRD/Explore/Design-Fe/Design-Server/Plan-Fe/Plan-Server/Test-Fe/Test-Server） |
| 存储格式 | 3 种（JSONL 图谱 / 二进制向量索引 / JSON 映射） |
| 端到端验证 | 12 阶段 `__verify__` 脚本 |
| 单测/集成测 | 7 个测试文件（dependency / state / integration / edge-builder / mapping-sources / business-mapper / mapping-eval） |

---

## 实现边界与诚实声明（答辩防线）

以下为**设计宣称与代码实际的差距**，论文写作时务必按此范围表述，避免超Claim：

1. **业务-代码映射 4 源已接通**：doc / semantic / git / name 四源经 noisy-OR 聚合已实现（change `business-map-multi-source`，含语义回填 + Git 接入 + bestSource 溯源修复 + 消融脚手架）；仅 AI 校准（ai-refine）为配置桩，留作未来工作。
2. **5 种边类型实为 3 种**：`call`、`inherit` 边从未生成，实际仅 contain/import/business_map。
3. **大纲确认 / 双门禁为提示层**：状态机 `outlining`/`confirmed` 未启用；`archive` 不强制门禁。
4. **语义检索为暴力扫描**：O(N·D)，无 ANN 索引，适用于万节点级。
5. **增量更新简化**：`updateGraph` 仍全量重建向量；`detectRequirementChanges` 为桩。
6. **`getShortestPath` 深度限制未实际生效**；`QueryOptions.module` 过滤声明未实现。
7. **"Token 降 60-80%"为设计预期**：需实验实测支撑，勿直接引用。
8. **Plan-Fe / Plan-Server 模板除标题外完全相同**。

> 原则：**论文的每个技术 claim 都应能在代码中指到对应实现；已设计未实现的归入"未来工作"，不作为已贡献。**
