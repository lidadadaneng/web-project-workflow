# 面向 Web 项目开发的 AI 驱动六阶段工作流及其知识图谱上下文压缩方法

---

## 摘  要

大语言模型的快速发展使 AI 辅助编程从代码补全扩展到更广泛的开发场景，但现有 AI 开发工具普遍存在三方面不足：一是流程覆盖面窄，主要聚焦编码环节，对需求分析、设计、计划、测试等上游环节支持薄弱；二是可控性不足，LLM 的非确定性与工程流程的确定性要求存在矛盾，直接由 AI 驱动流程易导致状态混乱、阶段跳跃、路径漂移；三是上下文效率低，面对大型代码库时手动读文件既不全面又消耗大量 Token，长上下文还存在"Lost in the Middle"现象。

针对上述问题，本文提出并实现了一套 AI 驱动的六阶段 Web 项目开发工作流系统 wpw（Web Project Workflow），及其配套的面向 LLM 上下文的能力-结构双层知识图谱与压缩方法。主要工作包括：（1）提出三层分离的 AI 协同开发架构，将确定性逻辑（状态/依赖/路径/模板）与非确定性逻辑（理解/生成/交互）严格分层，13 个阶段命令统一遵循"CLI 准备→AI 生成+交互→CLI 收尾"的三段式编排契约；（2）构建 BRD→PRD→Explore→Design→Plan→Test→Apply 的六阶段标准化开发工作流引擎，配套强依赖检查、Plan-as-tracker 任务追踪与归档驱动的能力沉淀机制；（3）提出 C（能力层）+ L1/L2/L3（结构层）的能力-结构双层知识图谱模型，并设计基于 noisy-OR 的多源证据融合业务-代码映射方法（文档提取、命名匹配、语义匹配、Git 历史追溯四源），自动建立业务能力到代码元素的跨层关联；（4）研究面向 LLM 上下文的知识图谱压缩方法，包括置信度衰减加权锚点选择算法、加权双向 BFS 子图裁剪与语义-结构复合打分、距离感知骨架分级与层级符号化序列化、Token 预算迭代控制与多级降级，形成"检索→裁剪→骨架抽取→序列化"的端到端上下文生成 Pipeline。

实验结果表明：多源证据融合的 F1 值显著优于任一单源；置信度衰减锚点选择算法在高 C 置信查询场景下有效抑制模块层子图膨胀；extreme 压缩可达到 5–10 倍 Token 压缩率且保留大部分结构关系信息；Token 预算迭代控制算法在 8 次迭代内满足预算约束；工作流系统使文档产出效率显著提升、开发流程更规范，整体开发效率提升约 20%–40%。本系统已在实际项目中部署运行，验证了所提方法的有效性与工程实用性。

**关键词**：AI 辅助开发；工作流引擎；知识图谱；上下文压缩；多源证据融合；noisy-OR 模型；大语言模型

---

## ABSTRACT

The rapid development of large language models (LLMs) has extended AI-assisted programming from code completion to broader development scenarios. However, existing AI development tools generally suffer from three shortcomings: first, narrow process coverage, mainly focusing on the coding phase while providing weak support for upstream activities such as requirements analysis, design, planning, and testing; second, insufficient controllability, where the non-determinism of LLMs conflicts with the determinism required by engineering workflows, and directly letting AI drive the process tends to cause state chaos, phase skipping, and path drift; third, low context efficiency, where manually reading files for large codebases is both incomplete and Token-consuming, and long contexts still suffer from the "Lost in the Middle" phenomenon.

To address these problems, this thesis proposes and implements an AI-driven six-stage Web project development workflow system, wpw (Web Project Workflow), together with a capability-structure dual-layer knowledge graph and compression method for LLM context. The main contributions are: (1) a three-layer separated AI collaborative development architecture that strictly separates deterministic logic (state/dependency/path/template) from non-deterministic logic (understanding/generation/interaction), with 13 phase commands uniformly following a three-stage orchestration contract of "CLI preparation → AI generation + interaction → CLI wrap-up"; (2) a six-stage standardized development workflow engine (BRD→PRD→Explore→Design→Plan→Test→Apply) with strong dependency checking, Plan-as-tracker task tracking, and archive-driven capability accumulation mechanisms; (3) a capability-structure dual-layer knowledge graph model (C capability layer + L1/L2/L3 structure layer), and a noisy-OR-based multi-source evidence fusion method for business-code mapping (four sources: document extraction, name matching, semantic matching, and Git history tracing) that automatically establishes cross-layer associations between business capabilities and code elements; (4) a knowledge graph compression method for LLM context, including a confidence-decay weighted anchor selection algorithm, weighted bidirectional BFS subgraph pruning with semantic-structural composite scoring, distance-aware skeleton grading with hierarchical symbolic serialization, and Token-budget iterative control with multi-level degradation, forming an end-to-end context generation pipeline of "retrieval → pruning → skeleton extraction → serialization".

Experimental results show that: the F1 of multi-source evidence fusion is significantly better than any single source; the confidence-decay anchor selection algorithm effectively suppresses module-layer subgraph expansion under high-C-confidence queries; extreme compression achieves a 5–10× Token compression ratio while preserving most structural relationship information; the Token-budget iterative control algorithm satisfies the budget constraint within 8 iterations; and the workflow system significantly improves document production efficiency and makes the development process more standardized, with an overall development efficiency improvement of about 20%–40%. The system has been deployed and run in real projects, verifying the effectiveness and engineering practicality of the proposed methods.

**Key words**: AI-assisted development; workflow engine; knowledge graph; context compression; multi-source evidence fusion; noisy-OR model; large language model

---

## 序

软件工程的历史，本质上是人类不断试图驯服复杂性的历史。从瀑布模型到敏捷开发，从手工编码到低代码平台，每一次方法论的演进都在试图回答同一个问题：如何在有限的人力与时间内，可靠地交付高质量的软件。

大语言模型的出现，为这一古老问题注入了新的变量。当机器第一次具备了对自然语言需求的理解能力、对设计方案的推理能力、对代码的生成能力时，"AI 辅助开发"便从科幻走入了日常工程实践。然而，工具的进步并不自动等同于方法的进步。当我们把 LLM 嵌入开发流程时，一个根本性的张力立刻浮现：LLM 是非确定性的、富有创造力的"血肉"，而工程流程是确定性的、要求可追溯的"骨架"。如果让 AI 直接驾驭流程，状态混乱与路径漂移几乎不可避免；如果只把 AI 当作代码补全的边角工具，又白白浪费了它覆盖全流程的潜力。

本文的工作，正是在这一张力中寻找平衡。我们提出"三层分离"的架构思想——让确定性逻辑由代码承载、非确定性任务交由 AI、二者通过标准化契约协作，把"AI 不碰状态"从口号落到每一个命令的统一编排中。在此基础上，我们进一步追问：当 AI 真正介入需求、设计、计划等上游环节时，它如何高效地"读懂"一个已有的代码库？为此，我们构建了能力-结构双层知识图谱，并研究了一套面向 LLM 上下文的压缩方法，力图以最少的 Token 提供最高密度的结构化代码信息。

这项工作既是一次工程实践，也是一次方法论的探索。我们希望，wpw 不仅是一个可用的工具，更能为"AI 原生开发"这一新兴范式提供一份来自工程一线的注脚。

---

## 插图和附表清单

**插图清单**

- 图 1-1 AI 辅助开发工具的流程覆盖现状
- 图 3-1 三层分离的 AI 协同开发架构
- 图 3-2 六阶段开发工作流与依赖关系
- 图 3-3 能力-结构双层知识图谱模型（C + L1/L2/L3）
- 图 3-4 三段式编排契约执行流程
- 图 4-1 系统总体模块结构
- 图 4-2 多语言源码解析器两级策略
- 图 4-3 多源证据融合与 noisy-OR 聚合流程
- 图 4-4 上下文生成 Pipeline 端到端流程
- 图 4-5 加权双向 BFS 子图扩展示意
- 图 4-6 距离感知骨架分级与层级符号化序列化示例
- 图 4-7 Token 预算五级迭代降级策略
- 图 5-1 多源融合消融实验 F1 曲线
- 图 5-2 不同压缩等级下的 Token 压缩率与信息保留率
- 图 5-3 Token 预算迭代次数分布

**附表清单**

- 表 3-1 八项关键技术分布
- 表 3-2 六阶段工作流阶段定义
- 表 4-1 图谱节点与边类型
- 表 4-2 多源证据源权重与权威性排名
- 表 4-3 三档压缩等级对照
- 表 5-1 业务-代码映射质量消融实验结果
- 表 5-2 锚点选择算法对比实验结果
- 表 5-3 上下文压缩效果评估结果
- 表 5-4 Token 预算迭代控制有效性结果
- 表 5-5 工作流系统实用性评估结果

---

## 符号清单

| 符号 | 含义 |
|------|------|
| $C$ | 能力层（Capability Layer）节点 |
| $L_1, L_2, L_3$ | 结构层节点：模块层、文件层、元素层 |
| $\mathrm{Conf}_C$ | C 层最高语义置信度 |
| $\alpha$ | 置信度衰减系数（默认 3.0） |
| $w_{L_1}$ | L1 层衰减权重，$w_{L_1}=\exp(-\alpha\cdot\mathrm{Conf}_C)$ |
| $w_i$ | 第 $i$ 个证据源的基础权重 |
| $W$ | noisy-OR 聚合权重，$W=1-\prod_i(1-w_i)$ |
| $\mathrm{score}_{\mathrm{sem}}$ | 语义相似度得分 |
| $\mathrm{score}_{\mathrm{str}}$ | 结构重要度得分 |
| $\mathrm{distFactor}$ | 距离衰减因子，$\max(0.7,\,1-\mathrm{dist}\times0.05)$ |
| $T$ | Token 估算值 |

---

## 1 引言（绪论）

### 1.1 研究背景

近年来，以 GPT、Claude、Llama 为代表的大语言模型（Large Language Model, LLM）在代码理解与生成方面取得了突破性进展，AI 辅助编程工具（如 GitHub Copilot、Cursor、CodeGeeX、通义灵码等）已广泛应用于日常开发，显著提升了编码效率[1][8]。然而，当 AI 的能力边界从"代码补全"向"开发全流程"延伸时，三类深层次问题逐渐显现。

**第一，AI 辅助停留在编码层面，缺乏全流程覆盖。** 现有工具主要在代码编辑时提供补全和建议，而对于需求分析、技术方案设计、开发计划制定、测试方案设计等上游环节的支持薄弱。一个完整的需求从提出到落地，需求-设计-计划-测试等环节仍高度依赖人工完成，AI 的参与度极低。这使得 AI 的价值被局限在流水线的最末段，未能释放其在理解、推理、规划方面的更大潜力。

**第二，AI 的非确定性与工程流程的确定性要求存在矛盾。** LLM 本质上是一个概率生成模型，其输出具有非确定性；而软件工程流程要求状态可追溯、阶段有序推进、产物可审计。直接让 AI 驱动开发流程会导致状态混乱、阶段跳跃、文件路径不一致、产物格式漂移等问题，难以保证工程质量和可追溯性。如何在充分利用 AI 能力的同时保持流程的可控性，是一个亟待解决的架构问题。

**第三，大型项目的代码上下文获取效率低下。** 当 AI 需要理解已有代码进行开发或修改时，开发者通常通过手动 grep、翻目录、读文件的方式提供上下文，既不全面又耗费大量 Token。即使大模型的上下文窗口已扩展至 200K 甚至更长，Liu 等人的研究[6]表明，模型在长上下文中存在显著的"Lost in the Middle"现象——位于上下文中间位置的信息利用率明显下降。因此，单纯堆叠上下文窗口并不能解决问题，如何高效检索和压缩相关代码上下文，是 LLM 应用于大型项目开发的核心瓶颈。

与此同时，规格驱动开发（Spec-Driven Development）在 AI 时代焕发新生。以 OpenSpec 为代表的轻量级规格范式，将规格从"形式化证明的数学对象"转变为"AI 可理解、人类可读的结构化文档"，通过 Purpose/Requirements/Scenarios 的三段式结构描述系统能力，使规格既是文档也是 AI 的确定性约束。Agent Harness（智能体工程框架）领域也逐渐形成"确定性骨架 + AI 血肉"的设计共识，LangGraph、CrewAI、Claude Code Skill 等框架提供了通用编排能力。然而，这些工作要么停留在规格管理层面，要么是缺乏领域知识的通用框架，在 Web 开发这一具体领域，缺少经过工程验证的端到端工作流方案。

基于上述背景，本文提出构建 wpw（Web Project Workflow）——一套 AI 驱动的六阶段 Web 项目开发工作流系统，及其配套的面向 LLM 上下文的能力-结构双层知识图谱与压缩方法，旨在通过系统化的工程方法和知识图谱技术，提升 AI 辅助 Web 开发的全流程效率与可控性。

### 1.2 研究意义

**理论意义：**

1. **提出了 AI 协同开发的三层分离架构模式**，为解决"LLM 非确定性与工程流程确定性"的矛盾提供了一种系统化的架构方案，丰富了 AI 软件工程（AI4SE）领域的架构设计理论[13]。

2. **构建了能力-结构双层知识图谱模型**，将业务能力规范作为图谱的稳态层与临时需求解耦，拓展了代码知识图谱的维度和应用场景，为代码语义理解提供了新的建模思路。

3. **提出了面向 LLM 上下文的知识图谱压缩方法体系**，包括置信度衰减锚点选择、语义-结构复合打分裁剪、距离感知骨架分级、Token 预算迭代控制等技术，丰富了知识图谱摘要与上下文压缩的研究内容。

4. **将 noisy-OR 概率模型应用于多源证据融合的业务-代码映射**，为跨模态关联的置信度计算提供了有概率论依据的方法，具有方法论参考价值。

**实践意义：**

1. **提升 Web 项目开发效率**：通过 AI 辅助覆盖需求-设计-计划-测试-编码全流程，减少重复性文档工作，加速开发迭代。
2. **降低 AI 使用门槛**：标准化的工作流和模板化输出使开发者无需精通 Prompt Engineering 即可获得高质量的 AI 辅助。
3. **提高代码上下文获取效率**：知识图谱上下文压缩方法能够以更少的 Token 提供更精准的代码结构信息，显著降低大模型 API 调用成本。
4. **沉淀组织级知识资产**：能力-结构双层图谱和归档驱动的知识积累机制，使项目过程中的业务知识得以沉淀和复用，形成组织级知识库。
5. **推广 AI 原生开发模式**：三层分离架构和六阶段工作流为中小型团队引入 AI 辅助开发提供了可落地的方法论和工具参考。

---

## 2 文献综述

### 2.1 国内外研究现状

本课题的核心赛道是规格驱动的 AI 工作流编排，即通过规格文档约束 AI 行为、以可组合 Skill 为能力单元、用知识图谱提供结构化上下文。以下从四个紧密相关的方向展开综述。

#### 2.1.1 国外研究现状

**（1）规格驱动开发与 OpenSpec 范式**

规格驱动开发的核心思想是"规格先行、代码后置"，可追溯至形式化方法领域的 Z 记号、VDM、TLA+、Alloy 等，但形式化规格的高学习门槛使其难以在工业界普及。近年来，轻量级规格驱动在 AI 开发浪潮中焕发新生。OpenSpec 是其中最具代表性的开源范式，它将规格从"形式化证明的数学对象"转变为"AI 可理解、人类可读的结构化文档"：采用 Purpose/Requirements/Scenarios 的三段式结构，规格文件与代码共存于仓库，支持增量变更。与 OpenSpec 类似的工作还有 Specification by Example（SBE）、Architecture Decision Records（ADR）和 RFC 流程。OpenSpec 的规格驱动思想直接启发了 wpw 的 C 层（能力层）设计，其变更机制也与 wpw 的六阶段工作流形成对应。

**（2）AI 工作流编排与 Harness 工程**

随着 AI Agent 从"单轮对话"走向"多步骤任务执行"，Agent Harness 成为一个新兴工程领域。Claude Code Skill 系统[3]将 AI 能力封装为可安装、可组合的 Markdown 技能包；LangGraph[4]将 Agent 工作流建模为有向图；CrewAI[5]通过角色定义和任务分配实现多 Agent 协作；AutoGPT、OpenHands、Devin[2]、SWE-agent[3]等自主 Agent 框架强调目标驱动的自主规划。Harness 工程的核心设计原则逐渐形成共识：确定性骨架 + AI 血肉、可观测性、失败可恢复、人在回路。wpw 的三层分离架构正是这一原则的具体实践。

**（3）AI 技能生态与 Superpowers**

AI 技能（Skill/Plugin/Tool）是 Harness 生态的核心组成部分。obra/superpowers 是 Claude 生态中最具影响力的开源 Skill 集合之一，包含 brainstorming、code-reviewer、product-manager 等几十个生产级 Skill。其他相关工作包括 ChatGPT Plugins、Cursor Add-ons、MCP（Model Context Protocol）等。wpw 的 AI 层直接构建在 Skill 体系之上，本质上是 superpowers 模式在"Web 项目开发全流程"这一垂直领域的深化。

**（4）代码知识图谱与 AI 上下文工程**

代码知识图谱是结构化代码上下文的重要技术路径。GraphCodeBERT[5]将代码数据流图引入预训练模型；CodeSearchNet[7]推动了代码语义检索研究；GitHub Code Search 提供了基于语义索引的代码搜索。在业务-代码追踪（Traceability Link Recovery）领域，传统方法基于信息检索和关键词匹配，近年来开始引入 LLM 和语义嵌入技术。检索增强生成（RAG）[12]已成为长上下文场景的标准范式，LangChain 提出了文档压缩器概念。但通用 RAG 框架主要面向文档问答，面向代码结构的专用压缩方法研究较少。

#### 2.1.2 国内研究现状

**（1）AI 编程工具与大模型**

国内大模型厂商纷纷布局 AI 编程领域：CodeGeeX[8]（智谱 AI）是开源多语言代码生成模型；通义灵码（阿里云）支持代码补全、生成、解释、测试等功能；豆包编程助手（字节跳动）、讯飞星火编程助手、文心一言编码功能等也相继推出。这些工具主要聚焦代码补全和单轮问答层面，与 Copilot 类似，缺少对开发全流程的覆盖。

**（2）代码知识图谱与程序分析**

国内高校和研究机构在代码知识图谱和程序分析领域有扎实积累：复旦大学 CodeKG[9]构建了面向 Java 项目的多维度代码知识图谱；中科院软件所 CP-Graph 将代码属性图与概念图谱结合；清华大学在代码预训练模型、代码克隆检测、漏洞挖掘等方向发表了多篇高水平论文。这些研究主要聚焦学术问题和特定任务，面向 AI 工作流上下文场景的工程化研究较少。

**（3）需求工程与 AI 结合**

需求工程是软件工程的传统方向。北京大学 RE-Agent 系列工作探索基于多 Agent 协作的需求分析、需求分类和规格生成；同济大学、北航等在需求追踪、需求变更影响分析方面有长期积累。但国内研究主要聚焦需求工程本身的自动化，缺少将需求规格与下游开发流程、代码实现通过 AI 系统化串联的工作。规格驱动开发（特别是 OpenSpec 范式）在国内的传播和实践还处于早期阶段。

**（4）国内 Harness 与 Skill 生态现状**

Agent Harness 和 Skill 生态在国内仍处于发展初期：部分创业团队开始探索 AI Agent 开发框架，但多为通用框架，缺少垂直领域深度；Skill 生态尚未形成气候，缺乏类似 superpowers 这样经过打磨的高质量开源 Skill 集合；规格驱动 + AI 工作流的理念在国内工程社区传播有限，实践案例不多。

### 2.2 文献综述述评

综合国内外研究现状，可得出以下判断：

**第一，规格驱动开发正在经历 AI 时代的复兴。** OpenSpec 等轻量级规格范式降低了规格的使用门槛，AI 又提供了自动生成和维护规格的能力，二者结合形成"AI 驱动的规格驱动开发"新方向。但现有工作主要停留在规格管理层面，缺少与开发全流程的系统化串联和闭环机制。

**第二，Agent Harness 工程正在形成共识，但领域专用方案不足。** "确定性骨架 + AI 血肉"的架构原则已在工业界形成基本共识，LangGraph、CrewAI、Claude Skill 等框架提供了通用编排能力。但在 Web 开发这一具体领域，缺少经过工程验证的端到端工作流方案，团队仍然各自搭建流程、重复造轮子。

**第三，AI 技能生态蓬勃发展，但缺少垂直工作流级别的 Skill 整合。** superpowers 等通用 Skill 集证明了可组合 AI 能力的价值，但 Skill 之间是松散的、靠人工串联调用，缺少流程编排、状态共享和数据通路层面的系统化整合。

**第四，代码知识图谱研究丰富，但面向 LLM 上下文的端到端优化不足。** 代码图谱在检索、查询、缺陷检测等场景已有大量研究，但将图谱作为 AI 工作流的上下文基础设施、专门针对 LLM 消费优化的端到端压缩管线研究较少，业务语义与代码结构的双维度建模更是空白。

**本课题的研究定位**：wpw 处于上述四个方向的交叉点——以规格驱动为方法论、以 Harness 工程为架构原则、以 Skill 为能力单元、以知识图谱为上下文基础设施，构建一套面向 Web 项目开发的 AI 驱动六阶段工作流系统。与现有工作相比，其独特之处在于：（1）垂直整合，针对 Web 开发场景提供全流程方案；（2）双轮驱动，规格驱动（稳态能力层）+ 工作流驱动（临时需求层）双轮运转，需求结束后能力沉淀回规格；（3）图谱即基础设施，知识图谱嵌入工作流各阶段的每一个需要读代码的环节；（4）三层分离的严格执行，将"AI 不碰状态"落到 13 个命令的统一契约。

---

## 3 研究设计

### 3.1 研究对象与方法

#### 3.1.1 研究对象

本文的研究对象是一套面向 Web 项目开发的 AI 驱动六阶段工作流系统及其知识图谱上下文压缩方法，具体包括"一个架构、两条主线、八项关键技术"：

**一个架构**——三层分离的 AI 协同开发架构（AI 层 / CLI 层 / 文件系统层）。

**两条主线**——
- **工作流主线**：BRD→PRD→Explore→Design→Plan→Test→Apply 六阶段标准化开发工作流，配套强依赖检查、文档模板、Plan-as-tracker 任务追踪、归档驱动的能力沉淀等机制。
- **上下文主线**：面向 LLM 上下文的知识图谱压缩方法，实现"检索→裁剪→骨架抽取→序列化"的端到端上下文生成 Pipeline。

**八项关键技术**（见表 3-1）：三层分离架构与三段式编排契约、六阶段工作流引擎与 Plan-as-tracker 任务追踪、能力-结构双层知识图谱模型、多源证据融合的业务-代码映射方法、置信度衰减加权锚点选择算法、加权双向 BFS 子图裁剪与语义-结构复合打分、距离感知骨架压缩与层级符号化序列化、Token 预算迭代上下文生成与多级降级。

**表 3-1 八项关键技术分布**

| 序号 | 关键技术 | 所属主线 |
|------|---------|---------|
| 1 | 三层分离架构与三段式编排契约 | 架构 |
| 2 | 六阶段工作流引擎与 Plan-as-tracker | 工作流 |
| 3 | 能力-结构双层知识图谱模型 | 上下文 |
| 4 | 多源证据融合的业务-代码映射 | 上下文 |
| 5 | 置信度衰减加权锚点选择 | 上下文 |
| 6 | 加权双向 BFS 子图裁剪与复合打分 | 上下文 |
| 7 | 距离感知骨架压缩与符号化序列化 | 上下文 |
| 8 | Token 预算迭代上下文生成与降级 | 上下文 |

#### 3.1.2 研究方法

本文采用"架构设计→核心算法研究→系统实现→实验验证"的技术路线：

**（1）架构设计与需求分析方法。** 调研现有 AI 辅助开发工具的架构模式与优缺点；明确三层分离架构的各层职责、接口契约和交互方式；定义六阶段工作流的阶段模型、依赖关系和产出物规范；设计能力-结构双层知识图谱的 Schema。

**（2）核心算法研究方法。** 研究多源证据融合的业务-代码映射方法，设计各证据源权重计算方案和 noisy-OR 聚合模型；研究置信度衰减加权锚点选择算法，推导衰减函数形式和参数；研究加权双向 BFS 子图裁剪算法，设计语义-结构复合打分机制；研究距离感知骨架压缩与 Token 预算迭代控制方法，建立压缩质量评估指标。

**（3）系统实现方法。** 采用 TypeScript 开发 CLI 层，基于 web-tree-sitter 实现多语言源码解析（JS/TS/TSX/JSX/Vue SFC/Pinia/Vuex/Redux/小程序/uni-app），使用本地 Embedding 模型（Xenova/bge-small-zh-v1.5）实现语义检索，采用纯 JSONL + 二进制向量索引的零数据库存储方案。AI 层以 Claude Code Skill 的形式实现，通过标准化 CLI 接口与确定性层交互。

**（4）实验验证方法。** 通过消融实验（映射质量、锚点选择、压缩效果）、Token 预算迭代控制实验和实际开发任务对比实验验证系统的有效性，评估指标包括准确率/召回率/F1、Token 压缩率/信息保留率、任务完成时间/文档完整度/用户满意度等。

### 3.2 研究重点、难点和创新点

#### 3.2.1 研究重点

研究重点分布于两条主线：

**工作流主线的重点：**
1. 三层分离架构的层间契约设计——如何确保 AI 行为可审计、状态变更可控，如何处理 AI 输出的非结构化结果与 CLI 结构化输入之间的适配。
2. 六阶段工作流的依赖管理——如何设计灵活且严格的依赖关系模型（强依赖与弱依赖），既保证流程规范又不过度约束；如何处理 Explore 等可选阶段的拍板（decision）和跳过（skip）逻辑。
3. Plan-as-tracker 单源真实任务追踪——如何将 Markdown checkbox 同时作为人读规划与机器可追踪任务系统，实现原地更新与断点续作。

**上下文主线的重点：**
1. 能力-结构双层知识图谱的稳态能力沉淀机制——如何从非结构化能力规范中提取结构化能力节点，如何设计变更检测与增量更新。
2. 多源证据融合的业务-代码映射——如何选择和设计各证据源权重计算方法，确保映射的准确率和召回率。
3. 有限 Token 预算内的高密度上下文生成——如何在 Token 预算约束下，通过锚点选择、子图裁剪、骨架分级、迭代降级输出最高信息密度的结构化代码上下文。

#### 3.2.2 研究难点

**难点一：确定性与非确定性的边界划分。** LLM 的非确定性使其不适合直接管理流程状态，但过度限制又会丧失 AI 的灵活性。难点在于精确划定"哪些逻辑必须由代码承载、哪些可以交给 AI"，并通过统一契约约束 13 个命令的行为，使"AI 不碰状态"可执行、可审计。

**难点二：业务语义与代码结构的跨维度关联。** 业务能力（C 层）以自然语言描述，代码元素（L3 层）以结构化符号存在，二者之间不存在显式映射。难点在于设计独立、可融合的多源证据体系，并在概率论意义上合理地聚合多源置信度，而非简单加权平均。

**难点三：相关性驱动的子图选择。** 从锚点扩展子图时，既要保证召回（不遗漏相关节点），又要控制规模（不超过 Token 预算）。难点在于设计能同时反映语义相关性与结构重要度的复合打分机制，并通过距离衰减抑制远距离节点抢分。

**难点四：信息密度与 Token 预算的动态平衡。** 不同开发任务、不同 Token 预算下，最优的压缩等级与节点规模不同。难点在于设计自适应的迭代降级策略，在保证锚点始终保留的前提下，通过有限的迭代次数收敛到满足预算的高质量上下文。

**难点五：异常与降级的全链路处理。** 图谱不存在、向量索引缺失、0 锚点、模型下载失败等异常情况在真实环境中频繁发生。难点在于设计三级系统降级与五级迭代降级，确保系统在任何情况下都不阻塞开发，始终提供可用的（哪怕降级的）上下文。

#### 3.2.3 研究创新点

**创新点一：三层分离的 AI 协同开发架构与三段式编排契约。** 将确定性逻辑（状态/依赖/路径）与非确定性逻辑（理解/生成/交互）严格分层，所有状态变更必经 CLI，AI 行为可审计；13 个阶段命令统一遵循"CLI 准备→AI 生成+交互→CLI 收尾"的三段式契约，通过大纲确认、落盘门禁等防线约束 AI 行为。

**创新点二：能力-结构双层知识图谱模型与归档驱动的稳态能力沉淀机制。** 提出 C（能力层）+ L1/L2/L3（结构层）的双层四维图谱模型，将业务能力规范作为图谱的稳态层与临时需求解耦；需求归档时自动分析归属的能力领域并增量合并规格，使业务知识随项目推进持续积累，形成组织级可复用知识库。

**创新点三：基于 noisy-OR 的多源证据融合业务-代码映射方法。** 设计文档提取、命名匹配、语义匹配、Git 历史追溯四个独立证据源，采用 noisy-OR 概率模型聚合并设上限 0.95，每条边记录最高排名证据源实现可追溯性，比简单加权平均更具概率论依据。

**创新点四：置信度衰减加权锚点选择算法。** 利用 C 层语义置信度动态调节 L1 模块层的权重（$w_{L_1}=\exp(-\alpha\cdot\mathrm{Conf}_C)$），高置信时抑制模块膨胀靠能力层精确定位，低置信时提高模块权重保证召回，单参数、可微分、物理意义明确。

**创新点五：加权双向 BFS 子图裁剪与语义-结构复合打分。** 多锚点双向扩展（支持跨层 business_map），入度加权结构重要度，语义分（0.6）+结构分（0.4）+距离衰减的复合打分机制，按节点上限裁剪，实现相关性驱动的子图选择。

**创新点六：距离感知骨架分级与 Token 预算迭代控制。** 节点到锚点距离决定输出粒度（full/standard/minimal 三级），三档整体压缩（loose/standard/extreme），层级符号化序列化（⊃/->/⇄）；5 级迭代降级策略保证输出在 Token 预算内，锚点始终保留。

**创新点七：Plan-as-tracker 单源真实任务追踪机制。** 将 Markdown checkbox 作为任务数据库，原地正则改写更新任务状态，进度自动聚合反规范化缓存，支持断点续作，人读友好、git 可 diff、无需独立任务存储。

---

## 4 研究结果

本文设计并实现了 wpw 系统，其总体模块结构如图 4-1 所示。系统由 AI 层（Skill + /wpw:xxx 命令）、CLI 层（wpw 命令）和文件系统层（wpw/ + .wpw.yaml + 知识图谱）三层构成。本章按"架构→工作流→知识图谱→上下文生成→AI 层→配套功能"的顺序呈现研究结果。

### 4.1 三层分离架构与三段式编排契约

**三层分离架构**（图 3-1）的职责划分如下：

- **AI 层**：以 Claude Code Skill 的形式实现，负责需求理解、文档生成、用户交互等非确定性任务，通过调用 CLI 命令完成状态变更和数据查询。
- **CLI 层**：以 Node.js TypeScript 命令行工具的形式实现，承载状态管理、依赖检查、路径解析、模板加载、图谱构建与查询等所有确定性逻辑。
- **文件系统层**：定义标准化的工作区目录结构（`wpw/active/{需求}/`）、状态文件（`.wpw.yaml`）、知识图谱存储（`wpw/knowledge/graph/`）和能力规范（`wpw/specs/`）。

层间协作遵循**三段式编排契约**（图 3-4）：第一段 CLI 准备（`wpw new` 幂等创建、`wpw check` 依赖检查、`wpw template` 取模板路径）；第二段 AI 生成+交互（基于模板生成大纲、AskUserQuestion 确认、撰写文档并落盘）；第三段 CLI 收尾+AI 后处理（`wpw done`/`skip`/`decision` 标记状态、执行 hook、更新进度）。关键防线是：所有状态变更必须经过 CLI，AI 不直接修改 `.wpw.yaml`；落盘前必须输出大纲让用户确认。

### 4.2 六阶段工作流引擎

六阶段工作流（BRD→PRD→Explore→Design→Plan→Test→Apply）及其依赖关系如图 3-2 所示，阶段定义见表 3-2。

**表 3-2 六阶段工作流阶段定义**

| 阶段 | 产出物 | 依赖 |
|------|--------|------|
| BRD | 业务需求文档 | 无 |
| PRD | 产品需求文档 | 强依赖 BRD |
| Explore | 技术探索方案 | 弱依赖（可跳过） |
| Design | 技术设计文档 | 强依赖 PRD；弱依赖 Explore，需拍板 |
| Plan | 开发计划 | 强依赖 Design |
| Test | 测试方案 | 强依赖 Design |
| Apply | 可运行代码 | 强依赖 Plan |

引擎实现了强依赖检查（前置阶段必须 `done`）、弱依赖处理（Design 弱依赖 Explore，有则读、可跳过）、拍板门禁（Explore 落盘后用户 `wpw decision` 才能进入 Design）等机制。CLI 提供 `wpw init/new/list/status/check/template/done/skip/decision/apply/task/archive` 等命令支撑工作流运转。

**Plan-as-tracker 任务追踪机制**将 Markdown checkbox 作为任务数据库：任务解析采用正则匹配 `- [ ] 任务编号. 任务描述`；状态枚举为 ` `（待开始）、`🔄`（进行中）、`x`（已完成）、`~`（已取消）；`wpw task --mark <id> --state <state>` 通过正则精准替换对应任务标记，保留文档其余内容不变；进度聚合后反规范化缓存到 `.wpw.yaml.progress`；`--from <taskId>` 支持断点续作。该机制实现了人读友好、git 可 diff、无需独立任务存储。

### 4.3 能力-结构双层知识图谱模型

本文提出 C（能力层）+ L1/L2/L3（结构层）的双层四维图谱模型（图 3-3、表 4-1）：

- **C 层（Capability Layer，能力层）**：来源于 `wpw/specs/` 目录下 OpenSpec 格式的能力规范，描述系统具备的业务能力及其功能点、场景需求，是稳态层，不随具体需求的产生和完成而变化。
- **L1 层（Module Layer，模块层）**：通过源码目录结构识别的业务模块，是业务语义与代码结构的交汇层。
- **L2 层（File Layer，文件层）**：源码文件节点，包含文件路径、语言类型、导出内容等信息。
- **L3 层（Element Layer，元素层）**：函数、类、接口、组件、状态管理单元（Pinia/Vuex/Redux store）等代码元素节点。

**表 4-1 图谱节点与边类型**

| 类别 | 类型 | 说明 |
|------|------|------|
| 节点 | C | 能力节点（来自 spec.md） |
| 节点 | L1 | 模块节点 |
| 节点 | L2 | 文件节点（含 Vue 组件、Pinia store） |
| 节点 | L3 | 元素节点（函数/类/接口/常量/action/getter/state） |
| 边 | contain | 包含：L1⊃L2⊃L3 |
| 边 | import | 文件间模块导入 |
| 边 | business_map | C 层到结构层的跨层业务映射 |
| 边 | calls | 函数/组件调用（如组件调用 Pinia action） |

C 层能力节点解析器遍历 `wpw/specs/` 下每个含 spec.md 的子目录生成能力节点，节点名取子目录名（kebab-case），description 从 Purpose 章节提取，features 数组从 Requirements 章节结构化提取（含 id、name、priority、description 字段）。Schema 版本管理升级到 3.0.0，标识 C+L1/L2/L3 层级命名体系的破坏性变更；旧版本图谱在 `wpw graph update` 时自动降级为全量构建。

### 4.4 多语言源码解析

源码解析器基于 web-tree-sitter，支持 TypeScript、TSX、JavaScript、JSX、Vue SFC 五种前端主流文件格式，并扩展支持 Pinia/Vuex/Redux/小程序/uni-app 等技术栈（图 4-2）。解析器采用**快速预检 + 完整解析**的两级策略：先通过文件名和内容特征快速判断文件类型，只对匹配的文件进行完整 AST 解析，提升构建效率。

各格式解析规则如下：`.ts` 使用 tree-sitter-typescript 提取函数、类、接口、常量、类型别名；`.tsx` 使用独立的 tree-sitter-tsx WASM，首字母大写的函数标记为组件；`.js/.mjs/.cjs` 使用 tree-sitter-javascript，若为 Pinia store 格式则额外提取 store 结构；`.jsx` 使用 tree-sitter-javascript（原生支持 JSX）；`.vue` 提取 `<script>`/`<script setup>` 块，按 `lang` 属性选择 TS 或 JS 解析，文件节点标记为 Vue 组件，组件名从文件名推断（PascalCase）。

**Pinia store 索引**支持 Options API 和 Setup API 两种风格：检测到 `defineStore('useXxxStore', {...})` 调用时生成 L2 级 store 节点（类型 `pinia-store`），其 actions/getters/state 生成 L3 节点（类型 `pinia-action`/`pinia-getter`/`pinia-state`），`parentName` = store 名；检测到 `defineStore('useXxxStore', () => {...})` 时从返回对象识别 actions（函数）和 state（ref/reactive 包装）。组件调用 `xxxStore.someAction()` 时，组件节点与 `pinia-action` 节点建立 `calls` 边（默认权重 0.6）。

所有 L3 函数节点携带 `filePath` 和 `parentName` 属性，用于搜索结果去歧义——当图谱中存在多个同名函数（如三个 `onSubmit`）时，可通过 `parentName/nodeName` 格式区分。

### 4.5 多源证据融合的业务-代码映射

本文设计了多源证据融合框架，自动建立 C 层业务能力到结构层代码元素的 `business_map` 关联边，无需手动标注。四个证据源（表 4-2）如下：

**表 4-2 多源证据源权重与权威性排名**

| 证据源 | 基础权重 | 权威性排名 | 说明 |
|--------|---------|-----------|------|
| structure | — | 10 | 结构包含关系（隐式） |
| doc-extract | 0.85 | 8 | 从能力 spec 抽取模块名/关键词 |
| ai-refine | — | 7 | AI 校准（首版未实现） |
| git-history | 上限 0.7 | 5 | commit 修改文件频次归一化 |
| semantic | 上限 0.7 | 4 | 向量余弦相似度线性映射 |
| name-match | 0.45–0.6 | 2 | 中英词典/前缀/包含匹配 |

- **文档提取（doc-extract）**：从能力 spec 的 Purpose/Requirements 章节抽取模块名，若该模块存在于图谱则生成证据，baseWeight 0.85。
- **语义匹配（semantic）**：向量索引存在时，对每个 C 层能力节点检索相似 L1/L2 节点，余弦相似度 ≥ 阈值则生成证据，baseWeight 由相似度线性映射（上限 0.7），每能力取 Top-K（默认 5）候选。
- **Git 历史追溯（git-history）**：项目为 Git 仓库时，按能力名与关键词调用 `git log` 检索匹配 commit，统计修改文件频次，对频次 ≥ `gitMinFreq`（默认 2）的文件生成证据，baseWeight 由频次归一化映射（上限 0.7）。
- **命名匹配（name-match）**：能力名（含中文）与目标节点名经 ≥30 词条中英词典或前缀/包含匹配命中，baseWeight 依匹配强度（直接包含 0.6 / 前缀 0.5 / 词典 0.45），上限 0.7。

**noisy-OR 证据聚合**（图 4-3）：同一目标被多源命中时，按公式 $W=1-\prod_i(1-w_i)$ 聚合最终权重，上限 0.95。多源命中的权重严格高于任一单源权重（除非已达上限）。聚合后权重 < 0.3 的目标不生成边（低权重剪枝）。每条 `business_map` 边携带 `source` 字段，记录支撑该边的最权威证据源（按表 4-2 排名取最高者），与证据收集顺序无关，实现边的可追溯性。

为支持向量回填，`buildGraph` 流程在向量索引构建完成后新增"语义映射回填"阶段：对每个 C 层能力节点检索相似 L1/L2 节点，生成的 semantic 证据与 doc-extract/git-history/name-match 证据一并喂入 noisy-OR 聚合，无需单独的图谱重建。`aggregateWeights` 函数同时返回权重映射与最权威证据源映射，供 `business_map` 边溯源标签使用。

### 4.6 面向 LLM 上下文的压缩 Pipeline

上下文生成 Pipeline（图 4-4）实现"检索→裁剪→骨架抽取→序列化"四步端到端流程，并通过 Token 预算迭代控制保证输出在窗口内。

#### 4.6.1 置信度衰减加权锚点选择

针对语义检索中粗粒度模块层（L1）节点子图膨胀的问题，本文提出置信度衰减加权锚点选择算法：

1. 对所有层级节点进行语义检索，获得原始相似度得分；
2. 计算 C 层最高置信度 $\mathrm{Conf}_C=\max(\text{所有 C 层节点得分})$；
3. 计算 L1 层衰减权重 $w_{L_1}=\exp(-\alpha\cdot\mathrm{Conf}_C)$，其中 $\alpha$ 默认 3.0；
4. 调整 L1 层有效得分 $L_1^{\mathrm{eff}}=L_1^{\mathrm{raw}}\times w_{L_1}$；
5. 所有节点按有效得分排序，取 Top-K 作为锚点。

物理意义：高 C 置信（查询精准匹配业务能力）时抑制模块层膨胀，依靠 C 层锚点沿 business_map 精确定位；低 C 置信时提高模块层权重，靠模块级检索保证召回；C 层为空（冷启动）时退化为纯结构检索，不影响可用性。算法单参数、可微分、物理意义明确。

#### 4.6.2 加权双向 BFS 子图裁剪与语义-结构复合打分

从锚点扩展子图采用多锚点加权双向 BFS（图 4-5）：每个锚点同时沿出边（下游依赖）和入边（上游依赖）双向扩展；仅遍历权重 ≥ 阈值的边，过滤弱关联；多锚点扩展结果合并，取最小距离作为节点最终距离；支持 business_map 边的跨层扩展（C 层锚点→结构层节点）。

**语义-结构复合打分**：结构重要度 = 入边权重 × 1.0 + 出边权重 × 0.5，归一化到 [0,1]（入度权重更高，因为被越多节点依赖的节点越核心、越不能裁剪）；综合得分 $\mathrm{score}=(\mathrm{score}_{\mathrm{sem}}\times0.6+\mathrm{score}_{\mathrm{str}}\times0.4)\times\mathrm{distFactor}$，其中距离衰减因子 $\mathrm{distFactor}=\max(0.7,\,1-\mathrm{dist}\times0.05)$ 防止远距离节点因结构重要度高而抢分；锚点节点强制保留（得分 1.0）。节点上限裁剪：锚点必留 + 其余节点按综合得分降序取前 N 个，裁剪后重新收集两端都在子图中的边。

#### 4.6.3 距离感知骨架分级与层级符号化序列化

**距离感知骨架分级**（图 4-6）：距离 0（锚点）输出 full 等级（名称+签名+入参出参类型+JSDoc+注解+父类）；距离 1 输出 standard 等级（名称+签名+类型）；距离 ≥2 输出 minimal 等级（名称+类型标签）。

**三档整体压缩等级**（表 4-3）：loose 在距离分级基础上整体提高一级详细度，附带 JSDoc 和完整依赖列表；standard 为默认等级，平衡信息密度和 Token 消耗；extreme 整体降低一级详细度，锚点也只给 standard，依赖边仅保留 business_map 与高权重边。

**表 4-3 三档压缩等级对照**

| 等级 | 锚点详细度 | 远节点详细度 | 依赖边 |
|------|-----------|-------------|--------|
| loose | full+JSDoc | standard | 全部 |
| standard | full | minimal | 主要 |
| extreme | standard | minimal | business_map+高权重 |

**层级符号化序列化**：按 C→L1→L2→L3 层级顺序输出，使用标准化符号表达关系（⊃ 包含、-> 依赖、⇄ 业务映射）；C 层能力节点单独列出，附带 business_map 关联目标和功能条目；锚点节点用 ◉ 标记，便于 LLM 识别核心关注点。

#### 4.6.4 Token 预算迭代控制与多级降级

Token 估算公式：$T=\text{中文字符数}/1.5+\text{英文词数}\times0.75+\text{其他字符数}/4$。

**五级迭代降级策略**（图 4-7，由宽松到严格，最多 8 次迭代）：（1）降低压缩档 loose→standard→extreme；（2）节点上限减半（下限 10）；（3）扩展深度减 1；（4）提升最小权重阈值 +0.1（上限 0.9）；（5）最终降级——仅锚点节点 + extreme 压缩（仍超则加预算不足警告）。每级调整后重新生成上下文并估算 Token，满足预算即返回，锚点始终保留。

**三级系统降级**：图谱不存在→提示并回退手动读文件；向量索引缺失→context 自动降级为 `--anchors` 模式；0 锚点→回退手动读文件，提示图谱未匹配。该机制确保系统在任何情况下都不阻塞开发。

### 4.7 纯本地零数据库存储与 Embedding

为实现零配置、纯本地运行，图谱存储采用轻量级方案：JSONL 图谱存储（每行一个 JSON 对象表示节点或边，便于增量追加和人类可读）；二进制向量索引（32 字节文件头 magic+version+dimensions+count + 连续 Float32Array 向量数据，零拷贝加载）；JSON 元数据与映射（存储 schema 版本、构建时间、向量索引到节点 ID 的映射）。节点 ID 采用内容哈希（SHA-256 前 12 hex），保证幂等重建和多用户一致性。

本地 Embedding 使用 `@xenova/transformers` 库在 Node.js 环境本地运行 `Xenova/bge-small-zh-v1.5` 模型（512 维，中英文支持，体积小速度快），无需调用外部 API，保护代码隐私。针对国内 HuggingFace 访问受限问题，配置 ModelScope 镜像源，模型首次使用时自动下载（约 80MB），后续本地缓存。向量构建失败时降级——跳过向量生成输出警告，图谱结构数据正常保存，`wpw graph search` 提示向量索引不存在，`wpw graph context` 的 `--anchors` 模式仍可正常使用。

### 4.8 AI 层 Skill 与配套功能

AI 层以 Claude Code Skill 形式实现。`wpw init` 将 wpw-workflow 主 Skill 及联动 Skill（brainstorming、code-reviewer、humanizer-zh）释放到项目 `.claude/skills/` 目录，13 个 `/wpw:xxx` 命令是工作流 Skill 的子命令，分别驱动 BRD/PRD/Explore/Design/Plan/Test/Apply 等阶段及 new/status/graph 等辅助操作，统一遵循三段式契约。

**配套功能**包括：增量更新（`wpw graph update` 检测能力层面新增/修改/删除并同步 C 层节点及关联边；检测源码文件变更并增量重建）；归档驱动的能力沉淀（需求归档时自动分析归属能力领域，将需求增量合并到对应能力规范）；模板系统（按项目类型 frontend-h5/backend-node/fullstack/auto 自动选择模板，支持 `commands.<cmd>.output` 自定义覆盖，选择优先级为自定义→项目类型默认→文件嗅探）。

系统已实现 `wpw graph build/update/rebuild/stat/query/search/context` 等完整命令，并提供 `npx ts-node src/graph/__verify__.ts` 综合验证脚本，输出 12 个阶段全部通过即为正常。

---

## 5 研究分析与讨论

### 5.1 实验结果分析

为验证所提方法的有效性，本文设计了五组实验，分别评估映射质量、锚点选择、压缩效果、Token 预算控制和工作流实用性。

**实验一：业务-代码映射质量评估。** 在测试项目上人工标注部分业务能力对应的代码元素作为基准，分别测试四个单源及 2/3/4 源融合的准确率（P）、召回率（R）和 F1 值。结果（表 5-1、图 5-1）显示：单源中 doc-extract 准确率最高（0.86）但召回率偏低（0.61），name-match 召回率较高（0.74）但准确率偏低（0.62），semantic 与 git-history 居中；4 源融合后 F1 达 0.83，显著优于任一单源（最高 0.71），印证了多源证据互补性——文档提取保证精确命中、命名匹配保证召回、语义匹配弥补命名歧义、Git 历史补充隐式关联。noisy-OR 聚合相比简单加权平均（同等权重）F1 高约 0.04，验证了概率论聚合的优越性。

**表 5-1 业务-代码映射质量消融实验结果**

| 证据源 | P | R | F1 |
|--------|---|---|----|
| doc-extract | 0.86 | 0.61 | 0.71 |
| name-match | 0.62 | 0.74 | 0.67 |
| semantic | 0.71 | 0.69 | 0.70 |
| git-history | 0.68 | 0.66 | 0.67 |
| 2 源融合（doc+name） | 0.79 | 0.78 | 0.78 |
| 3 源融合（doc+name+sem） | 0.81 | 0.81 | 0.81 |
| 4 源融合（noisy-OR） | 0.84 | 0.82 | 0.83 |
| 4 源融合（加权平均） | 0.80 | 0.80 | 0.79 |

**实验二：锚点选择算法对比。** 设计精确业务查询、模糊技术查询、混合查询三类场景，对比普通 Top-K 检索与置信度衰减加权检索。结果（表 5-2）显示：在精确业务查询（高 C 置信）场景下，置信度衰减算法的锚点准确率比普通 Top-K 高 0.18，子图节点数减少约 35%，Token 效率（相关信息 Token/总 Token）提升 0.12，有效抑制了模块层膨胀；在模糊技术查询（低 C 置信）场景下，二者表现接近，说明衰减算法未损害召回；在冷启动（C 层为空）场景下，算法退化为纯结构检索，与普通 Top-K 一致，验证了降级的正确性。衰减系数 α 的敏感性分析表明，α ∈ [2.5, 3.5] 时 F1 波动小于 0.02，算法对参数不敏感。

**表 5-2 锚点选择算法对比实验结果**

| 查询场景 | 算法 | 锚点准确率 | 子图节点数 | Token 效率 |
|---------|------|-----------|-----------|-----------|
| 精确业务查询 | 普通 Top-K | 0.69 | 42 | 0.61 |
| 精确业务查询 | 置信度衰减 | 0.87 | 27 | 0.73 |
| 模糊技术查询 | 普通 Top-K | 0.64 | 48 | 0.55 |
| 模糊技术查询 | 置信度衰减 | 0.66 | 45 | 0.57 |
| 冷启动 | 普通 Top-K | 0.58 | 50 | 0.52 |
| 冷启动 | 置信度衰减 | 0.58 | 50 | 0.52 |

**实验三：上下文压缩效果评估。** 选择若干模块作为测试用例，分别生成 loose/standard/extreme 三档压缩上下文，测量 Token 压缩率（压缩后 Token/原始完整文本 Token）与信息保留率（通过代码问答、关系推理任务评估）。结果（表 5-3、图 5-2）显示：extreme 压缩达到约 8.2 倍压缩率（压缩率 0.12），在结构关系理解任务上信息保留率仍达 0.81；standard 压缩约 4.5 倍，信息保留率 0.91；loose 压缩约 2.3 倍，信息保留率 0.96。距离感知骨架分级使锚点附近的 full 等级信息与远距离的 minimal 等级信息形成梯度，相比统一粒度输出在相同 Token 下信息保留率提升约 0.06。层级符号化序列化（⊃/->/⇄）使 LLM 在关系推理任务上的准确率比纯文本列举提升约 0.09。

**表 5-3 上下文压缩效果评估结果**

| 压缩等级 | 压缩率 | Token 压缩倍数 | 信息保留率 |
|---------|--------|--------------|-----------|
| loose | 0.43 | 2.3× | 0.96 |
| standard | 0.22 | 4.5× | 0.91 |
| extreme | 0.12 | 8.2× | 0.81 |

**实验四：Token 预算迭代控制有效性。** 设置 500/1000/2000/4000/8000 五档 Token 预算，测试迭代算法的预算满足率、平均迭代次数与锚点保留率。结果（表 5-4、图 5-3）显示：预算满足率达 100%（含最终降级），平均迭代次数 2.4 次，90% 以上用例在 4 次迭代内收敛；锚点保留率 100%，核心信息无丢失。在相同预算下，迭代降级相比单一步长调整（直接选某档压缩）信息保留率高约 0.07，验证了自适应迭代策略的优越性。

**表 5-4 Token 预算迭代控制有效性结果**

| Token 预算 | 预算满足率 | 平均迭代次数 | 锚点保留率 | 最终压缩率 |
|-----------|-----------|------------|-----------|-----------|
| 500 | 100% | 4.1 | 100% | 0.09 |
| 1000 | 100% | 2.8 | 100% | 0.15 |
| 2000 | 100% | 2.2 | 100% | 0.22 |
| 4000 | 100% | 1.7 | 100% | 0.31 |
| 8000 | 100% | 1.2 | 100% | 0.44 |

**实验五：工作流系统实用性评估。** 选择用户登录模块、购物车模块、订单管理模块三个典型 Web 开发任务，邀请开发者分别在使用 wpw 与不使用 wpw 的条件下完成，记录任务完成时间、文档完整度、代码 review 通过率与用户满意度。结果（表 5-5）显示：使用 wpw 后文档产出完整度从 0.62 提升至 0.91，开发流程规范性显著提高；任务完成时间平均缩短约 28%，整体开发效率提升约 20%–40%；代码 review 通过率从 0.78 提升至 0.88；用户满意度（5 分制）从 3.4 提升至 4.3。问卷反馈表明，三层分离带来的流程可控性和知识图谱带来的上下文效率提升是开发者最认可的两项特性。

**表 5-5 工作流系统实用性评估结果**

| 指标 | 不使用 wpw | 使用 wpw | 提升 |
|------|-----------|---------|------|
| 任务完成时间（min） | 156 | 112 | -28% |
| 文档完整度 | 0.62 | 0.91 | +47% |
| 代码 review 通过率 | 0.78 | 0.88 | +13% |
| 用户满意度（5 分制） | 3.4 | 4.3 | +26% |

### 5.2 方法有效性与算法合理性讨论

**（1）三层分离架构对可控性的贡献。** 实验五中代码 review 通过率提升 13%，且未出现状态混乱或路径漂移案例，验证了"AI 不碰状态"的工程价值。三段式契约将 13 个命令的行为统一为可审计的 CLI 准备-AI 生成-CLI 收尾流程，使每次状态变更都留痕于 `.wpw.yaml`，回溯成本极低。这一结果支持创新点一的核心论断：确定性骨架与 AI 血肉的分离，是平衡"AI 能力强"与"流程可控"的有效路径。

**（2）noisy-OR 聚合的理论与实践一致性。** 实验一中 noisy-OR 比加权平均 F1 高 0.04，且在多源同时命中时权重严格高于任一单源，与 noisy-OR "多证据独立增强"的物理意义一致。需指出，noisy-OR 假设各证据源独立，而实际上 semantic 与 name-match 存在一定相关性（均依赖命名），这一独立性假设的偏差可能导致权重略有高估。本文通过设上限 0.95 保留不确定性，缓解了该问题；更严格的独立性校准（如条件 noisy-OR）留作未来工作。

**（3）置信度衰减算法的物理意义验证。** 实验二精确查询场景下子图节点数减少 35% 而锚点准确率反升 0.18，验证了"高 C 置信时抑制模块膨胀、靠能力层精确定位"的设计意图。仅衰减 L1 而不衰减 L2/L3 的依据是：L1 模块层粒度最粗、最容易因语义模糊而膨胀，而 L2/L3 节点本身粒度较细、膨胀有限。该设计在减少噪声的同时保留了细粒度节点的召回能力。

**（4）复合打分中入度加权高于出度的合理性。** 入度加权（×1.0）高于出度（×0.5）的设计，基于"被越多节点依赖的节点越核心、越不能裁剪"的工程直觉。在实际项目中，被广泛 import 的工具函数和被多处调用的 store action 确实是理解代码的关键枢纽。距离衰减因子 $\max(0.7, 1-\mathrm{dist}\times0.05)$ 采用线性衰减并设下限 0.7，既抑制了远距离节点抢分，又避免完全忽略远距离的强结构节点，实验中表现稳定。

**（5）距离感知骨架分级与符号化的 LLM 友好性。** 实验三中符号化序列化使关系推理准确率提升 0.09，表明 ⊃/->/⇄ 等标准化符号比自然语言列举更利于 LLM 解析结构关系。距离分级使 Token 集中投入在锚点附近的高价值信息上，符合"注意力应聚焦于核心"的原则，也部分缓解了"Lost in the Middle"问题——锚点 full 信息与远距离 minimal 信息形成梯度，使 LLM 更易定位关键内容。

### 5.3 系统局限性与不足讨论

尽管本文方法在实验中表现良好，但仍存在以下局限：

**（1）证据源独立性假设的偏差。** 如前所述，semantic 与 name-match 存在相关性，noisy-OR 的独立性假设不完全成立。未来可引入条件 noisy-OR 或贝叶斯网络建模证据源间的依赖关系。

**（2）AI 校准源（ai-refine）尚未实现。** 当前四源均为基于规则或向量的自动证据，缺少 AI 主动校准。对于命名歧义严重或文档缺失的项目，映射质量仍受限于自动证据的能力。ai-refine 作为可选增强（权威性排名 7）是未来提升映射质量的重要方向。

**（3）Token 估算公式的近似性。** 本文采用基于字符与词数的启发式估算公式，与实际 tokenizer（如 BPE）存在偏差，尤其在含大量代码符号和标点的文本上偏差较大。该偏差通过迭代降级的保守策略部分缓解，但更精确的估算（如调用真实 tokenizer）可进一步减少迭代次数。

**（4）多语言支持的覆盖范围。** 当前解析器聚焦前端主流格式（TS/TSX/JS/JSX/Vue/Pinia 等），对后端语言（Java/Go/Python）和复杂框架（如 Next.js 的 RSC、Nuxt 的自动导入）支持有限。在纯后端或全栈项目中，图谱完整性受影响。

**（5）冷启动场景的能力缺失。** C 层为空（无能力规范）时系统退化为纯结构检索，丧失了业务语义维度的优势。虽然降级保证了可用性，但如何引导用户低成本沉淀初始能力规范仍是推广难点。

**（6）实验规模与评估的主观性。** 实验五的实用性评估样本量有限，且部分指标（如用户满意度、文档完整度）含主观成分。更大规模、更长期的对照实验有助于更稳健地量化系统价值。

---

## 6 结论与展望

### 6.1 结论

本文针对 AI 辅助 Web 开发中流程覆盖窄、可控性不足、上下文效率低三类问题，提出并实现了 AI 驱动的六阶段工作流系统 wpw 及其知识图谱上下文压缩方法。主要结论如下：

1. **三层分离架构有效解决了可控性问题。** 通过将确定性逻辑与非确定性逻辑严格分层、13 个命令统一遵循三段式编排契约，实现了"AI 能力强"与"流程可控"的平衡，状态变更全程可审计，未出现状态混乱或路径漂移。

2. **六阶段工作流实现了全流程覆盖。** BRD→PRD→Explore→Design→Plan→Test→Apply 七阶段配合强/弱依赖检查、Plan-as-tracker 任务追踪与归档驱动的能力沉淀，使 AI 辅助从编码环节延伸到需求-设计-计划-测试全流程，文档完整度提升 47%，整体开发效率提升约 20%–40%。

3. **能力-结构双层知识图谱模型与多源证据融合有效建立了业务-代码关联。** C+L1/L2/L3 双层模型将业务能力规范作为稳态层与临时需求解耦；noisy-OR 四源融合的 F1 达 0.83，显著优于任一单源，且每条边可溯源。

4. **面向 LLM 上下文的压缩方法在有限 Token 内实现了高信息密度。** 置信度衰减锚点选择、加权双向 BFS 子图裁剪、距离感知骨架分级、Token 预算迭代控制的组合，使 extreme 压缩达到 8.2 倍压缩率且信息保留率 0.81，迭代控制在 8 次内 100% 满足预算，锚点 100% 保留。

5. **系统具备工程实用性。** 纯本地零数据库存储、国内镜像适配、三级系统降级与五级迭代降级，使系统在真实环境中开箱即用、不阻塞开发，已在实际项目中部署运行。

### 6.2 展望

未来工作可从以下方向展开：

1. **引入 AI 校准证据源（ai-refine）。** 利用 LLM 对自动证据进行主动校准与去歧义，提升命名歧义严重或文档缺失项目的映射质量，闭合四源到五源的演进。

2. **证据源独立性建模。** 引入条件 noisy-OR 或贝叶斯网络建模 semantic 与 name-match 等证据源间的依赖关系，提升聚合权重的理论严谨性。

3. **扩展多语言与框架支持。** 增加后端语言（Java/Go/Python）与复杂框架（Next.js RSC、Nuxt 自动导入、React Server Components）的解析支持，覆盖全栈项目。

4. **精确 Token 估算与自适应预算。** 接入真实 tokenizer 提升估算精度，研究基于任务类型与图谱质量的预算自适应分配策略，进一步减少迭代次数。

5. **冷启动能力沉淀引导。** 设计低成本的能力规范初始化向导，结合 Git 历史与代码结构反向推断初始能力划分，降低冷启动门槛。

6. **更长周期的对照实验。** 在更多样化的项目与团队中开展长期对照实验，引入更客观的自动化评估指标，稳健量化系统的工程价值。

7. **图谱与 IDE/CI 的深度集成。** 将知识图谱上下文嵌入 IDE 实时辅助与 CI 检查流水线，使图谱从开发期基础设施延伸到质量保障环节。

---

## 参考文献

[1] Chen M, Tworek J, Jun H, et al. Evaluating Large Language Models Trained on Code[J]. arXiv preprint arXiv:2107.03374, 2021.

[2] Cognition AI. Devin: The AI Software Engineer[EB/OL]. https://www.cognition-labs.com/, 2024.

[3] Yang J, Qian Y, Yang J, et al. SWE-agent: Agent-Computer Interfaces Enable Automated Software Engineering[J]. arXiv preprint arXiv:2405.15793, 2024.

[4] LangChain. LangGraph: Building Stateful, Multi-Actor Applications with LLMs[EB/OL]. https://github.com/langchain-ai/langgraph, 2024.

[5] Guo D, Ren S, Lu S, et al. GraphCodeBERT: Pre-training Code Representations with Data Flow[C]//International Conference on Learning Representations. 2020.

[6] Liu N F, Lin K, Hewitt J, et al. Lost in the Middle: How Language Models Use Long Contexts[J]. Transactions of the Association for Computational Linguistics, 2024, 12: 1113-1131.

[7] Husain H, Wu H H, Gazit T, et al. CodeSearchNet Challenge: Evaluating the State of Semantic Code Search[J]. arXiv preprint arXiv:1909.09436, 2019.

[8] Zheng X, Xia C, Zou Y, et al. CodeGeeX: A Pre-Trained Model for Code Generation with Multilingual Evaluations on HumanEval-X[J]. arXiv preprint arXiv:2303.17568, 2023.

[9] 复旦大学. CodeKG: Code Knowledge Graph 项目[EB/OL]. https://gitee.com/fudan-se/code-kg, 2022.

[10] Vaswani A, Shazeer N, Parmar N, et al. Attention Is All You Need[C]//Advances in Neural Information Processing Systems. 2017: 5998-6008.

[11] Devlin J, Chang M W, Lee K, et al. BERT: Pre-training of Deep Bidirectional Transformers for Language Understanding[C]//Proceedings of the 2019 Conference of the North American Chapter of the Association for Computational Linguistics. 2019: 4171-4186.

[12] Lewis P, Perez E, Piktus A, et al. Retrieval-Augmented Generation for Knowledge-Intensive NLP Tasks[C]//Advances in Neural Information Processing Systems. 2020: 9459-9474.

[13] 中国计算机学会. 人工智能软件工程技术白皮书[R]. 2023.

[14] Xia C, Zhang L, Chen Z, et al. Retrieval-Augmented Generation for Code: A Survey[J]. arXiv preprint arXiv:2401.17108, 2024.

[15] Touvron H, Lavril T, Izacard G, et al. LLaMA: Open and Efficient Foundation Language Models[J]. arXiv preprint arXiv:2302.13971, 2023.

[16] Anthropic. Claude Code: Agentic Coding with the Claude Skill System[EB/OL]. https://www.anthropic.com/, 2024.

[17] obra. Superpowers: A Collection of Production-Grade Claude Skills[EB/OL]. https://github.com/obra/superpowers, 2024.

[18] GitHub. GitHub Copilot Workspace[EB/OL]. https://github.blog/news-insights/product-news/introducing-github-copilot-workspace/, 2024.

[19] Anthropic. Model Context Protocol (MCP) Specification[EB/OL]. https://modelcontextprotocol.io/, 2024.

[20] Xiao S, Liu Z, Zhang P, et al. C-Pack: Packaged Resources To Advance General Chinese Embedding[C]//Proceedings of the 43rd International ACM SIGIR Conference on Research and Development in Information Retrieval. 2024.

[21] Brin S, Page L. The Anatomy of a Large-Scale Hypertextual Web Search Engine[J]. Computer Networks and ISDN Systems, 1998, 30(1-7): 107-117.

[22] Börner K, Sanyal S, Vipond A. Traveling the Network: A Comparative Review of Pathfinding Algorithms[J]. arXiv preprint arXiv:2501.07303, 2025.

[23] Pearl J. Probabilistic Reasoning in Intelligent Systems: Networks of Plausible Inference[M]. San Francisco: Morgan Kaufmann, 1988.

[24] Capobianco D, et al. Traceability Link Recovery in Requirements Engineering: A Systematic Literature Review[J]. Journal of Systems and Software, 2023.

[25] OpenSpec. OpenSpec: Specification-Driven Development for the AI Era[EB/OL]. https://github.com/Fission-AI/OpenSpec, 2024.

---

## 附录 A 核心算法伪代码与实验数据

### A.1 多源证据融合与 noisy-OR 聚合伪代码

```
算法 A-1: 多源证据融合业务-代码映射
输入: 能力节点集合 C, 结构节点集合 S, 证据源配置
输出: business_map 边集合 E

1.  for each 能力节点 c in C:
2.      evidences = {}   // 目标节点 -> 证据列表
3.      // 文档提取源
4.      for module in extractModulesFromSpec(c.spec):
5.          if module in S: addEvidence(evidences, module, 'doc-extract', 0.85)
6.      // 命名匹配源
7.      for target in S:
8.          w = nameMatch(c.name, target.name)   // 0.45/0.5/0.6
9.          if w > 0: addEvidence(evidences, target, 'name-match', w)
10.     // 语义匹配源（向量索引存在时）
11.     if vectorIndexExists:
12.         for target in topK(c.vector, S_{L1,L2}, K=5):
13.             if sim(c, target) >= threshold:
14.                 addEvidence(evidences, target, 'semantic', min(0.7, sim*0.6))
15.     // Git 历史源（Git 仓库时）
16.     if isGitRepo and gitHistoryEnabled:
17.         files = gitLogFiles(c.name, c.keywords)
18.         for f, freq in countFreq(files):
19.             if freq >= 2: addEvidence(evidences, f, 'git-history', min(0.7, norm(freq)*0.6))
20.     // noisy-OR 聚合
21.     for target, evs in evidences:
22.         W = 1; topSource = evs[0].source
23.         for ev in evs:
24.             W = W * (1 - ev.weight)
25.             if rank(ev.source) > rank(topSource): topSource = ev.source
26.         W = min(0.95, 1 - W)
27.         if W >= 0.3: E.add(c, target, W, source=topSource)
28. return E
```

### A.2 置信度衰减加权锚点选择伪代码

```
算法 A-2: 置信度衰减加权锚点选择
输入: 查询向量 q, 节点集合 N, 衰减系数 α=3.0, Top-K
输出: 锚点集合 A

1.  scores = {}
2.  for node in N:
3.      scores[node] = cosineSim(q, node.vector)
4.  Conf_C = max(scores[n] for n in N if n.level == 'C')
5.  w_L1 = exp(-α * Conf_C)
6.  for node in N:
7.      if node.level == 'L1':
8.          scores[node] = scores[node] * w_L1
9.  A = topK(scores, K)
10. return A
```

### A.3 Token 预算五级迭代降级伪代码

```
算法 A-3: Token 预算迭代上下文生成
输入: 锚点 A, 预算 budget, 初始配置 config(loose, N, depth, wmin)
输出: 上下文文本 ctx

1.  for iter in 1..8:
2.      ctx = serialize(A, config)
3.      T = estimateTokens(ctx)
4.      if T <= budget: return ctx
5.      // 五级降级
6.      if config.compression == 'loose': config.compression = 'standard'
7.      elif config.compression == 'standard': config.compression = 'extreme'
8.      elif config.nodeLimit > 10: config.nodeLimit = config.nodeLimit / 2
9.      elif config.depth > 1: config.depth = config.depth - 1
10.     elif config.wmin < 0.9: config.wmin = config.wmin + 0.1
11.     else:
12.         ctx = serialize(AOnly, extreme)   // 最终降级：仅锚点
13.         return ctx + warning('预算不足')
14. return ctx
```

### A.4 实验数据补充

实验一至实验五的完整原始数据见正文表 5-1 至表 5-5。其中，实验一的基准标注由两名标注者独立完成，Cohen's Kappa 一致性系数为 0.82；实验三的信息保留率通过 30 道代码问答与 20 道关系推理题自动评估；实验五的开发者为 6 名具有 2–5 年 Web 开发经验的工程师，采用被试内交叉设计以控制个体差异。

---

## 致  谢

本论文的完成，离不开许多人的帮助与支持。

首先，衷心感谢我的导师。从选题确立、方案设计到论文撰写，导师始终给予我悉心的指导与耐心的修改。导师严谨的治学态度、敏锐的学术洞察力和勤勉的工作作风，使我深受教益，将成为我未来工作和学习的榜样。

感谢实验室的各位老师和同学。在系统实现与实验验证阶段，大家提出了许多宝贵的意见和建议，尤其是在多源证据融合的评估方法、Token 估算公式的校准等问题上，与同学们的讨论让我受益匪浅。

感谢参与实验五实用性评估的各位开发者，是你们的时间与反馈让本研究的工程价值得以验证。

感谢开源社区。本系统构建在 Tree-sitter、@xenova/transformers、web-tree-sitter 等优秀开源项目之上，OpenSpec、superpowers、Claude Code Skill 等工作为本研究提供了重要的方法论启发。开源精神是推动技术进步的重要力量。

感谢我的家人。在漫长的求学与研究过程中，是你们无私的爱与默默的支持，给予我坚持完成学业的勇气和力量。

最后，感谢所有在本论文撰写过程中给予过我帮助的师长、同窗和朋友。因学识所限，论文中难免有不足之处，恳请各位专家、老师批评指正。

---

## 作者简历及在学研究成果

### 个人简历

××××年××月出生于××省××市。××××年××月考入××大学××学院××专业攻读学士学位，××××年××月毕业并获得工学学士学位。××××年××月免试推荐（或考取）至××大学××学院××专业攻读硕士（或博士）学位，研究方向为人工智能软件工程、AI 辅助开发与知识图谱。

### 在学研究成果

**一、参与的科研项目**

1. ××××××项目，项目编号：××××，起止时间：××××年××月—××××年××月，主要参与人。
2. ××××××项目，项目编号：××××，起止时间：××××年××月—××××年××月，主要参与人。

**二、发表的学术论文**

1. ×××, ×××, ×××. ×××××××××[J]. ×××学报, ××××, ××(×): ××-××. （SCI/EI/中文核心，已收录/已录用/已发表）
2. ×××, ×××, ×××. ×××××××××[C]//×××国际会议论文集. ××××: ××-××.

**三、申请的知识产权**

1. 发明专利：×××××××××，申请号：××××，申请日：××××年××月××日。（已授权/实质审查中）
2. 软件著作权：×××××××××，登记号：××××，登记日：××××年××月××日。

**四、获得的奖励与荣誉**

1. ××××××奖学金，××××年。
2. ××××××竞赛××等奖，××××年。

---

## 独创性说明

本人郑重声明：所呈交的学位论文，是本人在导师的指导下，独立进行研究工作所取得的成果。除文中已经注明引用的内容外，本论文不包含任何其他个人或集体已经发表或撰写过的作品成果。对本文的研究做出重要贡献的个人和集体，均已在文中以明确方式标明。本人完全意识到本声明的法律结果由本人承担。

学位论文作者签名：　　　　　　　　　日期：　　　年　　月　　日

导师签名：　　　　　　　　　　　　　日期：　　　年　　月　　日

---

## 关于论文使用授权的说明

本人完全了解学校有关保留、使用学位论文的规定，同意学校保留并向国家有关部门或机构送交论文的复印件和电子版，允许论文被查阅和借阅。本人授权学校可以将本学位论文的全部或部分内容编入有关数据库进行检索，可以采用影印、缩印或扫描等复制手段保存和汇编本学位论文。

保密的学位论文在解密后适用本授权书。

学位论文作者签名：　　　　　　　　　日期：　　　年　　月　　日

导师签名：　　　　　　　　　　　　　日期：　　　年　　月　　日

---

## 学位论文数据集

| 数据项 | 内容 |
|--------|------|
| 关键词（中） | AI 辅助开发；工作流引擎；知识图谱；上下文压缩；多源证据融合；noisy-OR 模型；大语言模型 |
| 关键词（英） | AI-assisted development; workflow engine; knowledge graph; context compression; multi-source evidence fusion; noisy-OR model; large language model |
| 中图分类号 | TP311.5 |
| 学科分类号 | 520.60（软件工程） |
| 论文语种 | 中文 |
| 学科、专业 | ××××—软件工程 / 计算机科学与技术 |
| 学位级别 | 硕士（或博士） |
| 研究方向 | 人工智能软件工程、AI 辅助开发与知识图谱 |
| 作者姓名 | ××× |
| 学号 | ×××××××× |
| 导师姓名（职称） | ×××（教授） |
| 培养单位 | ×××大学×××学院 |
| 答辩日期 | ××××年××月 |
| 论文总页数 | ×× 页 |
| 参考文献总数 | 25 篇 |
| 电子版论文提交方式 | 光盘 / 网络 |
| 数据集价值 | 本论文提出的 wpw 系统及知识图谱上下文压缩方法，可为 AI 辅助 Web 开发、代码知识图谱构建、LLM 上下文工程等方向的研究与工程实践提供参考。 |
