# 第 6 章 实验设计与评估

本章检验第三章提出的项目知识组织与任务上下文生成方法。实验不把不同 AI 工具作为竞赛对象，而是在规格驱动开发（Specification-Driven Development，SDD）作为共同基础的条件下，比较 OpenSpec 基线与加入知识图谱（Knowledge Graph，KG）驱动上下文工程的 wpw 方法，考察 KG 对上下文构造和下游软件任务的增量作用。

## 6.1 实验目标、研究问题与假设

### 6.1.1 实验目标

已有研究表明，规格 grounding 和 specification-first workflow 能够为 AI 软件开发提供更明确的任务约束[7][60][61][62]。另一方面，代码库中的代码实体、接口、数据表及其依赖关系适合用图结构组织[38][39][40]；AST 派生图、Repository KG 和 KG-Agent 研究分别从代码库可靠性、仓库问答和 Agent 推理记忆角度说明了关系化知识的价值[63][64][65]。因此，本章将 SDD 作为共同开发基础，把“是否使用 KG 驱动的项目知识组织和上下文生成”作为主要实验变量。

实验目标包括：

1. 比较 OpenSpec 与 wpw 在相同 SDD 任务下的上下文构造效率和任务实现效果；
2. 分离知识图谱和业务—代码映射各自的作用；
3. 观察项目知识在多轮需求演化中的复用、成本变化和召回稳定性。

本章不评价美食推荐算法的准确率，不比较基础模型能力，也不据此建立 AI 工具排行榜。实验结论限定在当前系统、任务集、模型版本和受控开发条件内。

### 6.1.2 研究问题

- **RQ1：** 在相同 SDD 流程下，KG 驱动的项目知识组织是否能够提高任务相关代码的召回质量？
- **RQ2：** KG 驱动的任务上下文生成是否能够在保持必要信息的同时减少累计上下文 Token 和完成任务所需的模型总 Token？
- **RQ3：** 在连续需求演化中，Spec 增量知识机制是否能够降低后续任务的边际开发成本并保持稳定的上下文召回？

### 6.1.3 研究假设

- **H1（上下文效率假设）：** 与 OpenSpec 基线相比，wpw 的累计上下文 Token 消耗更低。
- **H2（相关性假设）：** 在相同任务和预算条件下，wpw 的任务相关代码召回率更高；Context Precision 作为补充指标报告。
- **H3（任务效果假设）：** wpw 能够降低完成任务所需的模型总 Token、提高验收通过率或减少返工次数。
- **H4（演化假设）：** 随着 T4 知识增量更新，wpw 的后续轮次成本增长低于初始冷启动成本，且 Context Recall 不出现持续下降。

以上假设是待检验命题。若数据不支持某一假设，仍如实报告非显著结果和失败案例。

## 6.2 实验对象与任务集

### 6.2.1 实验对象

实验对象是第五章的美食推荐系统软件开发过程。实验单元定义为“某一种方法完成某一个可独立验收任务的一次运行”，而不是将整个系统作为一个不可重复的样本。

任务不等同于单个代码模块。一个任务可以同时涉及多个模块、接口、数据表和页面。例如，购物车与订单流程同时涉及购物车、订单、地址和结算页面。

### 6.2.2 任务集与开发场景

| 任务集 | 场景 | 任务内容 | 用途 |
|---|---|---|---|
| T1 | 基础搭建 | 用户认证、美食分类和美食信息管理 | 主实验与消融 |
| T2 | 业务核心 | 个性化推荐、购物车和订单流程 | 主实验与消融 |
| T3 | 迭代增强 | 收藏、评论、美食资讯和个人中心 | 扩展验证 |
| T4 | 长期演化 | 会员积分、优惠券、会员等级和配送规则 | 连续演化 |

核心任务进一步拆分为：

- T1-1 用户注册与登录；
- T1-2 美食分类和美食信息管理；
- T2-1 个性化推荐接口；
- T2-2 购物车与订单流程。

T3 的扩展功能记录完成情况、模型总 Token 和返工次数，不对每项功能设置复杂的独立统计模型。T4 依次执行四轮需求，每轮从上一轮的代码、规格和知识状态继续。

### 6.2.3 任务规模与验收标准

每个任务在执行前记录需求点数量、涉及文件数、函数或接口数、业务模块数、数据表数、前端页面数和测试用例数。每个核心任务配备固定验收清单，例如：

- 注册成功后能够登录，失败时返回正确提示；
- 美食分类和美食信息能够完成新增、修改、删除和查询；
- 推荐接口能够根据既定规则返回结果；
- 购物车能够增加、修改和删除商品；
- 订单能够提交，并完成规定的状态流转。

任务规模和验收清单在实验开始前冻结，不因某一方法的实现结果而修改。

## 6.3 对比方法与实验控制

### 6.3.1 OpenSpec 基线

OpenSpec 作为代表性 SDD 基线，使用 proposal、spec、design 和 tasks 等规格制品组织变更，并按既定阶段推进实现。为保证比较公平，实验为两种方法规定等价的需求澄清、设计、计划、测试和实施阶段；OpenSpec 的制品映射到这些阶段后，基线可以读取规格制品和按统一规则选取的相关文件，但不使用本文的项目 KG、业务—代码映射和图谱驱动子图生成。

如果实验采用对 OpenSpec 流程的复现而不是官方程序，应在实验环境表中标明“OpenSpec-based SDD baseline”，同时记录复现脚本版本和提交标识，避免将自定义复现误称为官方工具。

### 6.3.2 wpw 方法

wpw 与 OpenSpec 共享需求输入、任务划分、阶段目标和验收标准，并在等价的 SDD 制品层基础上增加：

- Capability-Code 双层知识模型；
- 业务能力与代码元素的显式映射；
- 图谱驱动的锚点选择和子图裁剪；
- 骨架抽取、序列化和 Token 预算控制；
- Spec 驱动的知识增量更新。

因此，主实验的比较对象不是“有流程”与“无流程”，而是“传统 SDD 上下文组织”与“KG 驱动上下文组织”。若具体制品名称或阶段实现无法完全对齐，结果解释为两种 SDD 方法的综合差异，不单独归因于 KG。

### 6.3.3 其他方法的处理

普通对话式开发缺少稳定的 SDD 制品和阶段流程，不作为主要实证基线。Superpowers 主要作为技能化 Agent 工作流的相关方法参照，在第一章和第二章说明其定位，不纳入本章独立统计。本文不据此推断 OpenSpec、Superpowers 或普通对话式开发的绝对优劣。

### 6.3.4 控制变量与重复规则

固定以下条件：

- 相同需求说明、任务划分和验收清单；
- 相同初始代码快照、技术栈和基础模型及版本；
- 相同开发者、Token 预算和工具调用权限；
- 相同测试环境和代码提交规则；
- OpenSpec 与 wpw 使用独立分支，不共享对方生成的代码或知识制品。

所有实验统一遵循“当前对话是唯一 Agent，Token 取当前对话”的最高执行原则。每次运行由当前 Codex 对话直接完成规格、实现、测试、验收、数据清理和事件记录，不得启动、嵌套或委派第二个 Agent；模型 Token 只能取该当前对话中执行本实验任务的 `task_started`—`task_complete` 回合，不得混入子 Agent、其他对话或后续分类与文档维护回合的用量。`sdd-eval` 只承担工作区与记录管理、验收、计算和落盘，不作为第二个执行 Agent。

任务执行顺序随机化，工具版本、模型参数、随机种子和提交标识在附录中记录。wpw 首次源码扫描、能力规范生成、图谱构建和索引建立产生的模型 Token 计入冷启动成本。

T1 和 T2 包含 4 个核心任务，T3 包含 4 个扩展任务，T4 包含 4 轮连续演化任务。每项任务均由 OpenSpec 和 wpw 各运行 1 次，因此总体方法比较共形成 12 个任务配对、24 次运行；消融实验另包含 8 次关闭组件的运行。每次运行保存需求输入、规格制品、上下文内容、模型日志、代码提交或候选版本哈希、测试结果和人工修改记录。单次运行结果用于任务级描述与跨任务配对，不用于估计同一任务内的运行波动。

### 6.3.5 sdd-eval 的系统结构与工作原理

为统一实验执行与数据采集，本文实现了独立的 **SDD 开发实验执行与评估工具** `sdd-eval`（Node.js/TypeScript 命令行工具）。系统采用“配置—任务—执行—采集—评价—报告”的分层结构：

1. **配置层**：读取 `eval.config.json/yaml`，冻结项目源目录或快照、当前对话执行标识、超时终止规则、重复次数和 Token 统计方式；正式实验不配置二次 Agent 启动命令；
2. **任务层**：从内置任务集或 `tasks.yaml` 读取任务说明、场景、Gold 代码元素和验收检查项；
3. **工作区与调度层**：从同一源快照创建隔离工作区，过滤 `.git`、`node_modules`、`results` 和 `evaluator-private`，执行可选初始化命令，并为当前对话登记指定方法和运行编号，不再调度其他 Agent；
4. **方法适配层**：`OpenSpecAdapter` 对仓库进行确定性上下文扫描；`WpwAdapter` 优先读取 wpw 输出的 `.wpw-context.json` 或 `wpw-context.json`，先记录 `anchors`、`subgraph` 和 `compressedText` 作为知识导航层，再沿子图文件边界展开任务说明、导航摘要和源码内容，形成综合上下文观测；缺少该文件时使用受限的源码扫描作为回退。综合上下文观测作为当前对话这一唯一 Agent 的实际输入边界记录；
5. **验收与指标层**：执行任务中声明的命令或评测侧私有脚本，依据 Gold 元素和统一分词器计算累计上下文 Token、Recall、Precision、模型总 Token、验收通过率和返工代理值；
6. **结果层**：将每次运行的 `RunRecord`、标准输出、错误输出、事件日志和工作区索引保存到 `results/`，并由 `report` 命令按方法、任务或场景筛选汇总。

在实现上，`config.ts` 和 `tasks.ts` 负责配置与任务加载，`workspace.ts` 负责工作区复制和外部命令执行，`adapters.ts` 负责方法接入，`runner.ts` 编排单次运行和验收，`metrics.ts` 负责指标计算与汇总，`index.ts` 提供命令行入口。模块之间通过任务对象、上下文观测和 `RunRecord` 传递数据，保持评测器与被测方法解耦。

一次运行的基本流程为：

```text
冻结任务与版本 → 创建隔离工作区 → 接入 OpenSpec 或 wpw
        → 当前对话这一唯一 Agent 执行并记录事件 → 执行统一测试与验收
        → 汇总指标 → 生成可复现的结果文件
```

其核心原理是“同一输入、同一约束、仅替换方法适配器”。`sdd-eval` 为每次运行生成唯一 `run_id`，创建独立工作区并记录运行状态，但不再启动或嵌套另一个 Agent；任务模型总 Token 由评测器从当前对话的任务起止事件中读取，而不是由被测方法自行报告。这样可以在不改变 wpw 或 OpenSpec 内部开发逻辑的情况下，使用同一工作区隔离、超时、验收和指标计算规则进行比较。

为允许开发过程中的自主探索，执行器向 Agent 提供运行专属的上下文文件和事件文件路径（环境变量 `SDD_EVAL_CONTEXT_FILE`、`SDD_EVAL_EVENTS_FILE`、`SDD_EVAL_WORKSPACE`）。OpenSpec 和 wpw 均可在开发过程中自主追加搜索、图谱查询和源码读取，并按 JSONL 事件记录 `search`、`graph_query`、`source_read`、`context_append` 和 `model_call`。执行器只汇总 Agent 实际声明的探索轨迹和模型输入/输出 Token，不根据私有 Gold 推断遗漏文件、自动补齐上下文，也不以 Gold 命中作为停止条件。初始导航指标与全过程累计上下文指标分开保存；Gold 仅在运行结束后用于计算 Recall 和 Precision。由 Codex 当前对话直接执行的实验，其模型 Token 统一取执行该任务的当次对话回合：以匹配的 `task_started` 为起点，以 `task_complete` 为终点，读取终点前最后一条 `event_msg/token_count.total_token_usage`。后续分类、文档维护或讨论回合不计入先前任务；缓存输入和推理输出仅作诊断字段，不重复加入总 Token。

`sdd-eval` 不属于 OpenSpec 或 wpw 的内部模块，不实现被测的上下文生成方法，也不作为新的开发方法参与比较。wpw 仅输出必要的结构化上下文文件和过程日志；跨方法的上下文 Token、模型总 Token、Recall、Precision 和验收结果均由 `sdd-eval` 统一计算。评测侧的 Gold 标注和私有验收脚本不复制到 Agent 工作区。工具支持 `init`、`tasks`、`run`、`report` 和 `clean` 命令，其中 `--mock` 仅用于检查调度和数据链路，不作为正式实验结果。该工具作为支撑本章实验的本地命令行工程，不构成本文的独立方法创新。

## 6.4 评价指标与数据采集

### 6.4.1 上下文构造效率

记录：

- 综合上下文 Token 数（任务说明、导航摘要和实际注入的源码/规格内容）；
- KG 导航摘要 Token 数（仅作过程诊断，不作为最终上下文效果判定）；
- 模型输入和输出的总 Token 数；
- wpw 冷启动 Token。

累计上下文 Token 是初始任务与导航内容，以及 Agent 在开发过程中通过搜索、图谱查询和源码读取所追加内容的 Token 总和。OpenSpec 和 wpw 使用同一任务、同一代码快照和同一分词规则进行统计。KG 导航摘要单独记录为过程变量，不与累计上下文 Token 混用。

### 6.4.2 上下文相关性

对每个任务预先标注真实涉及的代码元素集合 $R$，并记录综合上下文实际提供的元素集合 $C_{ctx}$。Recall 和 Precision 只在 $C_{ctx}$ 上计算；KG 导航得到的锚点集合 $C_{nav}$ 仅报告为导航诊断指标，不作为最终结果判断依据。正式比较先检查 Recall 和 Precision 是否达到预设下限，再比较满足质量约束的综合上下文 Token。

$$\mathrm{Recall}_{ctx} = \frac{|R \cap C_{ctx}|}{|R|} \tag{6-1}$$

Context Precision：

$$\mathrm{Precision}_{ctx} = \frac{|R \cap C_{ctx}|}{|C_{ctx}|} \tag{6-2}$$

Recall 作为主指标，Precision 作为补充指标；$\mathrm{Recall}_{nav}$ 和 $\mathrm{Precision}_{nav}$ 只用于诊断 KG 是否成功定位候选范围。标注可在文件和函数两个粒度进行；若有两名标注者，报告 Cohen's kappa，否则报告标注规则、抽样复核过程和争议处理方式。

### 6.4.3 Agent 任务效果

任务资源效率以完成任务所需的模型总 Token 衡量，数值越低表示在相同验收要求下消耗的模型计算资源越少。

记录：

- 完成任务所需的模型总 Token；
- 需求验收通过率或需求覆盖度；
- 返工次数；
- 任务失败或中断情况。

验收由固定清单判断，返工定义为已提交实现因测试失败、验收不通过或需求遗漏而产生的再次修改。若没有独立缺陷登记，不额外构造单位代码缺陷率。

未配置可执行命令或脚本的验收项标记为“未执行”并判为不合格，不再自动计为通过；正式任务必须提供可运行的验收命令或评测侧私有脚本。

在当前 `sdd-eval` 实现中，`reworkCount` 记录未通过的验收检查项数，作为返工的可观测代理指标；正式实验若补充 Git 提交或事件级修改记录，则以实际再次修改次数替换该代理值，并在结果表中注明统计口径。

### 6.4.4 长期演化指标

T4 不新增一套指标，而是观察前述指标随轮次的变化：

- 单轮和累计综合上下文 Token，以及模型输入/输出 Token；
- 每轮 Context Recall；
- 每轮返工次数；
- 新增知识节点、更新关联和复用知识节点数量。

### 6.4.5 sdd-eval 的数据采集与结果文件

正式运行前，使用 `sdd-eval init` 写入评估配置并创建结果目录，使用 `sdd-eval tasks` 检查冻结任务集；随后通过 `sdd-eval run --task <任务编号> --method <openspec|wpw> --repetitions <次数>` 执行重复实验。每次运行生成唯一运行编号，并在 `results/` 下保存一条 JSON 记录及对应的标准输出、错误输出和事件日志；隔离工作区保存在 `results/workspaces/`。`sdd-eval report` 可按方法、任务或场景筛选记录并输出汇总 JSON；当前命令提供运行级记录和分组均值，汇总时只对实际存在的数值求均值，整组缺失的指标保留为 `null` 而不转换为 0。论文要求的中位数、四分位距、配对检验和统计图表在此基础上由后处理脚本生成。

单次运行记录至少包含任务编号、方法、重复编号、运行状态、初始导航观测、探索事件序列、累计上下文观测、模型输入/输出用量、验收明细、各项指标、日志路径和运行环境元数据。运行状态区分 `passed`、`failed`、`timeout`、`interrupted` 和 `error`；失败或超时记录保留在原始数据中，不通过删除异常运行改善结果。上下文 Token 按统一分词规则计算，正式报告中记录统计模式及其版本；模型 Token 读取当次任务对话的精确 `token_count`。由于 `task_complete` 在任务回复结束后写入，允许在下一回合从会话日志回填，但必须保存会话标识、任务起止事件和来源事件。

## 6.5 主实验：OpenSpec 与 wpw 对比

### 6.5.1 实验流程

1. 在 `sdd-eval` 中冻结同一代码快照、任务配置、验收清单、当前对话唯一 Agent 规则、超时和重复次数；
2. 从该快照为每次运行创建独立工作区，并执行必要的环境初始化；
3. 通过 `OpenSpecAdapter` 或 `WpwAdapter` 获取对应方法的任务上下文，并将其提供给当前对话这一唯一 Agent，不启动或委派第二个 Agent；
4. 由当前对话直接完成开发并记录输出、错误、事件和代码工作区，直至任务完成、失败或超时；
5. 在评测侧执行任务声明的验收命令或私有验收脚本，计算验收通过率并保留每项检查结果；
6. 当前任务回复完成后，读取同一对话对应 `task_complete` 前的精确模型 Token，由 `sdd-eval` 写入运行级 JSON 记录，再按任务、方法和轮次汇总成本、上下文相关性和任务效果指标。

### 6.5.2 核心比较

主结果重点报告：

- 任务验收通过率和失败情况；
- 当前对话执行本任务的全过程模型输入/输出 Token 与累计综合上下文 Token；
- 初始导航 Token、搜索/图谱查询/源码读取次数；
- Context Recall 及补充的 Precision，用于解释差异来源；
- 返工次数。

结果按核心任务和任务集分组展示，并同时报告绝对差值和相对变化率。

### 6.5.3 结果解释边界

若 wpw 表现更好，结论只能表述为：在本系统、指定任务、模型版本和控制条件下，KG 驱动上下文组织相对于 OpenSpec 基线表现出相应差异。结果不能直接推广到所有项目，也不能据此声称 wpw 一定优于 OpenSpec 或 Superpowers。

## 6.6 KG 机制消融实验

消融实验在 wpw 内部进行，目的是定位增量收益来源，而不是重新比较工具。

### 6.6.1 WPW-KG：关闭项目知识图谱

保留 SDD 流程和基本任务执行，关闭 Capability-Code 知识模型与图谱检索，改为直接读取原始文件。该组用于检验项目知识组织是否带来上下文和任务效果的变化。

### 6.6.2 WPW-Map：关闭业务—代码映射

保留代码结构图和基本检索，移除业务能力到代码元素的显式映射。该组用于检验 Capability 作为语义锚点的作用。

### 6.6.3 消融指标与规模

消融组统一记录累计上下文 Token、模型总 Token、Recall、验收通过率和返工次数。实验仅开展完整 wpw 与 WPW-KG、WPW-Map 的配对比较，分别观察项目知识图谱和业务—代码映射被移除后的变化。

## 6.7 长期演化实验

### 6.7.1 T4 需求序列

在独立的 OpenSpec 和 wpw 分支上，按以下顺序连续执行：

1. 会员积分；
2. 优惠券；
3. 会员等级；
4. 配送规则。

每轮均从上一轮的代码、SDD 制品和项目知识状态继续，不允许回到初始快照重新开始。

### 6.7.2 记录与分析

记录每轮模型总 Token、上下文 Token、Context Recall、返工次数以及新增和复用的知识节点。分别绘制单轮值和累计值，区分 wpw 的一次性冷启动成本与后续增量成本。

长期实验重点回答：

- 知识积累是否减少后续上下文构造成本；
- Recall 是否在多轮迭代中保持稳定；
- 返工是否随知识复用而减少；
- 哪些需求仍然需要人工补充或重新定位。

## 6.8 数据处理与统计分析

### 6.8.1 数据清洗

预先规定运行失败、中断、超时和缺失日志的处理方式，保留原始记录，不以删除异常运行的方式改善结果。所有 Token 使用统一统计口径。本章只报告实验设备实际采集并汇总的观测值，缺失项保持为空。

### 6.8.2 描述性统计

按方法、任务和轮次报告样本量、均值、中位数、标准差或四分位距、最小值、最大值及缺失情况。主表优先呈现中位数和四分位距，避免小样本下单一均值掩盖波动。

### 6.8.3 配对比较与效应量

由于同一任务由两种方法分别完成，分析以任务级配对差值为主。模型总 Token 的主要分析合并 T1—T4，共使用 12 个 OpenSpec—wpw 任务配对进行双侧精确 Wilcoxon 符号秩检验，并报告检验统计量 $W$、精确 $p$ 值和配对秩二列相关系数 $r_{rb}$，显著性水平设为 $\alpha=0.05$。T1/T2 核心任务、T3 扩展任务和 T4 连续演化任务的分场景结果用于解释不同开发阶段的变化，不替代总体比较。

累计上下文 Token 在 T1—T3 中按单项任务统计，而 T4 表中记录的是随轮次累积的数值，两者统计含义不同，因此不合并为 12 个配对进行推断检验，只按场景报告变化。两项消融比较分别只包含 4 个核心任务配对，其检验结果主要用于辅助解释组件作用。全部任务均来自同一软件项目，且 T4 各轮继承上一轮状态，因而总体检验应理解为当前任务集上的条件性任务级证据，不能作为跨项目的随机抽样推断。

### 6.8.4 标注一致性与可复现性

上下文相关性标注在实验前冻结规则。两名标注者独立标注时报告 Cohen's kappa；存在分歧时保留原始标注、讨论记录和最终仲裁结果。T1—T4 的数据由其他实验设备采集后汇总，分析时同时保存设备标识、软硬件环境、模型配置、工具提交标识、分析脚本、数据清洗规则和随机种子。

## 6.9 实验结果

本节列出 T1—T4 的实验观测值。数据由其他实验设备采集并汇总，表中数值均来自实际运行记录。6.9.1—6.9.5 报告原始观测，6.9.6 在此基础上进行配对分析并回答研究假设与研究问题。

### 6.9.1 主实验运行与验收记录

| 任务 | 方法 | 规格与实现 | 项目测试 | HTTP/MySQL 验收 | 模型总 Token | 纳入统计 |
|---|---|---|---:|---|---:|---|
| T1-1 用户注册与登录 | OpenSpec | 完成 | 9/9 | 7/7 | 7,154,327 | 是 |
| T1-1 用户注册与登录 | wpw | 完成 | 10/10 | 通过 | 4,341,714 | 是 |
| T1-2 美食分类和美食信息管理 | OpenSpec | 完成 | 20/20 | 20/20 | 11,162,207 | 是 |
| T1-2 美食分类和美食信息管理 | wpw | 完成 | 11/11 | 19/19 | 3,549,765 | 是 |
| T2-1 个性化推荐接口 | OpenSpec | 完成 | 25/25 | MockMvc 契约 3/3；MySQL 连接通过；无数据写入 | 5,953,742 | 是 |
| T2-1 个性化推荐接口 | wpw | 完成 | 9/9 | HTTP 契约通过；MySQL 连接通过 | 4,489,079 | 是 |
| T2-2 购物车与订单流程 | OpenSpec | 完成 | 38/38 | 领域测试 6/6；MockMvc 契约 5/5；真实 MySQL 流程与清理复核通过 | 13,874,175 | 是 |
| T2-2 购物车与订单流程 | wpw | 完成 | 21/21 | MockMvc 契约 5/5；真实 MySQL 流程 1/1；清理复核通过 | 14,723,130 | 是 |

### 6.9.2 OpenSpec 与 wpw 主实验数据

| 任务 | 方法 | 样本数 | 验收通过率 | 模型总 Token | 累计上下文 Token | Recall<sub>ctx</sub> | Precision<sub>ctx</sub> | 返工次数 |
|---|---|---:|---:|---:|---:|---:|---:|---:|
| T1-1 用户注册与登录 | OpenSpec | 1 | 100% | 7,154,327 | 1,410 | 1.000 | 1.000 | 0 |
| T1-1 用户注册与登录 | wpw | 1 | 100% | 4,341,714 | 1,929 | 1.000 | 1.000 | 0 |
| T1-2 美食分类和美食信息管理 | OpenSpec | 1 | 100% | 11,162,207 | 558 | 0.833 | 1.000 | 0 |
| T1-2 美食分类和美食信息管理 | wpw | 1 | 100% | 3,549,765 | 660 | 1.000 | 0.893 | 1 |
| T2-1 个性化推荐接口 | OpenSpec | 1 | 100% | 5,953,742 | 1,376 | 0.750 | 1.000 | 0 |
| T2-1 个性化推荐接口 | wpw | 1 | 100% | 4,489,079 | 24,372 | 1.000 | 1.000 | 0 |
| T2-2 购物车与订单流程 | OpenSpec | 1 | 100% | 13,874,175 | 1,515 | 1.000 | 1.000 | 3 |
| T2-2 购物车与订单流程 | wpw | 1 | 100% | 14,723,130 | 3,749 | 0.800 | 1.000 | 2 |

### 6.9.3 T3 扩展验证数据

T3 包含收藏、评论、美食资讯和个人中心四项扩展任务。下表记录两种方法在各任务上的实际运行数据。

| 任务 | 方法 | 模型总 Token | 累计上下文 Token | 返工次数 |
|---|---|---:|---:|---:|
| T3-1 收藏 | OpenSpec | 6,804,127 | 1,283 | 2 |
| T3-1 收藏 | wpw | 4,151,806 | 884 | 0 |
| T3-2 评论 | OpenSpec | 10,068,110 | 1,851 | 3 |
| T3-2 评论 | wpw | 5,945,317 | 1,264 | 2 |
| T3-3 美食资讯 | OpenSpec | 6,746,634 | 1,258 | 1 |
| T3-3 美食资讯 | wpw | 4,221,184 | 948 | 0 |
| T3-4 个人中心 | OpenSpec | 9,377,821 | 1,811 | 4 |
| T3-4 个人中心 | wpw | 6,000,527 | 1,370 | 2 |

### 6.9.4 wpw 消融实验数据

下表列出完整 wpw、WPW-KG 和 WPW-Map 在四项核心任务上的实际运行数据。完整 wpw 行复用主实验中相同任务的观测值，另外两组为对应的消融运行。

| 任务 | 方法 | 验收通过率 | 模型总 Token | 累计上下文 Token | Recall<sub>ctx</sub> | Precision<sub>ctx</sub> | 返工次数 |
|---|---|---:|---:|---:|---:|---:|---:|
| T1-1 | 完整 wpw | 100.00% | 4,341,714 | 1,929 | 1.000 | 1.000 | 0 |
| T1-1 | WPW-KG | 100.00% | 12,462,641 | 24,035 | 1.000 | 1.000 | 0 |
| T1-1 | WPW-Map | 100.00% | 8,848,516 | 1,423 | 1.000 | 1.000 | 0 |
| T1-2 | 完整 wpw | 100.00% | 3,549,765 | 660 | 1.000 | 0.893 | 1 |
| T1-2 | WPW-KG | 100.00% | 5,289,061 | 990 | 1.000 | 1.000 | 3 |
| T1-2 | WPW-Map | 100.00% | 4,295,143 | 779 | 1.000 | 1.000 | 2 |
| T2-1 | 完整 wpw | 100.00% | 4,489,079 | 24,372 | 1.000 | 1.000 | 0 |
| T2-1 | WPW-KG | 100.00% | 6,554,059 | 32,171 | 1.000 | 1.000 | 3 |
| T2-1 | WPW-Map | 100.00% | 5,297,117 | 27,297 | 1.000 | 0.890 | 1 |
| T2-2 | 完整 wpw | 100.00% | 14,723,130 | 3,749 | 0.800 | 1.000 | 2 |
| T2-2 | WPW-KG | 100.00% | 19,728,997 | 5,361 | 1.000 | 1.000 | 2 |
| T2-2 | WPW-Map | 100.00% | 16,489,909 | 4,761 | 1.000 | 1.000 | 3 |

### 6.9.5 T4 长期演化数据

T4 按会员积分、优惠券、会员等级和配送规则的顺序连续执行。下表记录每轮的实际运行数据；OpenSpec 不维护本文定义的项目知识节点，因此相应单元以“—”表示。

| 轮次 | 需求 | 方法 | 验收通过率 | 单轮模型 Token | 累计上下文 Token | Recall<sub>ctx</sub> | 返工次数 | 新增 / 更新 / 复用知识节点 |
|---|---|---|---:|---:|---:|---:|---:|---|
| T4-1 | 会员积分 | OpenSpec | 100.00% | 9,206,342 | 1,894 | 1.000 | 2 | — |
| T4-1 | 会员积分 | wpw | 100.00% | 7,601,438 | 2,196 | 1.000 | 0 | 38/12/16 |
| T4-2 | 优惠券 | OpenSpec | 100.00% | 12,794,815 | 2,765 | 1.000 | 0 | — |
| T4-2 | 优惠券 | wpw | 100.00% | 6,536,772 | 3,272 | 1.000 | 0 | 25/17/24 |
| T4-3 | 会员等级 | OpenSpec | 100.00% | 7,422,618 | 4,009 | 1.000 | 0 | — |
| T4-3 | 会员等级 | wpw | 100.00% | 5,176,294 | 4,449 | 1.000 | 1 | 35/11/34 |
| T4-4 | 配送规则 | OpenSpec | 100.00% | 10,908,537 | 5,773 | 1.000 | 0 | — |
| T4-4 | 配送规则 | wpw | 100.00% | 6,418,126 | 5,339 | 1.000 | 1 | 21/16/49 |

### 6.9.6 配对分析与研究问题回答

将 T1—T4 作为同类软件开发任务合并后，共形成 12 个 OpenSpec—wpw 任务配对。OpenSpec 的模型总 Token 合计为 111,473,455，wpw 为 73,155,152，wpw 减少 34.37%；12 项任务中有 11 项的模型总 Token 更低，只有 T2-2 高于 OpenSpec。双侧精确 Wilcoxon 符号秩检验结果为 $W=1$、$p=0.00098$、$r_{rb}=-0.974$，达到 $\alpha=0.05$ 显著性水平，说明 wpw 在当前任务集上表现出显著的模型 Token 节约效果。负的 $r_{rb}$ 表示 wpw 的数值总体更低。

分场景观察时，T1/T2 核心任务中 wpw 的模型总 Token 比 OpenSpec 少 28.94%，但累计上下文 Token 多 532.02%，两种方法的返工总数均为 3；T3 扩展任务中，wpw 的模型总 Token 和累计上下文 Token 分别少 38.42% 和 28.00%，返工总数由 10 次降至 4 次；T4 长期演化中，wpw 的模型总 Token 少 36.20%，末轮累计上下文 Token 为 5,339，低于 OpenSpec 的 5,773，四轮 Recall 均为 1.000，返工总数均为 2。这些分场景结果用于说明收益在不同开发阶段的表现，其中每个场景仅有 4 个任务配对，不单独承担总体显著性结论。

消融数据进一步限定了机制解释。相对于完整 wpw，关闭 KG 后四项任务合计的模型总 Token 和累计上下文 Token 分别增加 62.47% 和 103.70%，返工总数由 3 次增至 8 次；关闭业务—代码映射后，两项 Token 指标分别增加 28.88% 和 11.56%，返工总数增至 6 次。关闭 KG 后模型总 Token 和累计上下文 Token 均为 $W=0$、$p=0.125$、$r_{rb}=1.00$；关闭业务—代码映射后，模型总 Token 为 $W=0$、$p=0.125$、$r_{rb}=1.00$，累计上下文 Token 为 $W=2$、$p=0.375$、$r_{rb}=0.60$。两项消融比较均只有 4 个任务配对，未达到 $\alpha=0.05$ 显著性水平，因此主要作为组件作用的描述性证据。完整 wpw 也未在所有任务上取得更高的 Recall 或 Precision，现有结果不能证明 KG 和业务—代码映射能够稳定提高上下文相关性。

| 假设或研究问题 | 判断 | 数据依据 |
|---|---|---|
| H1：累计上下文 Token 消耗更低 | 未得到稳定支持 | 主实验中 wpw 的累计上下文 Token 合计增加 532.02%，T3 中减少 28.00%，T4 末轮累计值减少 7.52%。不同任务阶段的变化方向不一致，不能认为 wpw 稳定降低上下文构造成本。 |
| H2：任务相关代码召回率更高 | 未得到充分支持 | 主实验中 wpw 的 Recall 在 2 个任务上更高、1 个任务相同、1 个任务更低，平均值为 0.950，OpenSpec 为 0.896；wpw 的平均 Precision 为 0.973，低于 OpenSpec 的 1.000。Recall 的配对检验为 $W=2$、$p=0.750$、$r_{rb}=0.33$，差异方向不稳定。 |
| H3：降低任务模型总 Token、提高验收通过率或减少返工 | 支持 | 在 T1—T4 的 12 个任务配对中，wpw 的模型总 Token 合计减少 34.37%，其中 11 项任务更低；配对检验结果为 $W=1$、$p=0.00098$、$r_{rb}=-0.974$。表中返工总数由 OpenSpec 的 15 次降至 wpw 的 9 次。 |
| H4：长期演化中边际模型 Token 下降且 Recall 稳定 | 支持 | wpw 在 T4-2、T4-3 和 T4-4 的模型总 Token 分别比 T4-1 低 14.01%、31.90% 和 15.57%；四轮 Recall 均为 1.000，复用知识节点数由 16 增至 49。 |
| RQ1：KG 对任务相关代码召回质量的影响 | 未观察到稳定提升 | 主实验 Recall 的任务级方向不一致；消融后 Recall 也未系统下降。现有数据表明 wpw 能维持较高召回，但不能把召回变化稳定归因于 KG。 |
| RQ2：KG 上下文生成对 Token 消耗的影响 | 模型总 Token 显著下降，累计上下文 Token 依赖场景 | 12 个任务配对的模型总 Token 合计减少 34.37%，且配对检验达到显著性水平；累计上下文 Token 在 T1/T2 中增加、在 T3 中减少，T4 末轮累计值低于 OpenSpec。 |
| RQ3：增量知识对长期演化成本和召回的影响 | 表现为正向作用 | T4 后三轮的模型总 Token 均低于首轮，Recall 始终为 1.000，复用知识节点逐轮增加，说明增量知识在当前连续需求序列中降低了后续轮次的模型 Token 成本并保持了召回稳定性。 |

## 6.10 实验结论

综合 T1—T4 的 12 个任务配对，OpenSpec 的模型总 Token 合计为 111,473,455，wpw 为 73,155,152，wpw 减少 34.37%；12 项任务中有 11 项呈下降趋势。配对检验结果为 $W=1$、$p=0.00098$、$r_{rb}=-0.974$，表明 wpw 在当前任务集上具有显著的模型 Token 节约效果。分场景结果进一步显示，T1/T2、T3 和 T4 的模型总 Token 分别减少 28.94%、38.42% 和 36.20%，说明这种优势并非只由单一开发阶段产生。

wpw 并未在所有任务上同时减少累计上下文 Token。T1/T2 核心任务中的累计上下文 Token 增加 532.02%，T3 中减少 28.00%，T4 末轮累计值比 OpenSpec 低 7.52%。因此，现有结果支持 wpw 降低完成任务所需的模型总 Token，但不支持其稳定降低上下文构造成本。T4 中复用知识节点数由 16 增至 49，后三轮模型总 Token 均低于首轮，表明前期建立的项目知识在连续需求演化中产生了复用价值。

消融实验显示，关闭 KG 或业务—代码映射都会增加模型总 Token、累计上下文 Token 和返工次数，其中关闭 KG 的增幅更大。这说明项目知识图谱是 Token 成本控制的主要组成，业务—代码映射则提供了进一步的语义定位收益。不过，主实验和消融实验中的 Recall、Precision 变化并不一致，现有数据不能证明 KG 必然提高任务相关代码的召回质量。

因此，在本文的美食推荐系统、任务集和实验配置范围内，H1 未得到稳定支持，H2 未得到充分支持，H3 获得统计与描述性支持，H4 获得描述性支持。wpw 更适合作为面向多轮需求演化和知识复用的 SDD 上下文工程方法，其主要收益是降低模型总 Token，而不是保证每个任务都减少累计上下文 Token。总体模型 Token 比较包含 12 个任务配对并达到显著性水平，分场景和消融比较则各只有 4 个任务配对，主要用于解释阶段差异和组件作用。由于所有任务均来自同一软件项目，且 T4 各轮存在状态继承，本章结论仍是受当前任务范围约束的实证结论，不能直接推广到其他项目、模型或开发环境。
---

**本章参考文献映射**（沿用全文编号，正式合稿时统一核对）：

- [7] Piskala K. Spec-Driven Development: From Code to Contract in the Age of AI Coding Assistants[EB/OL]. arXiv:2602.00180, 2026.
- [38] Yang B, et al. KGCompass: Enhancing Repository-Level Software Repair via Repository-Aware Knowledge Graphs[EB/OL]. arXiv:2503.21710, 2025.
- [39] Athale M, et al. Knowledge Graph Based Repository-Level Code Generation[EB/OL]. arXiv:2505.14394, 2025.
- [40] Cherny-Shahar T, Yehudai A. Repository Intelligence Graph: Deterministic Architectural Map for LLM Code Assistants[EB/OL]. arXiv:2601.10112, 2026.
- [60] Haeri A, Ghelichi M. Specification Grounding Drives Test Effectiveness for LLM Code[EB/OL]. arXiv:2607.06636, 2026.
- [61] Tufano M, et al. Grounding AI Agents in Contracts: An Empirical Evaluation of Spec-Driven Test Generation[EB/OL]. arXiv:2608.17177 / SpecOps 2026, 2026.
- [62] Saha S, et al. From Vibe Coding to Specification-Driven Agentic Development: A Taxonomy, Lifecycle Model, and Artifact-Based Empirical Analysis[EB/OL]. 2026.
- [63] Chinthareddy M R. Reliable Graph-RAG for Codebases: AST-Derived Graphs vs LLM-Extracted Knowledge Graphs[EB/OL]. 2026.
- [64] Abedu S, Khatoonabadi S H, Shihab E. Synergizing LLMs and Knowledge Graphs for Software Repository-Related Question Answering[J]. ACM TOSEM, 2026.
- [65] Jiang J, et al. KG-Agent: An Efficient Autonomous Agent Framework for Complex Reasoning over Knowledge Graph[C]. ACL 2025.
